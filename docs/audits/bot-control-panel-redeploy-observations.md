# Bot Control Panel Redeploy Observations

## 2026-07-07T16:12:10Z - dep_val_smoke_002

- Bot: `dep_val_smoke_002`
- Candidate reason: Different from `Bars-July-6`; fleet list showed `BLOCKED`, flat exposure, no live runtime.
- Action taken: Opened `/broker/bots`, selected `dep_val_smoke_002`, used `Fresh run`, verified IBKR paper account `DUM284968`, `paper_orders`, `safe_canary`, `Start trading immediately`, and added required action plan `leg_1 long SPY x 1` with `CLOSE leg_1`.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: `Deploy & start` was blocked before creating a new run. No order/fill observed.
- Exact UI error: `Deploy — blocked dep_val_smoke_002 is durably STOPPED. Resume the bot to clear the stop latch before starting or using Deploy & start. Use Resume to set desired_state=RUNNING, then start the bot. Use Deploy only when you want to stage a new run without starting it.`
- Related UI state: On the bot detail page, `Resume trading` was disabled with `Broker safety is unknown. The backend cannot prove the broker is paper-only. Resume is held until broker safety is proven.`
- UX recommendation: Avoid sending the operator into a dead end. If `Deploy & start` requires clearing a durable stop latch, the deploy form should surface that before submit and provide a single guided recovery path. Either enable a safe `Resume + deploy/start` path once the global IBKR paper session is proven, or explain why per-bot broker proof is missing and what action will create it. Also keep blocking empty `deployment_validation` action plans before submit; this form still allowed a ready deploy state while the action plan was initially empty.

## 2026-07-07T16:42:10Z - DIagVal6

- Bot: `DIagVal6`
- Candidate reason: Different from `dep_val_smoke_002` and `Bars-July-6`; fleet list showed `BLOCKED`, flat exposure, `Fresh run only`, no live runtime, and 3 errors.
- Action taken: Opened `/broker/bots`, selected `DIagVal6`, used the detail-page `Fresh run` action, verified IBKR paper account `DUM284968`, `paper_orders`, `safe_canary`, `Start trading immediately`, then added required action plan `leg_1 long SPY x 1` with `CLOSE leg_1`.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: `Deploy & start` was blocked before creating a new run. No order/fill observed.
- Exact UI error: `Deploy — blocked DIagVal6 is durably STOPPED. Resume the bot to clear the stop latch before starting or using Deploy & start. Use Resume to set desired_state=RUNNING, then start the bot. Use Deploy only when you want to stage a new run without starting it.`
- Related UI state: The bot table and detail controls now clearly showed `Fresh run only`. On the detail page, `Start bot process`, `Resume trading`, `Pause trading`, `Flatten and pause`, `Stop bot`, and `Mark poisoned` were disabled; `Fresh run` was enabled. `Resume trading` still explained `Broker safety is unknown. The backend cannot prove the broker is paper-only. Resume is held until broker safety is proven.`
- UX recommendation: The new table/detail `Fresh run only` flag is a real improvement, but the deploy flow still sends the operator into a submit-time dead end. When a Fresh run inherits a durably STOPPED instance and `Start trading immediately` is checked, the deploy form should disable `Deploy & start` or convert the call-to-action to `Deploy only` with copy explaining that Resume must clear the latch first. It should also keep treating an empty deployment-validation action plan as incomplete; before manual leg entry, this form still showed `Ready to deploy` while `ON ENTER` and `ON EXIT` were empty.

## 2026-07-07T17:12:10Z - deiagAPPL6

- Bot: `deiagAPPL6`
- Candidate reason: Different from `DIagVal6`, `dep_val_smoke_002`, and `Bars-July-6`; fleet list showed `BLOCKED`, flat exposure, `Fresh run only`, and last run `Exited with error`.
- Action taken: Opened `/broker/bots`, selected `deiagAPPL6`, used the detail-page `Fresh run` action, verified IBKR paper account `DUM284968`, `paper_orders`, `safe_canary`, `Start trading immediately`, then added required action plan `leg_1 long AAPL x 1` with `CLOSE leg_1`.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: `Deploy & start` was blocked before creating a new run. No order/fill observed.
- Exact UI error: `Deploy — blockeddeiagAPPL6 is durably STOPPED. Resume the bot to clear the stop latch before starting or using Deploy & start. Use Resume to set desired_state=RUNNING, then start the bot. Use Deploy only when you want to stage a new run without starting it.`
- Related UI state: The bot table and detail controls showed `Fresh run only`; on the detail page, all live controls were disabled and `Fresh run` was enabled. The deploy form again showed `Ready to deploy` while the action plan was initially empty, then accepted the manually added AAPL entry/exit plan before surfacing the durable STOPPED block at submit time.
- UX recommendation: This confirms the durable STOPPED dead end is not bot-specific or symbol-specific. The deploy form should read the inherited instance latch before submit and either disable `Deploy & start`, switch the primary CTA to `Deploy only`, or offer a safe guided `Resume then deploy/start` recovery when paper broker proof is available. Also fix the alert spacing/copy (`blockeddeiagAPPL6`) and make empty action plans fail the Legs step before the form says `Ready to deploy`.

## 2026-07-07T17:42:10Z - Valgate-Jul6

- Bot: `Valgate-Jul6`
- Candidate reason: Different from prior attempts; fleet list showed flat SPY exposure, `DEGRADED`, not `Fresh run only`, and prior status `Clean · 2026-07-06 11:21:28`.
- Action taken: Opened `/broker/bots`, selected `Valgate-Jul6`, used the detail-page `Fresh run` action, verified IBKR paper account `DUM284968`, `paper_orders`, `safe_canary`, and `Start trading immediately`, then added required action plan `leg_1 long SPY x 1` with `CLOSE leg_1`.
- Run id: `31e2dbffc771907ad1658349721752ac742d7c21f363eb078ceaa3cde648d028`
- Outcome category: `died-after-start`
- Outcome: Deploy created a run and start was accepted, but the bot quickly returned to `Bot Off` with no live runtime. No order/fill observed; broker tail only showed account summary, positions, open orders, diagnostics, and live bars refreshes.
- Exact UI error: `Deployment created. Your strategy instance is ready. Start accepted: Host runner process is active.` Then on the bot detail page: `Current lifecycle focus Recovery lane · Poisoned. Meaning: Previous run halted for safety and requires recovery review. The previous run left a safety halt trigger. This comes from the last-exit poison flag; the prior-run field alone is only a coarse classification. Prior Halt Trigger is Cold Start Divergence. Source: Last Exit Poisoned Flag. Evidence time: 2026-07-07 13:46:51 ET.`
- Related UI state: The fleet row changed to `BLOCKED Fresh run only` with `Safety halt · 2026-07-07 12:46:51`. Detail controls showed `Only Fresh run is available`; Start/Resume/Pause/Flatten/Stop/Mark poisoned were disabled and Fresh run remained enabled. `SUBMIT` still read `Broker state unproven`; exposure stayed flat. The incidents panel said `No recent incidents` even though the lifecycle and node receipt showed a poisoned safety halt. The deploy form also continued to show `Ready to deploy` while the action plan was initially empty.
- UX recommendation: This is the first post-start death in the rotation. The UI should treat `Cold Start Divergence` as a first-class incident, not only a lifecycle receipt: put it in Recent Incidents, show the exact poisoned-flag forensic details and recovery path, and link to the relevant audit/log artifact if available. The fleet/table `Fresh run only` chip is helpful after the death, but the detail page should also explain why a bot that just accepted start became poison-blocked. The deploy form still needs pre-submit validation for empty action plans.

## 2026-07-07T18:12:10Z - Validation-Jul6

- Bot: `Validation-Jul6`
- Candidate reason: Different from prior attempts; fleet list showed flat SPY exposure, `DEGRADED`, not `Fresh run only`, and prior status `Clean · 2026-07-06 10:02:31`.
- Action taken: Opened `/broker/bots`, selected `Validation-Jul6`, used the detail-page `Fresh run` action, verified the form showed IBKR paper account `DUM284968`, `paper_orders`, `safe_canary`, and `Start trading immediately`, then added required action plan `leg_1 long SPY x 1` with `CLOSE leg_1`. Did not submit because the UI did not prove account safety.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: `Deploy & start` stayed disabled before submit. No order/fill observed.
- Exact UI error: `AccountNot proven Account truth is degraded and needs review before calling the account clean. Deploy command Deploy & start Deployment Validation Deploy & start Account NOT_PROVEN. Reconcile account before starting, or turn off "Start trading immediately" to deploy only.`
- Related UI state: The form still showed broker linked as `Data-plane paper session connected`, launch mode `PAPER ORDERS ENABLED`, `safe_canary`, and Start trading immediately checked. After the SPY action plan was added, the primary button remained disabled because account proof was `NOT_PROVEN`. The Legs step still appeared `Complete` while the action plan was initially empty.
- UX recommendation: This safety block is much better than the durable STOPPED cases because it disables the submit button before any run is created. Make it more actionable by deep-linking `Reconcile account` to the exact account-monitor/reconciliation view, naming the missing account-truth evidence, and explaining why a visible paper broker session is not enough to call the bot account-clean. The deploy form still needs to keep Legs incomplete until an entry and matching close leg exist.

## 2026-07-07T18:42:10Z - DEPVALJUL1

- Bot: `DEPVALJUL1`
- Candidate reason: Different from prior attempts; fleet list showed flat MU exposure, `DEGRADED`, not `Fresh run only`, and prior status `Clean · 2026-07-04 22:59:05`.
- Action taken: Opened `/broker/bots`, selected `DEPVALJUL1`, and used the detail-page `Fresh run` action. Did not add an action plan or submit because the deploy form contradicted the bot identity/signal symbol.
- Run id: None created during this attempt.
- Outcome category: `unclear`
- Outcome: Stopped before deploy/start. No order/fill observed.
- Exact UI error: None. The UI showed `Ready to deploy` with `Deploy & start` enabled, but the bot row/detail identified `DEPVALJUL1` as MU while the deploy form prefilled `signal_stream=SPY` and the signal stream field value `SPY`.
- Related UI state: Detail page showed `DEPVALJUL1`, `MU`, flat exposure, `Paper Only`, `SUBMIT Broker state unproven`, no live runtime, and latest signal `ENTER`. Fresh run form showed broker linked to the paper account, account clean, `paper_orders`, `safe_canary`, Start trading immediately checked, and the primary `Deploy & start` button enabled even though the action plan was empty.
- UX recommendation: Treat strategy instance symbol, signal stream, and action plan symbol as a deploy-time safety invariant. If an MU bot opens a Fresh run form with SPY signal stream, disable `Deploy & start` and explain whether the new run will intentionally become SPY or whether lineage failed. The form should carry symbol provenance from the prior ledger, show the exact inherited source, and keep Legs incomplete until a matching entry/exit plan exists.

## 2026-07-07T19:12:40Z - PrajiDemo

- Bot: `PrajiDemo`
- Candidate reason: Different from prior attempts; fleet list showed AAPL, flat exposure, `DEGRADED`, and prior status `Exited with error · 2026-07-03 08:57:35`.
- Action taken: Opened `/broker/bots`, selected `PrajiDemo`, and used the detail-page `Fresh run` action. Did not add an action plan or submit because the detail page and deploy form contradicted each other on exposure/symbol safety.
- Run id: None created during this attempt.
- Outcome category: `unclear`
- Outcome: Stopped before deploy/start. No order/fill observed.
- Exact UI error: None. The detail page showed `ExposureUnknown` for AAPL, while the Fresh run form showed `Ready to deploy` with `Deploy & start` enabled, `signal_stream=SPY`, and no entry/exit legs declared.
- Related UI state: The fleet table said `PrajiDemo` was flat, but the detail page said `ExposureUnknown`, `Paper Only`, `SUBMIT Broker state unproven`, no live runtime, and AAPL context. The Fresh run form showed broker/account clean, `paper_orders`, `safe_canary`, Start trading immediately checked, `signal_stream=SPY`, and `LegsComplete` despite empty action plan.
- UX recommendation: Block Fresh run start when table/detail exposure disagree or exposure is unknown. The deploy form should inherit the bot's visible symbol context, or explicitly label the run as a new SPY deployment and require operator confirmation before Start trading immediately can remain enabled. Empty action plans should make the Legs step incomplete and keep `Deploy & start` disabled.

## 2026-07-07T19:42:40Z - DVS-SPY-SPY-0701

- Bot: `DVS-SPY-SPY-0701`
- Candidate reason: Different from prior attempts; fleet list showed SPY, flat exposure, `DEGRADED`, and prior status `Exited with error · 2026-07-01 08:54:05`.
- Action taken: Opened `/broker/bots`, selected `DVS-SPY-SPY-0701`, used the detail-page `Fresh run` action, verified the form showed paper account `DUM284968`, `paper_orders`, `safe_canary`, Start trading immediately, account clean, and SPY signal context, then added required action plan `leg_1 long SPY x 1` with `CLOSE leg_1` and submitted `Deploy & start`.
- Run id: `c2fed91109d741fd4983944e3fd323e6050a95053a2a73a52ff53e09eb53b9f8`
- Outcome category: `died-after-start`
- Outcome: Deploy created a run and start was accepted, but the bot quickly returned to `Bot Off` with no live runtime. No order/fill observed; broker tail only showed account summary, positions, open orders, diagnostics, live bars, and symbol search refreshes.
- Exact UI error: `Deployment created. Your strategy instance is ready. Start accepted: Host runner process is active.` Then on the bot detail page: `Current lifecycle focus Recovery lane · Poisoned. Meaning: Previous run halted for safety and requires recovery review. The previous run left a safety halt trigger. This comes from the last-exit poison flag; the prior-run field alone is only a coarse classification. Prior Halt Trigger is Cold Start Divergence. Source: Last Exit Poisoned Flag. Evidence time: 2026-07-07 15:44:36 ET.`
- Related UI state: Detail controls changed to Fresh-run-only recovery posture: Start/Resume/Pause/Flatten/Stop/Mark poisoned disabled, Fresh run enabled. `SUBMIT` still read `Broker state unproven`, exposure stayed flat, and Recent Incidents again said `No recent incidents` despite the poisoned lifecycle receipt. Before submit, the deploy form also said `Ready to deploy` while the action plan was initially empty.
- UX recommendation: This reproduces the Valgate post-start death on another SPY deployment. The UI needs a safety-halt incident bridge so `Cold Start Divergence` appears in Recent Incidents with halt trigger, source flag, evidence time, run id, and artifact/log path. Also block or warn pre-submit when the prior lifecycle already says cold-start receipt is blocked, and keep empty Legs from appearing complete.

## 2026-07-07T20:12:40Z - June23

- Bot: `June23`
- Candidate reason: Different from prior attempts; fleet list showed SPY, flat exposure, `DEGRADED`, and prior status `Exited with error · 2026-06-25 09:01:15`.
- Action taken: Opened `/broker/bots`, selected `June23`, used the detail-page `Fresh run` action, verified the form showed paper account `DUM284968`, `paper_orders`, `safe_canary`, Start trading immediately, account clean, fleet clear, and SPY signal context, then opened the Legs step, added/select-confirmed `leg_1 long SPY x 1`, verified the auto-added `CLOSE leg_1`, and submitted `Deploy & start`.
- Run id: `d2356dafc816254ab6e854286eee00c1d2ecc515f519ec1882173e8dfa1cab02`
- Outcome category: `died-after-start`
- Outcome: Deploy created a run and start was accepted, but the bot quickly returned to `Bot Off` with no live runtime and Recovery lane poisoned. No order/fill observed; broker tail only showed account summary, positions, open orders, diagnostics, and position refreshes.
- Exact UI error: `Deployment created. Your strategy instance is ready. Start accepted: Host runner process is active. Run id d2356dafc816254ab6e854286eee00c1d2ecc515f519ec1882173e8dfa1cab02`. The same deploy form then also showed `Deploy & start "June23" is already running. Stop it first, or turn off "Start trading immediately" to deploy without starting.` On the bot detail page: `Current lifecycle focus Recovery lane · Poisoned. Meaning: Previous run halted for safety and requires recovery review. Operator action is required for this lifecycle step. The previous run left a safety halt trigger. This comes from the last-exit poison flag; the prior-run field alone is only a coarse classification. Prior Halt Trigger is Cold Start Divergence. Source: Last Exit Poisoned Flag. Evidence time: 2026-07-07 16:16:27 ET.`
- Related UI state: Detail page showed `Paper Only`, `SUBMIT Broker state unproven`, `ExposureFlat`, `SPY · No live bot runtime is bound`, `BotOff`, and `Pre-flight gates Poison sentinel`. Recent Incidents settled to `No recent incidents No warnings or errors for this run` despite the poisoned lifecycle receipt. Control buttons had disabled-looking titles such as `Start bot process Off. Redeploy required` and `Stop bot Off. Redeploy required`, but the actual button elements were still enabled in the DOM. The deploy form also showed `LegsComplete` and `Ready to deploy` while the action plan was initially empty; the Add stock controls were present but hidden until the Legs step was selected.
- UX recommendation: This is the third observed SPY post-start death with the same `Cold Start Divergence` poisoned flag. Recent Incidents needs to ingest lifecycle safety-halt receipts and show halt trigger, source flag, evidence time, run id, and artifact/log path. The deploy form should not show a success message next to an `already running` warning after a successful start; it should move the operator to the live run or explain that start is in progress. Also make the control disabled state semantic, keep empty Legs incomplete, and make the Legs step visibly required/editable before submit.

## 2026-07-08T00:46:57Z - dep_val_smoke_002

- Bot: `dep_val_smoke_002`
- Candidate reason: Different from the prior `JUN26TSLA` check; fleet list showed SPY, flat exposure, zero open positions, `BLOCKED`, `Fresh run only`, and last run `No result yet · 2026-07-07 19:44:02`.
- Action taken: Opened `/broker/bots`, selected `dep_val_smoke_002`, verified the detail-page live controls, opened `Fresh run`, added the required action plan `leg_1 long SPY x 1` with `CLOSE leg_1`, and submitted the UI's safe `Deploy only` command. Did not attempt Resume/start because the bot page kept Resume disabled for unknown broker safety.
- Run id: A deployment was created, but the success panel showed the `Run id` label with no visible value before `View deployment`.
- Outcome category: `failed-before-start`
- Outcome: Deploy-only succeeded, but no bot process started and no trade occurred. The bot remained `Bot Off`, `SUBMIT Broker state unproven`, `ExposureFlat`, `SPY · No live bot runtime is bound`, lifecycle `Deploy or start · Blocked`; broker tail showed account summary, broker positions, open orders, diagnostics, and symbol search refreshes only.
- Exact UI error: Before the action plan was added, the form said `Deployment Validation requires an action plan; ON ENTER and ON EXIT are both empty.` After adding the leg, it said `Durable STOPPED latch is set. This submit will deploy only; use Resume on the bot page to clear the latch before starting.` On return to detail, `Resume trading` was disabled with `Resume trading Off. Broker safety is unknown. The backend cannot prove the broker is paper-only. Resume is held until broker safety is proven.`
- Related UI state: This is an improvement over earlier durable-STOPPED attempts. The detail page now semantically disabled Start/Resume/Pause/Flatten/Stop/Mark poisoned and left only Fresh run enabled. The deploy form also kept Legs as `Needs input` until a stock entry and matching close action existed. However, the deploy form still showed `Start trading immediately` checked while the command was downgraded to `Deploy only`, and the daemon was stale by 65 commits.
- UX recommendation: Keep this disabled-control behavior; it answers the "only Fresh run possible" case well. Polish the remaining operator path by showing the generated run id, explaining why `Start trading immediately` is ignored/overridden when durable STOPPED forces `Deploy only`, deep-linking the disabled Resume control to broker-safety proof/reconciliation, and escalating stale-daemon state before deploy if it may change control-plane behavior.

## 2026-07-08T01:16:41Z - DIagVal6

- Bot: `DIagVal6`
- Candidate reason: Different from the prior `dep_val_smoke_002` check; fleet list showed SPY, flat exposure, zero open positions, `BLOCKED`, `Fresh run only`, and last run `No result yet · 2026-07-07 20:13:41`.
- Action taken: Opened `/broker/bots`, selected `DIagVal6`, inspected the critical launch-failure state, opened the detail-page `Redeploy bot` flow, added the required action plan `leg_1 long SPY x 1` with `CLOSE leg_1`, and submitted the UI's safe `Deploy only` command. No start/resume was attempted.
- Run id: None created during this attempt.
- Outcome category: `failed-before-start`
- Outcome: Deploy-only was blocked before run creation by the working-tree safety gate. The bot remained `Bot Off`, `Paper Only`, `SUBMIT Broker state unproven`, `ExposureFlat`, `SPY · No live bot runtime is bound`, lifecycle `Recovery lane · Blocked`; broker tail showed only account summary, broker positions, open orders, and diagnostics refreshes. No order/fill observed.
- Exact UI error: Detail page showed `Bot launch failed` and `The bot failed before it could start trading: process exited with code 1`. The run-history excerpt said `[START] HALT — desired_state=STOPPED for DIagVal6 (PythonDataService/artifacts/live_state/DIagVal6/desired_state.json). § 16.4 Resolution 7: clear it with 'run.py resume' before the bot will start.` After adding the SPY action plan, the deploy form said `Durable STOPPED latch is set. This submit will deploy only; use Resume on the bot page to clear the latch before starting.` Submit then showed `Deploy — blocked Working tree is dirty; commit or stash before deploying. working tree has 9 uncommitted change(s) within scope ['PythonDataService', 'references/qc-shadow']: [' M PythonDataService/app/engine/live/config.py', ' M PythonDataService/app/engine/live/deploy.py', ' M PythonDataService/app/engine/live/run.py', ' M PythonDataService/app/engine/strategy/spec/fixtures/deployment_validation.spec.json', ' M PythonDataService/app/routers/engine.py'] A run with these inputs already exists, or the working tree is dirty. Commit or stash the listed paths, then deploy again.`
- Related UI state: Detail controls are improved and were semantically disabled: Start/Resume/Pause/Flatten/Stop/Mark poisoned disabled, Fresh run enabled. The critical notice also exposed a `Redeploy bot` button while the page headline said `Only Fresh run is available`, which is functionally useful but copy-inconsistent. The form correctly kept Legs incomplete until the stock entry and close action existed. `Start trading immediately` still appeared checked while the command was `Deploy only`, and the page reported the daemon was stale by 65 commits.
- UX recommendation: Keep the pre-submit action-plan and STOPPED-latch gates. Add the critical `Bot launch failed` event to Recent Incidents instead of showing `No recent incidents`, align the `Fresh run`/`Redeploy bot` naming, show all 9 dirty scoped changes or say the list is truncated, and make `Start trading immediately` visually disabled/ignored whenever the command has been downgraded to `Deploy only`.

## 2026-07-08T01:44:53Z - JUNE-22

- Bot: `JUNE-22`
- Candidate reason: Different from the prior `DIagVal6` check; fleet list showed SPY, flat exposure, zero open positions, `BLOCKED`, `Fresh run only`, and prior status `Safety halt · 2026-07-07 16:45:41`.
- Action taken: Opened `/broker/bots`, selected `JUNE-22`, inspected the poisoned recovery state, opened the detail-page `Fresh run` form, and stopped before adding legs or submitting because the form had no selectable validated strategy.
- Run id: None created during this attempt.
- Outcome category: `failed-before-start`
- Outcome: Blocked in the deploy form before run creation. The bot remained `Bot Off`, `Paper Only`, `SUBMIT Broker state unproven`, `ExposureFlat`, `SPY · No live bot runtime is bound`, lifecycle `Recovery lane · Poisoned`; broker tail showed account summary, broker positions, open orders, diagnostics, contract details, and live bars refreshes only. No order/fill observed.
- Exact UI error: Detail page showed lifecycle `Bot lifecycle overview · Recovery lane · Poisoned` and current focus `recovery · Poisoned` with `Meaning: Previous run halted for safety and requires recovery review.` Recent Incidents still said `No recent incidents`. The Fresh run form showed `Deploy command Deploy & start`, `Strategy Needs input`, `Legs Needs input`, disabled `Deploy & start`, and the blocking copy `Select a validated strategy before deploying. Strategy must be validated before deployment. Open Strategy Validation to promote it.` The strategy selector contained only disabled `Select a strategy…` while the URL and text fields carried `strategy_key=deployment_validation`, the deployment validation spec path, QC backtest id, and audit-copy path.
- Related UI state: Detail controls were correctly semantically disabled: Start/Resume/Pause/Flatten/Stop/Mark poisoned disabled and Fresh run enabled. The form still showed `paper_orders`, safe canary, and `Start trading immediately` checked, even though no validated strategy could be selected and the button was disabled. The daemon was stale by 65 commits.
- UX recommendation: Treat missing validated-strategy hydration as a first-class redeploy blocker: either preselect the inherited `deployment_validation` strategy from the query params or show a clear "validation receipt unavailable/stale" recovery path with a link to the exact Strategy Validation record. Also carry the poisoned safety-halt receipt into Recent Incidents, and disable or visually mute `Start trading immediately` when the strategy gate prevents deploy/start.

## 2026-07-08T02:18:02Z - Bars-July-6

- Bot: `Bars-July-6`
- Candidate reason: Different from the prior `JUNE-22` check; fleet list showed SPY, flat exposure, zero open positions, `BLOCKED`, `Fresh run only`, and prior status `Safety halt · 2026-07-07 17:19:47`.
- Action taken: Opened `/broker/bots`, selected `Bars-July-6`, inspected the poisoned recovery state, opened the detail-page `Fresh run` flow, verified the form hydrated `deployment_validation` for SPY with paper launch mode, added the required action plan `leg_1 long SPY x 1` with `CLOSE leg_1`, and submitted `Deploy & start`.
- Run id: `f9af53a747539c53b0b1e8576d45484f207032ec86016401cbfddffc7a364ddb`
- Outcome category: `died-after-start`
- Outcome: Deploy created a run and start was accepted, but the bot quickly returned to `Bot Off` with no live runtime and lifecycle `Recovery lane · Blocked`. No order/fill rows were visible after the start; broker tail showed account summary, broker positions, open orders, and broker diagnostics only.
- Exact UI error: The deploy form showed `Deployment created. Your strategy instance is ready. Start accepted: Host runner process is active.` and `Start accepted for run f9af53a747539c53b0b1e8576d45484f207032ec86016401cbfddffc7a364ddb. View deployment to monitor the live run.` On the detail page, lifecycle showed `Recovery lane`, `recovery · Blocked`, and `Meaning: This run wrote poisoned.flag for cold_start_divergence. Reason: unknown_namespace. Review the halt evidence before starting a fresh run.` Recent Incidents showed `APP BLOCKING 2026-07-07 21:15:02 2026-07-08 02:15:02.888 Cold-start divergence — bot halted`, `On startup the bot couldn't reconcile its own records against the broker. It refused to resume on stale state.`, and `Recommended: Reconcile the broker account and re-deploy a fresh run_id. The same run cannot resume.`
- Related UI state: Detail controls were semantically disabled: Start/Resume/Pause/Flatten/Stop/Mark poisoned disabled and Fresh run enabled. The page showed `Paper Only`, `SUBMIT Broker state unproven`, `ExposureFlat`, `SPY · No live bot runtime is bound`, `BotOff`, and `Only Fresh run is available`. The deploy form correctly kept Legs incomplete until the SPY entry/close plan existed, but the top success panel still showed a blank `Run id` label while the lower success copy contained the run id. The form warned that engine code was stale by 67 commits before submit.
- UX recommendation: This is another SPY post-start cold-start halt, but the incident bridge is improved because Recent Incidents now explains the stale-state refusal and recommended recovery. Next polish: expose the raw halt evidence path from `View raw log` or make it copy a forensic breadcrumb, populate the success panel's `Run id` field, and escalate stale-daemon/stale-engine-code state before `Deploy & start` when it may immediately produce a cold-start safety halt.

## 2026-07-08T18:01:09Z - deiagAPPL6

- Bot: `deiagAPPL6`
- Candidate reason: Different from the prior selected bot `dep_val_smoke_002`; fleet list showed AAPL, flat exposure, zero open positions, `BLOCKED`, `Fresh run only`, and `No result yet · 2026-07-08 12:58:32`.
- Action taken: Opened `/broker/bots`, selected `deiagAPPL6`, inspected the fresh-run-only detail state, used `Fresh run`, opened the Legs step, selected `AAPL APPLE INC NASDAQ`, verified `leg_1 long AAPL x 1` and auto-created `CLOSE leg_1`, then submitted the enabled UI `Deploy` button. The form explicitly showed `Deploy only`, not `Deploy & start`, because a durable stopped latch was set.
- Run id: A deployment was created, but the success panel showed the `Run id` label with no visible value before `View deployment`.
- Outcome category: `failed-before-start`
- Outcome: Deploy-only succeeded and returned to the bot detail page. No bot process started, the bot remained `BOT Off`, and no AAPL order/fill appeared in `/broker/orders`.
- Exact UI error: None blocking after the action plan was added. Before the action plan, the form showed `Deployment Validation requires an action plan; ON ENTER and ON EXIT are both empty.` After the AAPL leg was added, it showed `Durable STOPPED latch is set. This submit will deploy only; use Resume on the bot page to clear the latch before starting.` The success panel showed `Deployment created`, `Your strategy instance is ready.`, `Run id`, and `View deployment`, with the run id value blank.
- Related UI state: Detail page showed `BROKER PROOF Paper Only`, `SUBMIT Broker state unproven`, `EXPOSURE Flat`, `AAPL · No live bot runtime is bound`, `BOT Off`, lifecycle `Deploy or start · Blocked`, and copy saying `Only Fresh run is available` plus `Live controls are locked for this run.` The deploy URL and hidden signal-stream field carried `AAPL`, but the visible strategy copy emphasized validation case `SPY`; the collapsed Signal stream step said only `Complete`, so the operator had to inspect the field/URL to know the leg should be AAPL. After `View deployment`, the detail page still showed no runtime bound while the chart showed `LIVE` and `Streaming` for AAPL, which can read like a live bot even though only broker market data is streaming. The orders page showed `Order ledger (55)`, all visible rows stamped `learn-ai/DEPVALSPYJUL8/v1:*` for SPY, with no AAPL order/fill visible.
- UX recommendation: Keep the deploy-only downgrade for durable STOPPED latches, but classify the post-deploy state more clearly as "deployment created, not running" and populate/copy the run id. The form should show the active signal stream (`AAPL`) prominently in the Legs editor because the validation-case `SPY` copy is visually dominant and can lead operators to choose the wrong symbol. On the detail page, distinguish market-data streaming from bot-runtime streaming so `LIVE` does not imply the bot is running. Orders should support filtering by bot/symbol so a no-trade deploy-only outcome is easier to verify.

## 2026-07-08T18:30:51Z - DIagVal6

- Bot: `DIagVal6`
- Candidate reason: Different from the prior selected bot `deiagAPPL6`; fleet list showed SPY, flat exposure, zero open positions, `BLOCKED`, `Fresh run only`, and `No result yet · 2026-07-08 13:28:21`.
- Action taken: Opened `/broker/bots`, selected `DIagVal6`, inspected the prior crash/fresh-run-only detail state, used `Fresh run`, opened the Legs step, selected `SPY SS SPDR S&P 500 ETF TRUST-US ARCA`, verified `leg_1 long SPY x 1` and auto-created `CLOSE leg_1`, then submitted the enabled UI `Deploy` button. The form explicitly showed `Deploy only`, not `Deploy & start`, because a durable stopped latch was set.
- Run id: A deployment was created, but the success panel showed the `Run id` label with no visible value before `View deployment`.
- Outcome category: `failed-before-start`
- Outcome: Deploy-only succeeded and returned to the bot detail page. No bot process started, the bot remained `BOT Off`, and no `DIagVal6` order/fill appeared in `/broker/orders`.
- Exact UI error: None blocking after the action plan was added. The detail page initially showed `Previous host runner crashed` and run-history `Launch Failed` with excerpt `HALT — desired_state=STOPPED for DIagVal6 (PythonDataService/artifacts/live_state/DIagVal6/desired_state.json). § 16.4 Resolution 7: clear it with 'run.py resume' before the bot will start.` Before the action plan, the form showed `Deployment Validation requires an action plan; ON ENTER and ON EXIT are both empty.` After adding the SPY leg, it showed `Durable STOPPED latch is set. This submit will deploy only; use Resume on the bot page to clear the latch before starting.` The success panel showed `Deployment created`, `Your strategy instance is ready.`, `Run id`, and `View deployment`, with the run id value blank.
- Related UI state: Detail page showed `BROKER PROOF Paper Only`, `SUBMIT Broker state unproven`, `EXPOSURE Flat`, `SPY · No live bot runtime is bound`, `BOT Off`, lifecycle `Deploy or start · Blocked`, and copy saying `Only Fresh run is available` plus `Live controls are locked for this run.` The prior crash explanation was visible in Run History, but Recent Incidents still showed `No recent incidents`. After `View deployment`, the bot detail still showed no runtime bound while the chart showed `LIVE` and `Streaming` for SPY, which can read like a live bot even though only broker market data is streaming. `/broker/orders` showed `Order ledger (63)`, all visible rows stamped `learn-ai/DEPVALSPYJUL8/v1:*` for SPY, and no `DIagVal6` order ref.
- UX recommendation: Keep the deploy-only downgrade for durable STOPPED latches and the locked live controls, but promote the launch-failure/desired-state explanation into a current incident or recovery banner with the run id/log path and a UI-native recovery action. Populate/copy the deploy success run id. Distinguish broker market-data streaming from bot-runtime streaming on the detail chart, and add bot-instance filtering to the Orders page so deploy-only/no-trade verification does not require scanning unrelated `DEPVALSPYJUL8` fills.

## 2026-07-08T19:01:22Z - DEPVALSPYJUL8

- Bot: `DEPVALSPYJUL8`
- Candidate reason: Different from the prior selected bot `DIagVal6`; the fleet list showed this previously trading SPY bot had changed to `DEGRADED`, `Exited with error · 2026-07-08 13:47:01`, open position `SPY 1`, and `1` error.
- Action taken: Opened `/broker/bots`, selected `DEPVALSPYJUL8`, and inspected the frozen detail page. Did not use `Fresh run`, start, resume, or flatten controls because the UI showed an active account freeze, long SPY exposure, no live runtime bound, and an explicit instruction to clear the account only after broker exposure is proven.
- Run id: Not visible on the detail page during this inspection.
- Outcome category: `died-after-start`
- Outcome: The bot had traded earlier but is now `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Long`, `SPY · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`. No new trade was attempted. Prior runtime evidence remained visible: `67 trades`, run history `135 row(s)`, and broker tail `784 event(s)`.
- Exact UI error: `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Control-plane lease lost`, `Recovery flatten failed — positions may be open`, `Engine stopped unexpectedly`, and repeated `Unknown error — see raw traceback`.
- Related UI state: Live process controls were semantically disabled: `Start bot process`, `Resume trading`, `Pause trading`, `Flatten and pause`, and `Mark poisoned` all had `disabled: true` and `aria-disabled: true`. `Fresh run` and `Stop bot` were still visible and enabled while the account was frozen; `Open runbook` was enabled. The lifecycle step selector also exposed `Deploy or start` as `Frozen`. Broker tail showed 67 broker fills and later position/open-order/diagnostic refreshes, while the chart still showed `LIVE` and `Streaming`, which can read like runtime health even though no bot process is bound.
- UX recommendation: Make frozen-account plus open exposure the dominant operator state everywhere: fleet row, detail header, lifecycle focus, and control bar should say `Account frozen: long SPY 1, flatten timed out`. Disable or require an explicit recovery-runbook gate for `Fresh run` while account freeze is active and exposure remains open. Add a one-click evidence bundle with the failed flatten attempt, raw traceback/log path, run id, last order/fill status, and broker position proof. Also distinguish market-data streaming from bot-runtime streaming so the `LIVE` chart cannot be mistaken for a running bot.

## 2026-07-08T19:28:38Z - DVS-SPY-SPY-0701

- Bot: `DVS-SPY-SPY-0701`
- Candidate reason: Different from the prior selected bot `DEPVALSPYJUL8`; fleet list showed SPY, flat exposure, zero open positions, `BLOCKED`, `Fresh run only`, and prior status `Safety halt · 2026-07-07 14:44:36`.
- Action taken: Opened `/broker/bots`, selected `DVS-SPY-SPY-0701`, inspected the frozen detail page, then opened the enabled `Fresh run` form only to inspect its safety gates. Did not add legs, deploy, start, resume, or submit anything because the detail page showed an active account freeze and `watchdog.flatten_timed_out`.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Flat`, `SPY · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, and `Runtime reconciliation is waiting for a live bot process`. The Fresh run form then showed `Deploy & start`, `Account Clean`, `Fleet Clear`, `paper_orders`, `safe_canary`, and `Start trading immediately` checked, with the only visible blocker `Deployment Validation requires an action plan; ON ENTER and ON EXIT are both empty.`
- Related UI state: Detail controls were semantically disabled for `Start bot process`, `Resume trading`, `Pause trading`, `Flatten and pause`, `Stop bot`, and `Mark poisoned`, while `Fresh run` and `Open runbook` remained enabled. Recent Incidents said `No recent incidents` even though the lifecycle showed account freeze. The deploy form inherited `inherited_exposure_posture=FLAT` and did not surface the global account-freeze state that was visible on the bot detail page.
- UX recommendation: The Fresh run form must carry forward account-freeze and fleet-freeze state from the detail page. When the source bot says `SUBMIT Account frozen`, the deploy header should not say `Account Clean` or `Fleet Clear`, and `Start trading immediately` plus `paper_orders` should be disabled or visually overridden by a frozen-account recovery gate. Also show the freeze as a Recent Incident so operators do not have to reconcile contradictory lifecycle and form states.

## 2026-07-08T19:58:16Z - tsladepvaljul7

- Bot: `tsladepvaljul7`
- Candidate reason: Different from the prior selected bot `DVS-SPY-SPY-0701`; fleet list showed TSLA, open position `TSLA 1`, `BLOCKED`, `Fresh run only`, and prior status `Exited with error · 2026-07-07 11:39:05`.
- Action taken: Opened `/broker/bots`, selected `tsladepvaljul7`, inspected the frozen detail page, then opened `Fresh run` only to inspect whether TSLA exposure and account freeze propagated into the deploy form. Did not add legs, deploy, start, resume, or submit anything because the detail page showed account freeze, `watchdog.flatten_timed_out`, and long TSLA exposure.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Long`, `TSLA · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `Desired state STOPPED — start refused until intent changes`, `No live bot runtime is bound`, and recent incidents including repeated `Unknown error — see raw traceback` plus `APP CRITICAL ... Engine stopped unexpectedly`. The Fresh run form showed `Deploy only`, `Account Clean`, `Fleet Clear`, `Deployment Validation requires an action plan; ON ENTER and ON EXIT are both empty.`, and inherited exposure `Posture Long`, `Positions TSLA 1`, `Pending orders 0`.
- Related UI state: Detail controls were semantically disabled for Start/Resume/Pause/Flatten/Stop/Mark poisoned while `Fresh run` and `Open runbook` remained enabled. The deploy form correctly carried the signal stream as `TSLA`, inherited `LONG` exposure, and downgraded the command to `Deploy only`; however, its readiness facts still said `Account Clean` and `Fleet Clear` despite the source detail page showing `SUBMIT Account frozen`. The form also reported daemon code `Stale — 1 commit behind`.
- UX recommendation: Preserve the good parts of this path: inherited symbol/exposure are visible and the command is downgraded to `Deploy only`. Fix the dangerous contradiction by making account freeze override the deploy-form readiness facts and by showing a recovery banner such as `Account frozen: TSLA 1 still open; deploy-only allowed only after runbook acknowledgement` instead of `Account Clean/Fleet Clear`. Link the stale-daemon warning to whether frozen-account fixes are missing from the running control plane.

## 2026-07-08T20:28:13Z - JUN26GM

- Bot: `JUN26GM`
- Candidate reason: Different from the prior selected bot `tsladepvaljul7`; fleet list showed GM, degraded, prior status `Exited with error · 2026-06-26 10:12:47`, and no recent observation today.
- Action taken: Opened `/broker/bots`, selected `JUN26GM`, inspected the frozen detail page, then opened `Fresh run` only to inspect the deploy-form guards. Did not add legs, deploy, start, pause, stop, resume, or submit anything because the detail page showed account freeze and unresolved broker/runtime proof.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Unknown`, `GM · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, and `Trading session not accepting strategy activity`. Recent Incidents said `No recent incidents`. The Fresh run form showed `Deploy & start`, `Account Clean`, `Fleet Clear`, `Inherited bot symbol GM conflicts with Signal stream SPY. Confirm the new run identity before Deploy & start.`, `Inherited exposure Posture Unknown`, `Positions Flat`, `Pending orders unknown`, and `Confirm exposure state`.
- Related UI state: Detail controls had Start/Resume/Flatten/Mark poisoned disabled, but `Pause trading`, `Stop bot`, `Fresh run`, and `Open runbook` were enabled while the bot was off and the account was frozen. The deploy form correctly surfaced the GM-vs-SPY run-identity conflict and unknown exposure confirmation gate, but still advertised `Account Clean` and `Fleet Clear` and kept paper-orders launch controls available. The form also reported daemon code `Stale — 1 commit behind`.
- UX recommendation: Keep the identity/exposure confirmation gates; they are the right operator friction for a GM bot whose deploy form defaults signal stream to SPY. Fix the safety hierarchy so account freeze overrides every deploy-form readiness fact and action label. `Pause trading` and `Stop bot` should not be enabled on an off/no-runtime/frozen bot unless they perform a clearly explained recovery action; otherwise show disabled reason text. Recent Incidents should include the account-freeze receipt instead of saying no incidents.

## 2026-07-08T20:58:06Z - DEPVALJUL1

- Bot: `DEPVALJUL1`
- Candidate reason: Different from the prior selected bot `JUN26GM`; fleet list showed MU, flat exposure, zero open positions, `DEGRADED`, and prior status `Clean · 2026-07-04 22:59:05`.
- Action taken: Opened `/broker/bots`, selected `DEPVALJUL1`, inspected the frozen detail page, then opened `Fresh run` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, resume, or submit anything because the detail page showed account freeze and runtime/broker proof unavailable.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Flat`, `MU · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, and `Trading session not accepting strategy activity`. Recent Incidents contained repeated `Unknown error — see raw traceback`, `Control-plane lease lost`, `Recovery flatten failed — positions may be open`, and `Market data farm degraded`. The Fresh run form showed `Deploy & start`, `Account Clean`, `Fleet Clear`, `Inherited bot symbol MU conflicts with Signal stream SPY. Confirm the new run identity before Deploy & start.`, and `Legs Needs input`.
- Related UI state: Detail controls correctly disabled Start/Resume/Pause/Flatten/Mark poisoned, but `Stop bot`, `Fresh run`, and `Open runbook` were enabled while the bot was off and frozen. The deploy form surfaced the MU-vs-SPY run-identity conflict but did not surface the source detail page's account-freeze state; paper-orders launch controls remained available and `Start trading immediately` remained checked. The form also reported daemon code `Stale — 1 commit behind`.
- UX recommendation: The fleet row saying `Clean` conflicts with the detail page saying `SUBMIT Account frozen`; account freeze should override stale row summaries. Keep the inherited-symbol conflict guard, but make frozen account state the first deploy-form banner and force `Deploy & start` into a disabled recovery mode until the freeze/runbook evidence is cleared. `Stop bot` should be disabled or renamed if there is no live runtime to stop.

## 2026-07-08T21:28:10Z - PrajiDemo

- Bot: `PrajiDemo`
- Candidate reason: Different from the prior selected bot `DEPVALJUL1`; fleet list showed AAPL, `DEGRADED`, and prior status `Exited with error · 2026-07-03 08:57:35`.
- Action taken: Opened `/broker/bots`, selected `PrajiDemo`, inspected the frozen detail page, then opened `Fresh run` only to inspect deploy-form guards. Did not add legs, deploy, start, pause, stop, resume, or submit anything because the detail page showed account freeze and unresolved broker/runtime proof.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Unknown`, `AAPL · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, `Desired state no durable intent recorded`, and `Trading session not accepting strategy activity`. Recent Incidents included `BROKER UNKNOWN ... Unknown error — see raw traceback` and `APP UNKNOWN ... Unknown error — see raw traceback`. The Fresh run form showed `Deploy & start`, `Account Clean`, `Fleet Clear`, `Inherited bot symbol AAPL conflicts with Signal stream SPY. Confirm the new run identity before Deploy & start.`, `Inherited exposure Posture Unknown`, `Positions Flat`, `Pending orders unknown`, and `Confirm exposure state`.
- Related UI state: Detail controls had Start/Resume/Flatten/Mark poisoned disabled, but `Pause trading`, `Stop bot`, `Fresh run`, and `Open runbook` were enabled while the bot was off and account-frozen. The deploy form correctly surfaced the AAPL-vs-SPY run-identity conflict and unknown exposure confirmation, but still omitted the account freeze and kept paper-orders/start-immediately launch controls active. The form also reported daemon code `Stale — 1 commit behind`.
- UX recommendation: Account freeze needs to be a shared cross-page safety primitive, not just a detail-page receipt. The Fresh run form should replace `Account Clean`/`Fleet Clear` with the freeze reason and disable `Deploy & start`/start-immediately controls until the runbook is cleared. Keep the inherited-symbol and exposure confirmations, but place them below the account-freeze blocker. Disable or explain `Pause trading` and `Stop bot` when there is no runtime to act on.

## 2026-07-08T21:58:06Z - Validation-Jul6

- Bot: `Validation-Jul6`
- Candidate reason: Different from the prior selected bot `PrajiDemo`; fleet list showed SPY, flat exposure, zero open positions, `DEGRADED`, and prior status `Clean · 2026-07-06 10:02:31`.
- Action taken: Opened `/broker/bots`, selected `Validation-Jul6`, inspected the frozen detail page, then opened `Fresh run` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, resume, or submit anything because the detail page showed account freeze and broker/runtime proof unavailable.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Flat`, `SPY · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, and `Trading session not accepting strategy activity`. Recent Incidents showed many `BROKER UNKNOWN ... Unknown error — see raw traceback` rows. The Fresh run form showed `Deploy & start`, `Account Clean`, `Fleet Clear`, and `Deployment Validation requires an action plan; ON ENTER and ON EXIT are both empty.`
- Related UI state: Detail controls correctly disabled Start/Resume/Pause/Flatten/Mark poisoned, but `Stop bot`, `Fresh run`, and `Open runbook` remained enabled while the bot was off and frozen. The deploy form inherited SPY signal/exposure cleanly, so there was no symbol-conflict or exposure-confirmation warning to slow the operator down; the only visible blocker was missing Legs. Paper-orders launch mode and Start trading immediately remained selected. The form also reported daemon code `Stale — 1 commit behind`.
- UX recommendation: This is the highest-risk variant of the frozen-form mismatch because SPY matches SPY, so the operator sees no identity/exposure warning. Account freeze must appear before the Legs gate and force `Deploy & start` into a recovery-only disabled state. The fleet `Clean` row, deploy-form `Account Clean`, and detail `SUBMIT Account frozen` should collapse into one canonical safety status. `Stop bot` should be disabled or clearly described when no runtime is bound.

## 2026-07-08T22:28:09Z - JUN26TSLA

- Bot: `JUN26TSLA`
- Candidate reason: Different from the prior selected bot `Validation-Jul6`; fleet list showed TSLA, open position `TSLA 1`, `DEGRADED`, and prior status `Exited with error · 2026-06-26 10:39:37`.
- Action taken: Opened `/broker/bots`, selected `JUN26TSLA`, inspected the frozen detail page, then opened `Fresh run` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, resume, or submit anything because the detail page showed account freeze, long TSLA exposure, and prior recovery failure.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Long`, `TSLA · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, and broker proof `Paper Only`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, and `Trading session not accepting strategy activity`. Recent Incidents included `Control-plane lease lost`, `Recovery flatten failed — positions may be open`, `Engine stopped unexpectedly`, and repeated `Unknown error — see raw traceback`. The Fresh run form showed `Deploy & start`, `Account Clean`, `Fleet Clear`, `Inherited bot symbol TSLA conflicts with Signal stream SPY. Confirm the new run identity before Deploy & start.`, `Inherited exposure Posture Long`, `Positions TSLA 1`, `Pending orders 0`, and `Confirm exposure state`.
- Related UI state: Detail controls correctly disabled Start/Resume/Pause/Flatten/Mark poisoned, but `Stop bot`, `Fresh run`, and `Open runbook` remained enabled while the bot was off and frozen. The deploy form did preserve the inherited TSLA exposure and identity conflict, but it did not preserve the account-freeze state and kept paper-orders/start-immediately launch controls available. The form also reported daemon code `Stale — 3 commits behind`.
- UX recommendation: Keep the TSLA exposure and identity-conflict guards, but make account freeze the top-level deploy blocker. A long-position bot with a failed recovery flatten should never present `Account Clean`, `Fleet Clear`, or `Deploy & start` without a recovery/runbook acknowledgement and broker position proof. Also disable or rename `Stop bot` when no runtime is bound, and connect the stale-daemon warning to whether the running UI/control plane may be missing freeze propagation fixes.

## 2026-07-09T02:58:21Z - tsladepvaljul7

- Bot: `tsladepvaljul7`
- Candidate reason: Different from the prior selected bot `DEPVALSPYJUL8`; fleet list showed TSLA, open exposure `TSLA 1`, one open position, `Sick bay`, and prior status `Exited with error · 2026-07-07 11:39:05`.
- Action taken: Opened `/broker/bots`, selected `tsladepvaljul7`, inspected the frozen/sick-bay detail page and event stream, then opened `Change via redeploy` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, or submit anything because the UI showed account/fleet freeze, no live runtime bound, desired state stopped, and open exposure.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Long`, `TSLA · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, broker proof `Paper Only`, with current exposure `TSLA 1`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `Desired state STOPPED — start refused until intent changes`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, `No live runtime is bound Host process is IDLE`, and `Trading session not accepting strategy activity`. Event stream showed 5 rows including `Order Filled`, `Signal Fired`, and `Launch Failed`; recent incidents showed 6 incidents including `Foreign fill dropped`, repeated `Unknown error — see raw traceback`, and `Engine stopped unexpectedly`. Broker tail showed `Broker positions refreshed`, `Open orders refreshed`, `Account summary refreshed`, `Position P&L refreshed`, and `Account P&L refreshed`. The deploy form showed `Account Frozen`, `Fleet Frozen`, command `Deploy only`, disabled `Deploy`, `Deployment Validation requires an action plan; ON ENTER and ON EXIT are both empty.`, `Inherited exposure Posture Long`, `Positions TSLA 1`, `Pending orders 0`, and `Deploy-only will stage these values without starting.`
- Related UI state: The improved path is now much safer than the older observation: the detail page hides live controls, the deploy form preserves `Signal stream TSLA` and `Positions TSLA 1`, the engine code is `Up to date`, and the action is downgraded from `Deploy & start` to `Deploy only`. Remaining issues: `Change via redeploy` still opens editable next-run settings under account sick bay, `Fleet policy Clear` still coexists with `Fleet Frozen`, launch and legs sections are still editable even though the operator is in recovery context, and the broker-account input says `Broker account unavailable` while a connected paper account is shown.
- UX recommendation: Keep the TSLA exposure preservation and deploy-only downgrade. For stopped/open-exposure sick-bay bots, rename the path to `Review recovery deploy only` and make legs/launch read-only until the operator explicitly acknowledges the runbook. Make `Fleet Frozen` override `Fleet policy Clear`, clarify connected-but-frozen broker copy, and replace raw `Unknown error — see raw traceback` rows with a grouped incident summary plus a forensic link.

## 2026-07-09T03:28:22Z - JUNE-25

- Bot: `JUNE-25`
- Candidate reason: Different from the prior selected bot `tsladepvaljul7`; fleet list showed mixed symbols `AAPL, SPY`, open exposure `SPY 1`, one open position, `Sick bay`, and prior status `Exited with error · 2026-06-25 12:15:33`.
- Action taken: Opened `/broker/bots`, selected `JUNE-25`, inspected the frozen/sick-bay detail page, then opened `Change via redeploy` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, or submit anything because the UI showed account/fleet freeze, no live runtime bound, a closed trading session, and open exposure.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Long`, `AAPL · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, broker proof `Paper Only`, with inherited exposure `SPY 1`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, `Desired state no durable intent recorded`, and `Trading session not accepting strategy activity`. Recent incidents included repeated `Engine stopped unexpectedly`, `Unknown error — see raw traceback`, and broker warnings `Market data farm degraded`. Broker tail showed fresh `Broker positions refreshed`, `Broker diagnostic`, `Open orders refreshed`, `Account summary refreshed`, `Position P&L refreshed`, and `Account P&L refreshed` evidence. The redeploy form showed `Account Frozen`, `Fleet Frozen`, disabled `Deploy & start`, `Account freeze active. Resolve the account sick-bay condition before starting.`, `Inherited bot symbol AAPL`, `Signal stream SPY`, `Inherited exposure Posture Long`, `Positions SPY 1`, `Pending orders 0`, `Legs Needs input`, and engine code `Up to date`.
- Related UI state: The form now preserves the actual mixed identity/exposure facts and blocks `Deploy & start` under account freeze, which is the correct safety posture. Remaining issues: the detail hero still emphasizes `AAPL · No live bot runtime is bound` while the risky thing to resolve is `SPY 1`; the deploy form still shows `Fleet policy Clear` beside `Fleet Frozen`; launch and action-plan controls remain editable even though the account is in recovery context; and the broker-account field is labeled `Broker account unavailable` while the page header says the data-plane paper session is connected.
- UX recommendation: Make mixed identity/exposure impossible to miss before the redeploy form: use header copy such as `AAPL strategy · SPY 1 open exposure · account frozen`, and mirror that wording in the table row action. Let `Fleet Frozen` override `Fleet policy Clear`, keep `Deploy & start` disabled, and make legs/launch read-only until the freeze/runbook acknowledgement is complete. Replace raw `Unknown error — see raw traceback` incident rows with a grouped incident summary and a forensic link.

## 2026-07-09T03:58:22Z - JUN26TSLA

- Bot: `JUN26TSLA`
- Candidate reason: Different from the prior selected bot `JUNE-25`; fleet list showed TSLA, open exposure `TSLA 1`, one open position, `Sick bay`, and prior status `Safety halt · 2026-06-26 10:39:37`.
- Action taken: Opened `/broker/bots`, selected `JUN26TSLA`, inspected the frozen/sick-bay detail page and broker tail, then opened `Change via redeploy` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, or submit anything because the UI showed account/fleet freeze, no live runtime bound, closed trading session, and open TSLA exposure.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `BOT Off`, `SUBMIT Account frozen`, `EXPOSURE Long`, `TSLA · No live bot runtime is bound`, lifecycle `Deploy or start · Frozen`, broker proof `Paper Only`, with inherited exposure `TSLA 1`.
- Exact UI error: Detail page showed `Account freeze active`, `watchdog.flatten_timed_out`, `Open the freeze/runbook evidence and clear the account only after broker exposure is proven.`, `No live bot runtime is bound`, `Runtime reconciliation is waiting for a live bot process`, `Pre-flight gates are clear`, and `Trading session not accepting strategy activity`. Recent incidents included `Foreign fill dropped`, repeated `Unknown error — see raw traceback`, `Control-plane lease lost`, `Recovery flatten failed — positions may be open`, and `Engine stopped unexpectedly`. Broker tail showed fresh `Broker positions refreshed`, `Broker diagnostic`, `Open orders refreshed`, `Account summary refreshed`, `Contract details refreshed`, `Live bars refreshed`, `Position P&L refreshed`, and `Account P&L refreshed` evidence. The redeploy form showed `Account Frozen`, `Fleet Frozen`, disabled `Deploy & start`, `Account freeze active. Resolve the account sick-bay condition before starting.`, `Inherited bot symbol TSLA`, `Signal stream SPY`, `Validation case SPY`, `Inherited exposure Posture Long`, `Positions TSLA 1`, `Pending orders 0`, `Legs Needs input`, and engine code `Up to date`.
- Related UI state: The safety block is now stronger than older observations: account freeze is present on the form and the primary `Deploy & start` button is disabled. Remaining issues: the form still shows `Fleet policy Clear` beside `Fleet Frozen`, launch controls still advertise `PAPER ORDERS ENABLED` and `Start trading immediately`, the broker account input is labeled `Broker account unavailable` while a paper session is connected, and the TSLA bot would redeploy with `Signal stream SPY` unless the operator catches the identity warning.
- UX recommendation: Keep the disabled start and inherited `TSLA 1` exposure proof. Promote the identity conflict into the main command label, for example `Blocked: TSLA bot would start on SPY signal stream`, and make launch controls read-only while account freeze is active. Collapse `Fleet policy Clear` plus `Fleet Frozen` into one canonical frozen status, clarify connected-but-frozen broker copy, and replace repeated raw traceback rows with one grouped incident summary plus a forensic link.

## 2026-07-09T04:28:52Z - DEPVALSPYJUL8

- Bot: `DEPVALSPYJUL8`
- Candidate reason: Different from the prior selected bot `JUN26TSLA`; fleet list showed SPY, open exposure `SPY 1`, one open position, `Sick bay`, and prior status `Exited with error · 2026-07-08 13:47:01`.
- Action taken: Opened `/broker/bots`, selected `DEPVALSPYJUL8`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, or submit anything because the UI showed account/fleet freeze, closed session, sick-bay state, and open SPY exposure.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, `PAPER`, `POSITION Long 1 SPY`, `SESSION Closed`, with deploy/start blocked by account freeze and inherited exposure `SPY 1`.
- Exact UI error: Compact detail page showed `SICK BAY 2 conditions need a cure before start.`, `POSITION Long 1 SPY`, `UNREALIZED P&L Not proven`, and `SESSION Closed`. Full history showed `LATEST SIGNAL SIGNAL HOLD`, a chart summary with `SPY · Loading… · 67 trades`, broker tail `3834 event(s)`, `ORDER Broker fills 67 events 2026-07-08 13:46:02`, `Broker positions refreshed`, `Broker diagnostic`, `Open orders refreshed`, `Account summary refreshed`, `Position P&L refreshed`, `Account P&L refreshed`, and `Closed trade 33 events`. Recent incidents showed 16 incidents, including repeated `Unknown error — see raw traceback`, `Recovery flatten failed — positions may be open`, and `Engine stopped unexpectedly`. The deploy form showed `Account Frozen`, `Fleet Frozen`, disabled `Deploy & start`, `Account freeze active. Resolve the account sick-bay condition before starting.`, inherited exposure `Posture Long`, `Positions SPY 1`, `Pending orders 0`, `Legs Needs input`, and engine code `Up to date`.
- Related UI state: This is the same-symbol high-risk case: because bot symbol, validation case, signal stream, and exposure are all SPY, there is no identity-conflict warning to slow an operator down. The compact page also hides most lifecycle/proof detail until the operator opens `Full history`, and the recovery action is labeled `Change settings…` rather than a recovery-specific redeploy/review path. The deploy form correctly disables `Deploy & start`, but still shows `Fleet policy Clear` beside `Fleet Frozen`, paper-orders/start-immediately launch copy, and a `Broker account unavailable` input while a connected paper account is shown.
- UX recommendation: For same-symbol open-exposure bots, make the blocker explicit in the first viewport and primary action: `Account frozen: SPY 1 still open after recovery flatten failed`. Rename `Change settings…` to `Review recovery settings` or `Review deploy-only settings`, keep launch controls read-only under freeze, and show disabled action buttons with reasons instead of hiding the unavailable start/redeploy controls behind `More actions`. Collapse `Fleet policy Clear` plus `Fleet Frozen` into one canonical status and group the 16 raw traceback incidents into one recovery-failure summary with a forensic link.

## 2026-07-09T04:58:52Z - DIagVal6

- Bot: `DIagVal6`
- Candidate reason: Different from the prior selected bot `DEPVALSPYJUL8`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Not yet proven · 2026-07-08 23:32:00`.
- Action taken: Opened `/broker/bots`, selected `DIagVal6`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `SESSION Closed`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `DIagVal6 · SPY SPY · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Closed`. Full history showed `BROKER TAIL 827 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, `Account summary refreshed`, `Contract details refreshed`, and `Live bars refreshed`, with latest broker evidence at `2026-07-08 23:44:50`; Recent Incidents showed `No recent incidents`. The deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, and `Fleet policy Clear`.
- Related UI state: The form correctly disabled `Deploy & start` before any order path was possible. Remaining issues: the top broker control still offered `Disconnect` while the status said the data-plane broker session was down; `Change settings…` stayed available even though the blocker was broker connectivity rather than settings; `Recent Incidents` said no incidents despite the active broker-down condition; and the launch section still displayed paper-orders/start-immediately copy below a hard broker-down gate.
- UX recommendation: Promote this into a connection-first recovery flow. Replace the disabled primary action label with `Connect broker session first`, show a single `Reconnect IBKR data-plane` action or direct account-monitor link, and mark broker-tail/account evidence as stale because it cannot refresh while the data-plane client is down. Recent Incidents should include the active broker-down blocker, and settings/launch controls should be read-only or visually secondary until connected-account identity is proven.

## 2026-07-09T05:28:52Z - deiagAPPL6

- Bot: `deiagAPPL6`
- Candidate reason: Different from the prior selected bot `DIagVal6`; fleet list showed AAPL, flat exposure, zero open positions, `Sick bay`, and prior status `Not yet proven · 2026-07-09 00:01:40`.
- Action taken: Opened `/broker/bots`, selected `deiagAPPL6`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `SESSION Closed`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `deiagAPPL6 · AAPL · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Closed`. Full history showed an AAPL chart context, `BROKER TAIL 827 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, `Account summary refreshed`, `Contract details refreshed`, and `Live bars refreshed`, with latest broker evidence at `2026-07-08 23:44:50`; Recent Incidents showed `No recent incidents`. The deploy URL preserved `signal_stream=AAPL` and `inherited_symbol=AAPL`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, and `Validation case SPY`.
- Related UI state: The guard correctly blocks starting without broker proof, and the URL/state preservation for AAPL is good. Remaining issues: the visible deploy form does not make the active AAPL signal stream as prominent as the SPY validation case, `Recent Incidents` says no incidents despite an active broker-down blocker, the top broker action still says `Disconnect` while the data-plane client is down, and launch controls still advertise paper-orders/start-immediately below the hard broker gate.
- UX recommendation: Keep preserving AAPL in the redeploy URL, but show it directly in the command card as `AAPL signal stream · broker disconnected`. Use a connection-first primary action such as `Reconnect IBKR data-plane`, mark broker-tail/account evidence as stale, and include the broker-down blocker in Recent Incidents. Make launch/settings controls read-only or visually secondary until connected-account identity is proven.

## 2026-07-09T05:58:52Z - JUN26GM

- Bot: `JUN26GM`
- Candidate reason: Different from the prior selected bot `deiagAPPL6`; fleet list showed GM, flat/unknown exposure proof, `Sick bay`, and prior status `Safety halt · 2026-06-26 10:12:47`.
- Action taken: Opened `/broker/bots`, selected `JUN26GM`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Not proven`, `SESSION Closed`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `JUN26GM · GM GM · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Not proven`, `UNREALIZED P&L Not proven`, and `SESSION Closed`. Full history showed a GM chart context, `BROKER TAIL 825 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, and `Account summary refreshed`, with latest broker evidence at `2026-07-08 23:44:50`; Recent Incidents showed `No recent incidents`. The deploy URL preserved `inherited_symbol=GM` and `inherited_exposure_posture=UNKNOWN`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Inherited bot symbol GM`, `Signal stream SPY`, `Posture Unknown`, `Positions Flat`, `Pending orders unknown`, `Legs Needs input`, and `Validation case SPY`.
- Related UI state: The connection guard correctly blocked starting before broker proof. Remaining issues: this is an identity-risk variant under broker-down state because the URL carries GM while the visible command card says `Signal stream SPY`; the table says flat while detail says `POSITION Not proven`; `Recent Incidents` says no incidents despite the active broker-down blocker; and paper-orders/start-immediately launch copy remains visible below a hard broker gate.
- UX recommendation: Make unresolved identity/exposure explicit even when broker connection is the primary blocker: `GM bot · signal stream unresolved (currently SPY) · broker disconnected`. Do not let a GM bot's settings review visually default to SPY without an explanation of whether this is validation-case-only copy or the next-run stream. Mark position proof as stale/not-proven consistently across table and detail, include the broker-down blocker in Recent Incidents, and make launch/settings controls read-only until connected-account identity is proven.

## 2026-07-09T06:28:52Z - DEPVALJUL1

- Bot: `DEPVALJUL1`
- Candidate reason: Different from the prior selected bot `JUN26GM`; fleet list showed MU, flat exposure, zero open positions, `Sick bay`, and prior status `Clean · 2026-07-04 22:59:05`.
- Action taken: Opened `/broker/bots`, selected `DEPVALJUL1`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `SESSION Closed`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `DEPVALJUL1 · MU MU · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Closed`. Full history showed MU chart context, `LATEST SIGNAL SIGNAL ENTER`, `BROKER TAIL 825 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, and `Account summary refreshed`, with latest broker evidence at `2026-07-08 23:44:50`. Recent Incidents showed 54 incidents, including `Foreign fill dropped`, repeated `Unknown error — see raw traceback`, `Control-plane lease lost`, `Recovery flatten failed — positions may be open`, and `Broker connection lost`. The deploy URL preserved `inherited_symbol=MU` and `inherited_exposure_posture=FLAT`, but the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Inherited bot symbol MU`, `Signal stream SPY`, `Legs Needs input`, `Fleet policy Clear`, and `Validation case SPY`.
- Related UI state: The connection guard correctly blocked starting before broker proof. Remaining issues: this is another identity-risk variant under broker-down state because the URL carries MU while the visible command card says `Signal stream SPY`; the table says the prior run was `Clean` while the detail is `UNKNOWN`/`SICK BAY`; `Change settings…` remains available even though the active blocker is connectivity; and the top broker control still says `Disconnect` while the data-plane broker session is down.
- UX recommendation: Make the settings-review header connection-first and identity-specific: `MU bot · signal stream unresolved (currently SPY) · broker disconnected`. Explain whether SPY is only the validation case or the next-run stream before the operator can confirm identity. Reconcile the fleet row's `Clean` label with the detail-page sick-bay state, mark broker evidence stale while the data-plane client is down, group the 54 old incidents into a recovery summary, and make launch/settings controls read-only until connected-account identity is proven.

## 2026-07-09T06:58:52Z - dep_val_smoke_001

- Bot: `dep_val_smoke_001`
- Candidate reason: Different from the prior selected bot `DEPVALJUL1`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Exited with error · 2026-06-22 23:33:46`.
- Action taken: Opened `/broker/bots`, selected `dep_val_smoke_001`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `SESSION Closed`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `dep_val_smoke_001 · SPY SPY · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Closed`. Full history showed `LATEST SIGNAL SIGNAL HOLD`, `BROKER TAIL 827 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, `Account summary refreshed`, `Contract details refreshed`, and `Live bars refreshed`, with latest broker evidence at `2026-07-08 23:44:50`. Recent Incidents showed 61 incidents, including `Broker event log write failed`, repeated `Unknown error — see raw traceback`, `Broker connection lost`, `Engine stopped unexpectedly`, and `Recovery flatten failed — positions may be open`. The deploy URL preserved `inherited_symbol=SPY`, `inherited_symbol_source=strategy_spec.symbols fallback`, and `inherited_exposure_posture=FLAT`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, `Engine code Up to date`, and `Validation case SPY`.
- Related UI state: The connection guard correctly blocked starting before broker proof and the symbol is same-symbol SPY, so no identity-conflict warning is needed here. Remaining issues: `Change settings…` remains available even though the active blocker is broker connectivity, the form still surfaces `Fleet policy Clear` and editable-looking legs/launch controls below a hard broker gate, and the top broker control still says `Disconnect` while the data-plane broker session is down.
- UX recommendation: For same-symbol flat bots under broker-down state, keep the disabled `Deploy & start` but relabel the command card to `Connect broker session first`. Make legs/launch read-only while account identity is unproven, move incident history behind a grouped recovery summary, mark broker-tail evidence stale because it cannot refresh, and replace the ambiguous `Disconnect` control with a connection-first action or account-monitor link.

## 2026-07-09T07:29:22Z - Bars-July-6

- Bot: `Bars-July-6`
- Candidate reason: Different from the prior selected bot `dep_val_smoke_001`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Safety halt · 2026-07-07 21:15:02`.
- Action taken: Opened `/broker/bots`, selected `Bars-July-6`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed a live-binding problem plus the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `SESSION Closed`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page showed `Live binding invalid.`, `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `Bars-July-6 · SPY SPY · UNKNOWN`, `SICK BAY 3 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Closed`. Full history initially showed `BROKER TAIL Loading history...`, then hydrated to `BROKER TAIL 827 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, `Account summary refreshed`, `Contract details refreshed`, and `Live bars refreshed`, with latest broker evidence at `2026-07-08 23:44:50`; Recent Incidents showed `No recent incidents`. The deploy URL preserved `signal_stream=SPY`, `inherited_symbol=SPY`, `inherited_symbol_source=run_ledger.live_config.action stock target`, and `inherited_exposure_posture=FLAT`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, `Engine code Up to date`, and `Validation case SPY`.
- Related UI state: The connection guard correctly blocked starting before broker proof, and same-symbol SPY identity was preserved. Remaining issues: `Live binding invalid.` is not carried into the deploy form summary, `Recent Incidents` says no recent incidents despite the active live-binding and broker-down blockers, `Change settings…` remains available even though the primary blocker is connectivity/binding, and the broker tail briefly sits at a loading state before showing stale evidence.
- UX recommendation: Treat `Live binding invalid` as a first-class sick-bay condition in the fleet row, detail header, incidents list, and deploy form. The disabled command should read `Fix live binding and connect broker first`, with `Change settings…` renamed to a read-only `Review blocked settings` path. Mark broker-tail evidence stale while the data-plane client is down, and make legs/launch controls read-only until both live binding and connected-account identity are proven.

## 2026-07-09T07:59:22Z - JUNE-22

- Bot: `JUNE-22`
- Candidate reason: Different from the prior selected bot `Bars-July-6`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Safety halt · 2026-07-07 16:45:41`.
- Action taken: Opened `/broker/bots`, selected `JUNE-22`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `JUNE-22 · SPY SPY · UNKNOWN`, `SICK BAY 3 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history showed `BROKER TAIL 827 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, `Account summary refreshed`, `Contract details refreshed`, and `Live bars refreshed`, with latest broker evidence at `2026-07-08 23:44:50`; Recent Incidents showed `No recent incidents`. The deploy URL preserved `signal_stream=SPY`, `inherited_symbol=SPY`, `inherited_symbol_source=run_ledger.live_config.action stock target`, and `inherited_exposure_posture=FLAT`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, `Engine code Up to date`, and `Validation case SPY`.
- Related UI state: The connection guard correctly blocked starting before broker proof, and same-symbol SPY identity was preserved. Remaining issues: the bot detail shows a live trading window state (`SESSION Pre-market`) even though broker account identity cannot refresh; `Recent Incidents` says no recent incidents despite the active broker-down blocker; `Change settings…` remains available even though the primary blocker is connectivity; and launch/legs controls remain editable-looking below a hard broker gate.
- UX recommendation: For broker-down bots in pre-market, make the session state secondary to account proof: `Pre-market, but broker identity unavailable`. The disabled command should read `Connect broker session first`, with `Change settings…` renamed to `Review blocked settings`. Mark broker-tail evidence stale while the data-plane client is down, include the active broker-down blocker in Recent Incidents, and make legs/launch controls read-only until connected-account identity is proven.

## 2026-07-09T08:29:23Z - June23

- Bot: `June23`
- Candidate reason: Different from the prior selected bot `JUNE-22`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Safety halt · 2026-07-07 15:16:27`.
- Action taken: Opened `/broker/bots`, selected `June23`, waited through an initial `Loading bot control...` state, inspected the compact bot-control detail page after it hydrated, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`, with deploy/start blocked by missing broker connection and unproven connected-account identity.
- Exact UI error: Detail page initially showed `June23 Loading bot control...`, then hydrated to `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `June23 · SPY SPY · UNKNOWN`, `SICK BAY 3 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history showed `BROKER TAIL 827 event(s)`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, `Account summary refreshed`, `Contract details refreshed`, and `Live bars refreshed`, with latest broker evidence at `2026-07-08 23:44:50`; Recent Incidents showed `No recent incidents`. The deploy URL preserved `signal_stream=SPY`, `inherited_symbol=SPY`, `inherited_symbol_source=run_ledger.live_config.action stock target`, and `inherited_exposure_posture=FLAT`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, `Engine code Up to date`, and `Validation case SPY`.
- Related UI state: The connection guard correctly blocked starting before broker proof, and same-symbol SPY identity was preserved. Remaining issues: the initial loading state gives no reason or retry affordance while the broker-down banner is already known; the bot detail shows `SESSION Pre-market` even though broker account identity cannot refresh; `Recent Incidents` says no recent incidents despite the active broker-down blocker; and launch/legs controls remain editable-looking below a hard broker gate.
- UX recommendation: Add a hydration fallback for `Loading bot control...`, such as `Still loading bot state; broker identity is currently unavailable`, with a retry/account-monitor link. For hydrated broker-down bots in pre-market, keep session state secondary to account proof: `Pre-market, but broker identity unavailable`. The disabled command should read `Connect broker session first`, broker-tail evidence should be marked stale, Recent Incidents should include the active broker-down blocker, and legs/launch controls should be read-only until connected-account identity is proven.

## 2026-07-09T08:59:23Z - DVS-SPY-SPY-0701

- Bot: `DVS-SPY-SPY-0701`
- Candidate reason: Different from the prior selected bot `June23`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Safety halt · 2026-07-07 14:44:36`.
- Action taken: Opened `/broker/bots`, selected `DVS-SPY-SPY-0701`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed a live-binding problem plus the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`, with deploy/start blocked by missing broker connection, unproven connected-account identity, and incomplete legs.
- Exact UI error: Detail page showed `Live binding invalid.`, `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `DVS-SPY-SPY-0701 · SPY SPY · UNKNOWN`, `SICK BAY 3 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history showed `LATEST SIGNAL TRADE CHART`, `Not live`, `SPY · Loading…`, `BROKER TAIL 827 event(s)`, `Broker diagnostic 181 events 2026-07-08 23:44:50`, `Open orders refreshed 182 events 2026-07-08 23:44:50`, `Broker positions refreshed 278 events 2026-07-08 23:44:50`, `Account summary refreshed 184 events 2026-07-08 23:44:50`, `Contract details refreshed 1 event 2026-07-08 23:28:48`, and `Live bars refreshed 1 event 2026-07-08 23:28:48`; Recent Incidents showed `No recent incidents`. The deploy URL preserved `signal_stream=SPY`, `inherited_symbol=SPY`, `inherited_symbol_source=run_ledger.live_config.action stock target`, and `inherited_exposure_posture=FLAT`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, `Engine code Up to date`, `safe_canary`, `paper_orders`, checked start-immediately control, and `Validation case SPY`.
- Related UI state: The connection guard correctly blocked starting before broker proof, and same-symbol SPY identity was preserved. Remaining issues: `Live binding invalid.` is not carried into the deploy command card, the only available operator path is still `Change settings…` even though the command is blocked, `Recent Incidents` says no recent incidents despite active live-binding and broker-down blockers, and stale broker evidence from `2026-07-08 23:44:50` is not visually called out as stale while the chart says `Not live`.
- UX recommendation: For bots where only a fresh/redeploy review is possible, make the table and detail action explicit: `Review blocked fresh run` or disabled `Redeploy` with reasons `Fix live binding · connect broker · add legs`. Carry `Live binding invalid` into the deploy command card, mark broker-tail evidence stale, include the active blockers in Recent Incidents, and make legs/launch controls read-only until connected-account identity is proven.

## 2026-07-09T09:29:23Z - Valgate-Jul6

- Bot: `Valgate-Jul6`
- Candidate reason: Different from the prior selected bot `DVS-SPY-SPY-0701`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Safety halt · 2026-07-07 12:46:51`.
- Action taken: Opened `/broker/bots`, selected `Valgate-Jul6`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Did not add legs, deploy, start, stop, retire, disconnect, or submit anything because the UI showed the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`, with deploy/start blocked by missing broker connection, unproven connected-account identity, and incomplete legs.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `Valgate-Jul6 · SPY SPY · UNKNOWN`, `SICK BAY 3 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history initially showed `BROKER TAIL Loading history…`, then hydrated to `Not live`, `286 Polygon`, `SPY · 2026-07-03 to 2026-07-09`, `BROKER TAIL 827 event(s)`, `Broker diagnostic 181 events 2026-07-08 23:44:50`, `Open orders refreshed 182 events 2026-07-08 23:44:50`, `Broker positions refreshed 278 events 2026-07-08 23:44:50`, `Account summary refreshed 184 events 2026-07-08 23:44:50`, `Contract details refreshed 1 event 2026-07-08 23:28:48`, and `Live bars refreshed 1 event 2026-07-08 23:28:48`; Recent Incidents showed `No recent incidents`. The deploy URL preserved `signal_stream=SPY`, `inherited_symbol=SPY`, `inherited_symbol_source=run_ledger.live_config.action stock target`, and `inherited_exposure_posture=FLAT`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, `Engine code Up to date`, `safe_canary`, `paper_orders`, checked start-immediately control, and `Validation case SPY`. After an extra wait, the command card still showed `Engine Checking Waiting for daemon health.`, `Fleet Checking Waiting for fleet policy.`, and `Engine up Checking` even though the top summary said `Live engine Running`, `Fleet policy Clear`, and `Engine code Up to date`.
- Related UI state: The connection guard correctly blocked starting before broker proof, and same-symbol SPY identity was preserved. Remaining issues: the detail page says `SESSION Pre-market` even though account identity cannot refresh; `Recent Incidents` says no recent incidents despite the active broker-down blocker; full history can sit at `BROKER TAIL Loading history…` before showing stale evidence; and the deploy command card has inconsistent `Engine Checking`/`Fleet Checking` copy after the top-level health panels say running/clear/up-to-date.
- UX recommendation: Make the disabled primary action connection-first: `Connect broker session first`, with `Valgate-Jul6 · SPY · flat · broker identity unavailable` in the command header. Treat broker-down as a current incident, mark broker-tail evidence stale, and collapse transient `Engine/Fleet Checking` states once the authoritative top-level checks are resolved. For the table/action menu, show `Fresh run unavailable` or `Redeploy disabled` with blocker chips instead of routing a busy user into editable-looking settings.

## 2026-07-09T09:59:23Z - DEPVALSPYJUL8

- Bot: `DEPVALSPYJUL8`
- Candidate reason: Different from the prior selected bot `Valgate-Jul6`; fleet list showed SPY signal/exposure, open position count `1`, `Sick bay`, and prior status `Exited with error · 2026-07-08 13:47:01`.
- Action taken: Opened `/broker/bots`, selected `DEPVALSPYJUL8`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards and inherited exposure. Did not add legs, confirm exposure state, deploy, start, stop, retire, disconnect, or submit anything because the UI showed a long SPY position with prior recovery failure, plus the IBKR data-plane broker session was down and connected-account identity could not be proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Long 1 SPY`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. The prior run had died with recovery trouble, and redeploy/start was unsafe because account identity and current broker state could not be proven.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `DEPVALSPYJUL8 · SPY SPY · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Long 1 SPY`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history showed `LATEST SIGNAL SIGNAL HOLD`, `Not live`, `286 Polygon`, `SPY · 2026-07-03 to 2026-07-09`, `BROKER TAIL 827 event(s)`, broker evidence last refreshed at `2026-07-08 23:44:50`, and `16 incidents`. Incident highlights included repeated `Unknown error — see raw traceback`, `Control-plane lease lost`, `Recovery flatten failed — positions may be open`, and `Engine stopped unexpectedly`. The deploy URL preserved `signal_stream=SPY`, `inherited_symbol=SPY`, `inherited_symbol_source=run_ledger.live_config.action stock target`, `inherited_exposure_posture=LONG`, `inherited_exposure_positions={"SPY":1}`, and `inherited_exposure_pending_order_count=0`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Inherited exposure Posture Long`, `Positions SPY 1`, `Pending orders 0`, `Confirm exposure state`, `Legs Needs input`, `safe_canary`, `paper_orders`, checked start-immediately control, and `Validation case SPY`. After an extra wait, the form still showed `Live engine Checking…`, `Engine Checking Waiting for daemon health.`, `Fleet policy Checking…`, and `Engine up Checking`.
- Related UI state: This is the first recent sample where the UI preserved and displayed a non-flat inherited exposure, which is good. Remaining issues: the action menu still routes to `Change settings…` even though the operator needs a risk-recovery flow, `Confirm exposure state` is enabled while broker account identity is unavailable, the `Deploy & start` label does not explain that the inherited long position and failed recovery make redeploy unsafe, and the incident list still relies on repeated `Unknown error — see raw traceback` rows instead of summarizing the actual operator hazard.
- UX recommendation: For bots with `Recovery flatten failed — positions may be open`, promote a recovery-first action: `Review open exposure` or `Reconnect broker to verify/flatten` instead of `Change settings…`. Disable or explain `Confirm exposure state` until broker identity and positions can refresh. The table row should show a strong risk badge such as `Open SPY 1 · recovery failed`, and the deploy command card should say `Start disabled: verify existing SPY position before redeploy`.

## 2026-07-09T10:29:23Z - tsladepvaljul7

- Bot: `tsladepvaljul7`
- Candidate reason: Different from the prior selected bot `DEPVALSPYJUL8`; fleet list showed TSLA signal/exposure, open position count `1`, `Sick bay`, and prior status `Exited with error · 2026-07-07 11:39:05`.
- Action taken: Opened `/broker/bots`, selected `tsladepvaljul7`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards and inherited exposure. Did not add legs, confirm exposure state, deploy, start, stop, retire, disconnect, or submit anything because the UI showed a long TSLA position, foreign-fill/engine-stop incidents, and the IBKR data-plane broker session was down with connected-account identity unavailable.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Long 1 TSLA`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. The prior run had exited with error and an exit signal was visible, but redeploy/start was unsafe because account identity, current broker state, and exposure proof could not refresh.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `tsladepvaljul7 · TSLA · UNKNOWN`, `SICK BAY 3 conditions need a cure before start.`, `POSITION Long 1 TSLA`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history showed `LATEST SIGNAL SIGNAL EXIT`, `Not live`, `885 Polygon`, `TSLA · 2026-07-03 to 2026-07-09`, `BROKER TAIL 827 event(s)`, broker evidence last refreshed at `2026-07-08 23:44:50`, and `6 incidents`. Incident highlights included `Foreign fill dropped`, repeated `Unknown error — see raw traceback`, and `Engine stopped unexpectedly`. The deploy URL preserved `signal_stream=TSLA`, `inherited_symbol=TSLA`, `inherited_symbol_source=run_ledger.live_config.action stock target`, `inherited_exposure_posture=LONG`, `inherited_exposure_positions={"TSLA":1}`, and `inherited_exposure_pending_order_count=0`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Inherited exposure Posture Long`, `Positions TSLA 1`, `Pending orders 0`, `Confirm exposure state`, `Legs Needs input`, `safe_canary`, `paper_orders`, checked start-immediately control, `Validation case SPY`, and `Signal stream TSLA`. The form also kept showing `Live engine Checking…`, `Engine Checking Waiting for daemon health.`, and `Engine up Checking`.
- Related UI state: The UI correctly preserved the TSLA inherited symbol and long exposure in the redeploy URL/form, avoiding the prior SPY/TSLA identity-conflict problem. Remaining issues: `SIGNAL EXIT` plus `Long 1 TSLA` is a strong operator hazard but is not surfaced as the primary command reason, `Confirm exposure state` is enabled while broker identity is unavailable, `Foreign fill dropped` is not summarized into a clear recommended recovery action, and the form mixes `Signal stream TSLA` with `Validation case SPY` without a compact explanation in the command card.
- UX recommendation: For non-flat bots with a latest exit signal, make the action recovery-first: `Verify/close TSLA exposure` or `Reconnect broker to confirm exit` instead of `Change settings…`. The command card should say `Start disabled: TSLA long still shown after exit signal; broker proof unavailable`. Disable or gate `Confirm exposure state` until account identity and positions refresh, and summarize `Foreign fill dropped` as a named hazard with an account-monitor/reconciliation next step.

## 2026-07-09T10:59:23Z - PrajiDemo

- Bot: `PrajiDemo`
- Candidate reason: Different from the prior selected bot `tsladepvaljul7`; fleet list showed AAPL, `Flat`, unknown position/open-order counts (`— —`), `Sick bay`, and prior status `Exited with error · 2026-07-03 08:57:35`.
- Action taken: Opened `/broker/bots`, selected `PrajiDemo`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards, run identity, and inherited exposure. Did not add legs, confirm new run identity, confirm exposure state, deploy, start, stop, retire, disconnect, or submit anything because the UI showed broker data-plane down, account identity unavailable, AAPL/SPY run-identity mismatch, and exposure not proven.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Not proven`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. The table's `Flat` row did not carry through as proven exposure, and redeploy/start was unsafe because account identity, run identity, and exposure proof could not refresh.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `PrajiDemo · AAPL · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Not proven`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history showed `Not live`, `912 Polygon`, `AAPL · 2026-07-03 to 2026-07-09`, `BROKER TAIL 827 event(s)`, broker evidence last refreshed at `2026-07-08 23:44:50`, and `4 incidents`. Incident highlights included repeated `Unknown error — see raw traceback` and `Broker connection lost`. The deploy URL preserved `instance_id=PrajiDemo`, `inherited_symbol=AAPL`, `inherited_symbol_source=run_ledger.live_config.action stock target`, `inherited_exposure_posture=UNKNOWN`, and empty `inherited_exposure_positions`, but also carried `signal_stream=SPY`; the deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Run identity`, `Inherited bot symbol AAPL`, `Signal stream SPY`, `Live Config Symbol`, `Confirm new run identity`, `Inherited exposure Posture Unknown`, `Positions Flat`, `Pending orders unknown`, `Confirm exposure state`, `Legs Needs input`, `safe_canary`, `paper_orders`, checked start-immediately control, and `Validation case SPY`. The form also showed `Live engine Running`, `Engine code Up to date`, but command-card checks still included `Engine Checking`, `Fleet Checking`, and `Engine up Checking`.
- Related UI state: This is a useful identity-mismatch variant: the bot is AAPL, the validation case is SPY, and the signal stream defaults to SPY even while broker identity/exposure are unavailable. Remaining issues: the fleet row says `Flat` despite detail saying `POSITION Not proven`; `Confirm new run identity` and `Confirm exposure state` are enabled while broker account identity is unavailable; `Positions Flat` appears next to `Posture Unknown`; and the incidents summarize as raw traceback rows rather than a broker-connection recovery instruction.
- UX recommendation: In the table, render this as `Exposure not proven` instead of `Flat` when counts are unknown. In the deploy form, make `AAPL bot · SPY signal stream` a hard identity warning in the command card and disable or explain `Confirm new run identity` until broker/account proof is available. Clarify whether `SPY` is validation-case-only or the next run's actual stream, and replace `Change settings…` with `Review blocked identity/exposure` for this state.

## 2026-07-09T11:29:23Z - Validation-Jul6

- Bot: `Validation-Jul6`
- Candidate reason: Different from the prior selected bot `PrajiDemo`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and prior status `Clean · 2026-07-06 10:02:31`.
- Action taken: Opened `/broker/bots`, selected `Validation-Jul6`, inspected the compact bot-control detail page, opened `More actions`, checked `Full history`, opened the inline `why?` state explanation, then used `Change settings…` only to inspect deploy-form guards. Attempted to select the `Legs` tab so a supported long-stock/close-leg action plan could be added before any submit, but the tab state became inconsistent and the leg controls stayed hidden/unactionable. Did not deploy, start, stop, retire, disconnect, or submit anything because the UI showed broker data-plane down, account identity unavailable, and an active account-wide freeze.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-credentials`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. The prior run looked clean in the fleet row, but current deploy/start was blocked by missing broker proof, unavailable connected-account identity, unresolved reconciliation proof, and a hidden action-plan panel.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `Validation-Jul6 · SPY SPY · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. Full history showed `Not live`, `286 Polygon`, `SPY · 2026-07-03 to 2026-07-09`, `BROKER TAIL 827 event(s)`, broker evidence last refreshed at `2026-07-08 23:44:50`, and `96 incidents`; incident rows were mostly `BROKER UNKNOWN ... Unknown error — see raw traceback` plus `Infra` rows. The `why?` explanation showed `WHY THIS STATE`, `This account has an active freeze. An account-wide unresolved-exposure artifact is present, so every sibling bot must treat submission as stopped.`, `Broker proof is not available yet.`, `Account frozen`, `Waiting for reconciliation proof.`, `No live runtime is bound yet.`, `Latest Reconcile no reconcile receipt`, `Broker Safety Verdict Unknown`, `Broker Connection Unknown`, `Reconciliation State Not Available`, and `Account Freeze Watchdog Flatten Timed Out 2026-07-08 13:47:01`. The deploy form showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Legs Needs input`, `Fleet policy Clear`, `Engine code Up to date`, `safe_canary`, `paper_orders`, checked start-immediately control, `Signal stream SPY`, and `Validation case SPY`.
- Related UI state: The broker/account guard correctly blocked deployment. Same-symbol SPY identity was preserved and `safe_canary`/`paper_orders` defaults were good. Remaining issues: the fleet row says prior outcome `Clean` even though the current state is not trade-ready; the current `why?` explanation classifies the account-wide unresolved-exposure artifact better than the command card does; the incident list repeats `Unknown error — see raw traceback` instead of summarizing the account freeze; and after selecting `Legs`, both `Strategy` and `Legs` reported `aria-selected="true"` while the `Legs` action-plan panel still had `step-hidden` and zero-size `+ Add stock` / `+ Add option` controls.
- UX recommendation: Split prior run outcome from current readiness in the fleet row, e.g. `Last run: Clean` plus `Current: account freeze · broker proof unavailable`. Promote the `why?` freeze explanation into the disabled command card: `Start disabled: connect broker and resolve account freeze/reconcile receipt`. Collapse repeated raw incidents into one account-freeze summary with a recovery link. Fix the deploy wizard tab state so only one tab is active and the `Legs` panel becomes visible/actionable before requiring `Legs Needs input`; if broker/account proof is unavailable, make the action-plan tab read-only with the reason instead of leaving hidden controls.

## 2026-07-09T11:59:23Z - DIagVal6

- Bot: `DIagVal6`
- Candidate reason: Different from the prior selected bot `Validation-Jul6`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, and a refreshed prior status `Not yet proven · 2026-07-09 06:46:14`.
- Action taken: Opened `/broker/bots`, selected `DIagVal6`, inspected the compact bot-control detail page, opened the inline `why?` state explanation, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards. Tried to open the `Legs` tab so the required long-stock/close-leg action plan could be reviewed or added, but the visible tab did not activate and the action-plan controls remained hidden at zero size. Did not deploy, start, stop, retire, disconnect, add legs, or submit anything because the UI showed broker data-plane down, account identity unavailable, an account-wide freeze, and a durable STOPPED intent latch.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. A safe redeploy/start was blocked before launch by missing broker proof, unavailable connected-account identity, unresolved reconciliation proof, account freeze, desired-state STOPPED, and inaccessible action-plan controls.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `DIagVal6 · SPY SPY · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. The `why?` explanation showed `This account has an active freeze. An account-wide unresolved-exposure artifact is present, so every sibling bot must treat submission as stopped.`, `Broker proof is not available yet.`, `Trade submit Account frozen`, `Reconciliation Waiting for reconciliation proof.`, `Runtime No live runtime is bound yet.`, `Desired State STOPPED — start refused until intent changes`, `Latest Reconcile no reconcile receipt`, `Broker Safety Verdict Unknown`, `Broker Connection Unknown`, `Submission Capability State Unknown`, `Reconciliation State Not Available`, and `Account Freeze Watchdog Flatten Timed Out 2026-07-08 13:47:01`. Full history showed `Not live`, `SPY · Loading…`, `BROKER TAIL 827 event(s)`, broker evidence last refreshed at `2026-07-08 23:44:50`, and `Recent Incidents No recent incidents No warnings or errors for this run.` The deploy form preserved `signal_stream=SPY`, `inherited_symbol=SPY`, `inherited_exposure_posture=FLAT`, `safe_canary`, `paper_orders`, and checked start-immediately control, but showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Engine Checking`, `Fleet Checking`, `Fleet clear Checking`, and `Legs Needs input`.
- Related UI state: Same-symbol SPY identity and safe sizing/launch defaults were preserved. The stronger operator issue is priority/copy: the `why?` panel knows about durable STOPPED and account freeze, while the deploy command card mostly says broker/account unavailable and required fields missing. `Recent Incidents` says no incidents even though the state drawer lists active blockers. The `Legs` tab remains visibly clickable, but after clicking it stayed `aria-selected="false"` while the action-plan panel remained `step-hidden` with zero-size `+ Add stock` and `+ Add option` buttons.
- UX recommendation: Promote STOPPED intent and account freeze into the primary command card: `Start disabled: resume intent, resolve account freeze, and connect broker`. If a durable STOPPED latch is present, offer a dedicated read-only recovery route instead of `Deploy & start` language. Treat the `why?` blockers as current incidents, not only hidden diagnostics. Fix the deploy wizard so `Legs Needs input` either opens an actionable panel or is disabled with an explanation while broker/account proof is unavailable.

## 2026-07-09T12:29:24Z - deiagAPPL6

- Bot: `deiagAPPL6`
- Candidate reason: Different from the prior selected bot `DIagVal6`; fleet list showed AAPL, flat exposure, zero open positions, `Sick bay`, and a refreshed prior status `Not yet proven · 2026-07-09 07:03:03`.
- Action taken: Opened `/broker/bots`, selected `deiagAPPL6`, inspected the compact bot-control detail page, opened the inline `why?` state explanation, opened `More actions`, checked `Full history`, then used `Change settings…` only to inspect deploy-form guards and symbol propagation. Tried to open the `Legs` tab so the required long-stock/close-leg action plan could be reviewed or added, but the tab did not activate and the action-plan controls remained hidden at zero size. Did not deploy, start, stop, retire, disconnect, add legs, or submit anything because the UI showed broker data-plane down, account identity unavailable, an account-wide freeze, and a durable STOPPED intent latch.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new trading action was taken. The bot remained `SICK BAY`, account/broker status `UNKNOWN`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. A safe deploy/start was blocked before launch by missing broker proof, unavailable connected-account identity, unresolved reconciliation proof, account freeze, desired-state STOPPED, and inaccessible action-plan controls.
- Exact UI error: Detail page showed `IBKR Data-plane broker session down`, `IB Gateway/TWS may be logged in, but the FastAPI data-plane IBKR client is not connected. Account positions, connected-account identity, and reconciliation evidence cannot refresh.`, `deiagAPPL6 · AAPL · UNKNOWN`, `SICK BAY 2 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Pre-market`. The `why?` explanation showed `This account has an active freeze. An account-wide unresolved-exposure artifact is present, so every sibling bot must treat submission as stopped.`, `Broker proof is not available yet.`, `Trade submit Account frozen`, `Reconciliation Waiting for reconciliation proof.`, `Runtime No live runtime is bound yet.`, `Desired State STOPPED — start refused until intent changes`, `Latest Reconcile no reconcile receipt`, `Broker Safety Verdict Unknown`, `Broker Connection Unknown`, `Submission Capability State Unknown`, `Reconciliation State Not Available`, and `Account Freeze Watchdog Flatten Timed Out 2026-07-08 13:47:01`. Full history showed `Not live`, `AAPL · Loading…`, `BROKER TAIL 827 event(s)`, broker evidence last refreshed at `2026-07-08 23:44:50`, and `Recent Incidents No recent incidents No warnings or errors for this run.` The deploy URL preserved `signal_stream=AAPL`, `inherited_symbol=AAPL`, and `inherited_exposure_posture=FLAT`; the deploy form preserved AAPL in the signal stream field, `safe_canary`, `paper_orders`, and checked start-immediately control, but showed `Data-plane broker session down`, `Broker Hard down`, `Broker Disconnected`, `Connected broker account unavailable. Connect the broker session before deploying.`, `Account Not proven`, `Fields Required fields missing`, disabled `Deploy & start`, `Engine Checking`, `Fleet Checking`, `Fleet clear Checking`, `Legs Needs input`, and `Validation case SPY`.
- Related UI state: The good news is the AAPL bot no longer falls back to SPY for the live signal stream; `signal_stream=AAPL` and `inherited_symbol=AAPL` were preserved. The confusing part is that the command card still emphasizes broker/account and generic required fields while the state drawer knows about durable STOPPED and account freeze. `Recent Incidents` says no incidents despite active blockers. The `Legs` tab stayed `aria-selected="false"` after click, leaving the action-plan panel `step-hidden` with zero-size `+ Add stock` and `+ Add option` controls.
- UX recommendation: Keep the correct AAPL propagation, but surface it in the command header as `AAPL bot · AAPL signal stream · broker proof unavailable`. Promote STOPPED intent and account freeze into the disabled command card: `Start disabled: resume intent, resolve account freeze, and connect broker`. Treat the `why?` blockers as current incidents, not hidden diagnostics. Fix the wizard so `Legs Needs input` either opens a visible action-plan panel or is disabled with a clear reason while broker/account proof is unavailable.

## 2026-07-09T12:59:24Z - dep_val_smoke_002

- Bot: `dep_val_smoke_002`
- Candidate reason: Different from the prior selected bot `deiagAPPL6`; fleet list showed SPY, flat exposure, zero open positions, `Retired`, and `Blocked: desired_state — STOPPED — start refused until intent changes. Not yet proven · 2026-07-09 07:32:36`.
- Action taken: Opened `/broker/bots`, selected `dep_val_smoke_002`, inspected the sparse retired detail page, opened the inline `why?` explanation, checked `Full history`, then used `Change settings…` to inspect the deploy-only recovery form. Added the required deployment_validation action plan locally: `leg_1 long SPY × 1` with matching `CLOSE leg_1`, using safe-canary sizing and paper-orders launch defaults. Submitted the enabled `Deploy` button only after the UI showed paper broker proof; the UI blocked before creating or starting a run. Did not start, stop, retire, disconnect, or submit any broker order.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new bot runtime was created and no order/fill appeared. The bot returned to the fleet list as `Retired`, SPY, flat exposure, zero open positions, with refreshed status `Blocked: desired_state — STOPPED — start refused until intent changes. Not yet proven · 2026-07-09 08:04:43`.
- Exact UI error: Detail page showed `dep_val_smoke_002 · SPY SPY · PAPER` and `RETIRED`. The `why?` explanation showed `This account has an active freeze. An account-wide unresolved-exposure artifact is present, so every sibling bot must treat submission as stopped.`, `Paper broker proof is available; broker connection remains unproven.`, `Trade submit Account frozen`, `Runtime No live runtime is bound yet.`, `Desired State STOPPED — start refused until intent changes`, `Latest Reconcile no reconcile receipt`, `Broker Safety Verdict Paper Only`, `Broker Connection Unknown`, `Submission Capability State Unknown`, `Reconciliation State Not Available`, and `Account Freeze Watchdog Flatten Timed Out 2026-07-08 13:47:01`. Full history showed `Not live`, `BROKER TAIL 2703 event(s)`, latest broker evidence `Account P&L refreshed`, `Position P&L refreshed`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, and `Account summary refreshed` around `2026-07-09 08:02:02` to `08:02:10`, plus `Recent Incidents No recent incidents`. The deploy form showed `Data-plane paper session connected`, broker account `DUM284968`, `Account Frozen`, `Fleet Frozen`, `Fleet policy Clear`, `Deploy only`, `This bot is off duty. This submit will deploy only; run roll call on the bot page before starting.`, `Fleet clear Account frozen`, action-plan `leg_1 long SPY × 1` and `CLOSE leg_1`. After submit, the user-facing alert was `Deploy — blocked This broker account is frozen until unresolved exposure is reconciled. A run with these inputs already exists, or the working tree is dirty. Commit or stash the listed paths, then deploy again.`
- Related UI state: This is the first recent sample where the Legs tab worked correctly after selection and the action plan could be completed. The safety posture still has copy/affordance gaps: the command card enabled `Deploy` while showing `Account frozen`, `Fleet Frozen`, and a retired/STOPPED source bot; `Fleet policy Clear` sat beside `Fleet Frozen`; the retired detail page was almost empty until opening `why?`; and the final alert combined account freeze, duplicate-run, and dirty-working-tree causes without showing which one actually blocked this submit.
- UX recommendation: For retired durable-STOPPED bots, make the primary action a recovery-specific route such as `Review retired bot` or `Clone/recreate without starting`, and keep any deploy button disabled until the account-freeze cause is acknowledged or resolved. Promote the `why?` blockers directly into the deploy command card: `Deploy blocked: account freeze + desired state STOPPED`. Split the final precondition alert into separate rows with proven/unknown status for `Account freeze`, `Duplicate input run`, and `Working tree dirty`, including the exact dirty paths or duplicate run id when applicable. Keep the fixed Legs-tab behavior.

## 2026-07-09T13:29:24Z - Bars-July-6

- Bot: `Bars-July-6`
- Candidate reason: Different from the prior selected bot `dep_val_smoke_002`; fleet list showed SPY, flat exposure, zero open positions, `Sick bay`, three conditions, and prior status `Safety halt · 2026-07-07 21:15:02`.
- Action taken: Opened `/broker/bots`, selected `Bars-July-6`, inspected the detail page, opened the inline `why?` state explanation, checked `Full history`, opened the recovery details drawer, then used `Change settings…` to inspect the deploy/start form. Added the required deployment_validation action plan: `leg_1 long SPY × 1` with matching `CLOSE leg_1`, with `safe_canary`, `paper_orders`, and `Start trading immediately` already selected. Did not submit because `Deploy & start` remained disabled under account freeze.
- Run id: None created during this attempt.
- Outcome category: `blocked-by-safety`
- Outcome: No new runtime was created and no order/fill appeared. The bot returned to the fleet list as `Off duty`, `Sick bay`, SPY, flat exposure, zero open positions, with the same `Safety halt · 2026-07-07 21:15:02` last-run status.
- Exact UI error: Detail page showed `Live binding invalid.`, `Bars-July-6 · SPY SPY · PAPER`, `SICK BAY`, `3 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Open`. The `why?` / recovery drawer showed `This account has an active freeze.`, `An account-wide unresolved-exposure artifact is present, so every sibling bot must treat submission as stopped.`, `Paper broker proof is available; broker connection remains unproven.`, `Trade submit Account frozen`, `Runtime No live runtime is bound yet.`, `Poison Sentinel poisoned`, `Latest Reconcile no reconcile receipt`, `Host Process State Waiting For Host`, `Broker Safety Verdict Paper Only`, `Broker Connection Unknown`, `Submission Capability State Satisfied`, `Trading Session Phase RTH 2026-07-09 08:31:20`, and `Account Freeze Watchdog Flatten Timed Out 2026-07-08 13:47:01`. Full history showed `Not live`, then `LIVE`, `286 Polygon`, `SPY · Streaming`, `BROKER TAIL 6784 event(s)`, latest broker evidence including `Account P&L refreshed`, `Position P&L refreshed`, `Live bars refreshed`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, and `Account summary refreshed` around `2026-07-09 08:30:57` to `08:31:06`, plus `Recent Incidents No recent incidents`. The deploy form showed `Data-plane paper session connected`, account `DUM284968`, `Account Frozen`, `Fleet Frozen`, `Fleet policy Clear`, disabled `Deploy & start`, `Account freeze active. Resolve the account sick-bay condition before starting.`, `Open account monitor`, `Fleet clear Account frozen`, completed SPY action plan, `safe_canary`, `paper_orders`, and checked start-immediately control.
- Related UI state: This is a better safety result than the retired-bot case: even after the action plan became complete, `Deploy & start` stayed disabled. Remaining issues: the deploy form drops the important `Live binding invalid` and `Poison Sentinel poisoned` blockers from the main command card; `Fleet policy Clear` conflicts with `Fleet Frozen`; launch controls remain editable-looking under an account freeze; `Recent Incidents` says no recent incidents even though the state drawer lists active poison/freeze blockers; and the action menu places destructive `Take off roster` / `Retire & Replace` beside benign inspection actions.
- UX recommendation: Keep disabling `Deploy & start` under account freeze, but change the disabled command copy to `Start blocked: account freeze + poison sentinel + live binding invalid`. Mirror `Live binding invalid` and `Poison Sentinel poisoned` into the deploy form and Recent Incidents, collapse `Fleet policy Clear` plus `Fleet Frozen` into one canonical status, and visually separate destructive roster/retire actions from safe review actions.

## 2026-07-09T13:59:24Z - JUNE-22

- Bot: `JUNE-22`
- Candidate reason: Different from the prior selected bot `Bars-July-6`; fleet list showed SPY, flat exposure, zero open positions, `Off duty`, `Sick bay`, three conditions, and prior status `Safety halt · 2026-07-07 16:45:41`.
- Action taken: Opened `/broker/bots`, selected `JUNE-22`, inspected the detail page, opened the inline `why?` state explanation, checked `Full history`, then used `Change settings…` to inspect the deploy/start form. Added the required deployment_validation action plan locally: `leg_1 long SPY × 1` with matching `CLOSE leg_1`, with `safe_canary`, `paper_orders`, and `Start trading immediately` selected. Did not submit because `Deploy & start` remained disabled under account freeze.
- Run id: None created during this attempt; the redeploy form inherited `parent_run_id=628a810b7ebd14424b244cedf51bd03071ca25fe03bdfae45c614d8141fbf4fa`.
- Outcome category: `blocked-by-safety`
- Outcome: No new runtime was created and no new order/fill was attributable to this attempt. The bot returned to the fleet list as `Off duty`, `Sick bay`, SPY, flat exposure, zero open positions, with the same `Safety halt · 2026-07-07 16:45:41` last-run status.
- Exact UI error: Detail page showed `JUNE-22 · SPY SPY · PAPER`, `SICK BAY`, `3 conditions need a cure before start.`, `POSITION Flat`, `UNREALIZED P&L Not proven`, and `SESSION Open`. The `why?` drawer showed `This account has an active freeze.`, `An account-wide unresolved-exposure artifact is present, so every sibling bot must treat submission as stopped.`, `Paper broker proof is available; broker connection remains unproven.`, `Trade submit Account frozen`, `Account owner Owner generation 0 is accepting commands.`, `Reconciliation Waiting for reconciliation proof.`, `Runtime No live runtime is bound yet.`, `Poison Sentinel poisoned`, `Latest Reconcile no reconcile receipt`, `Host Process State Idle`, `Broker Safety Verdict Paper Only`, `Broker Connection Unknown`, `Submission Capability State Satisfied`, `Reconciliation State Not Available`, `Trading Session Phase RTH 2026-07-09 09:02:08`, `Account Freeze Watchdog Flatten Timed Out 2026-07-08 13:47:01`, and `Readiness Poison Sentinel Poisoned 2026-07-09 09:02:08`. Full history showed `LIVE`, `286 Polygon`, `SPY · Streaming`, `BROKER TAIL 9894 event(s) · stream projection`, latest broker evidence including `Account P&L refreshed`, `Live bars refreshed`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, and `Account summary refreshed` around `2026-07-09 09:02:50` to `09:02:57`, plus a historical `Order submitted 2 events 2026-07-09 08:50:07` and `Recent Incidents No recent incidents`. The deploy form showed `Data-plane paper session connected`, account `DUM284968`, `Account Frozen`, `Fleet Frozen`, `Fleet policy Clear`, disabled `Deploy & start`, `Account freeze active. Resolve the account sick-bay condition before starting.`, `Open account monitor`, `Fleet clear Account frozen`, completed SPY action plan, `readonly_at_start: false · submission_capability: PAPER_ORDERS_ENABLED`, `Paper orders are enabled for launch. Guardrail limit: 2000 orders/day.`, and checked `Start trading immediately`.
- Related UI state: The safety gate behaved correctly: once the action plan was complete, `Deploy & start` still stayed disabled. Remaining issues: the deploy form hides the important `Poison Sentinel poisoned` and live-runtime binding blockers behind the detail-page `why?` drawer; `Fleet policy Clear` conflicts with `Fleet Frozen`; `Recent Incidents` says no recent incidents even though active account-freeze/poison/reconcile blockers are present; the broker tail includes a historical `Order submitted` event without clear attribution to another run; and the action menu still groups destructive `Take off roster` / `Retire & Replace` near safe inspection actions.
- UX recommendation: Keep disabling `Deploy & start` under account freeze, but change the command-card copy to `Start blocked: account freeze + poison sentinel + no reconcile receipt`. Mirror `Poison Sentinel poisoned`, `Broker Connection Unknown`, and `Runtime No live runtime is bound yet` into the deploy form and Recent Incidents. Collapse `Fleet policy Clear` plus `Fleet Frozen` into one canonical fleet status, add run attribution to broker-tail order events, and visually separate destructive roster/retire actions from safe review actions.

## 2026-07-09T14:29:24Z - qqqjul9

- Bot: `qqqjul9`
- Candidate reason: New top-of-list paper bot, different from prior `JUNE-22`; fleet showed QQQ, `Flat` exposure with unknown open-position/P&L counts (`— —`), `Off duty`, `Sick bay`, one condition, and `Not yet proven · 2026-07-09 09:30:08`.
- Action taken: Opened `/broker/bots`, selected `qqqjul9`, inspected detail, opened `why?`, checked `Full history`, then used `Change settings…` to inspect the deploy/start form. Added a supported action plan using the inherited bot symbol: `leg_1 long QQQ × 1` with matching `CLOSE leg_1`. Did not click `Run account reconcile`, `Confirm new run identity`, `Confirm exposure state`, `Deploy & start`, roster actions, retire actions, disconnect, or any broker-order control.
- Run id: None created during this attempt; the redeploy form inherited `parent_run_id=c63b2affdfae197fde6b604b3d69ecbafb1a0f82fe6c221a30798e359b4330c8`.
- Outcome category: `blocked-by-safety`
- Outcome: No new runtime was created and no order/fill appeared. The bot returned to the fleet list as `Off duty`, `Sick bay`, QQQ, with stale account evidence and `Not yet proven · 2026-07-09 09:33:45`.
- Exact UI error: Detail page showed `qqqjul9 · QQQ QQQ · PAPER`, `SICK BAY`, `1 condition need a cure before start.`, `Account evidence stale`, `Receipt acct-recon-DUM284968-1783606106456-e2804ad325bfc4b9 expired before this triage snapshot.`, `Run account reconcile`, `POSITION Not proven`, `UNREALIZED P&L Not proven`, and `SESSION Open`. The `why?` drawer showed `Broker state is not proven enough to submit.`, `The backend cannot prove the broker/session/reconciliation facts needed before a submit.`, `Paper broker proof is available; broker connection remains unproven.`, `Trade submit Broker state unproven`, `Runtime No live runtime is bound yet.`, `Desired State no durable intent recorded`, `Latest Reconcile no reconcile receipt`, `Host Process State Idle`, `Broker Safety Verdict Paper Only`, `Broker Connection Unknown`, `Submission Capability State Unknown`, `Reconciliation State Not Available`, and `Readiness Desired State no durable intent recorded 2026-07-09 09:30:36`. Full history showed `Not live`, `QQQ · Loading…`, `BROKER TAIL 160 event(s) · stream projection`, latest broker evidence `Account P&L refreshed 2026-07-09 09:31:53`, `Broker diagnostic`, `Open orders refreshed`, `Broker positions refreshed`, and `Account summary refreshed 2026-07-09 09:31:48`, plus `Recent Incidents No recent incidents`. The deploy form showed `Account Clean`, `Fleet Clear`, disabled `Deploy & start`, `Inherited bot symbol QQQ conflicts with Signal stream SPY. Confirm the new run identity before Deploy & start.`, `Inherited exposure Posture Unknown`, `Positions Flat`, `Pending orders unknown`, `Confirm new run identity`, `Confirm exposure state`, completed QQQ action plan, `readonly_at_start: false · submission_capability: PAPER_ORDERS_ENABLED`, and checked `Start trading immediately`. After returning to the fleet, the row showed `Receipt acct-recon-DUM284968-1783607563266-5cf2a7fb08b45b19 expired before this triage snapshot.`
- Related UI state: This sample exposed a useful contradiction: the detail/why surfaces say broker state and reconciliation are unproven, but the deploy form says `Account Clean`, `Broker Connected`, `Fleet Clear`, and `Fields Complete`. The form correctly keeps `Deploy & start` disabled, but it also leaves `Confirm new run identity` and `Confirm exposure state` enabled while the evidence says pending orders are unknown and the signal stream is still SPY for a QQQ bot. The fleet row says `Flat` even though detail says `POSITION Not proven`, and `Recent Incidents` ignores the stale reconcile receipt.
- UX recommendation: Make stale account evidence and missing reconcile receipt the primary disabled-command reason: `Start blocked: reconcile receipt expired; cannot prove QQQ exposure or pending orders.` If inherited symbol and signal stream conflict, offer a single explicit choice like `Use QQQ from bot ledger` or `Use SPY validation stream`, then regenerate the action-plan symbol from that choice. Disable `Confirm exposure state` until pending orders are proven, show `Exposure not proven` in the fleet instead of `Flat`, and surface stale reconcile receipts as current incidents with one recovery button.
