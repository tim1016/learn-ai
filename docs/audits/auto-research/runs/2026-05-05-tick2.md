# Auto-research run — 2026-05-05 (tick 2)

**Mode:** baseline-in-progress
**Started:** 2026-05-05T23:45:59-05:00
**Ended:** 2026-05-05T23:45:59-05:00 (interactive single-tick — user reported they would be unable to interact further during this run)
**Stop reason:** manual-interrupt (interactive single-tick)
**Phases touched:** [1 (continuation/completion), 3 (prep grep + rollup)]
**Cursor at end:** see `state.json` — phase=2 (`python-authority`), Phase 3 per-file triage deferred to next tick.

## Findings opened (12 new — F-0009 through F-0020, no F-0009 was opened twice; F-0009..F-0020 contiguous except for ID assignment order)

### P1 (5 new)
- **F-0009 — timestamp — `awaiting-human`** — `app/services/sanitizer.py:79` emits `strftime("%Y-%m-%dT%H:%M:%S.%fZ")` for the wire and line 57 silently drops duplicates. Cross-references prior audit `docs/audits/computational-fidelity-2026-04-22.md` top-10 #1 + #2 (CRITICAL). Status `awaiting-human` because remediation is a coordinated change spanning Python sanitizer + .NET DTO; status is not a deferral, it's a recognition that the prior audit already raised this and the human owns sequencing.
- **F-0010 — python-authority** — `Backend/Services/Implementation/PositionEngine.cs` does FIFO lot accounting + realized PnL in .NET; not in registry. Borderline rule-5 case (persistence-layer math vs. canonical-Python rule). Needs registry classification.
- **F-0011 — python-authority** — `Backend/Services/Implementation/SnapshotService.cs::ComputeDrawdownSeries` is a third drawdown implementation (in addition to the Python canonical and the `BacktestService.cs` legacy). Live consumer (portfolio drawdown UI) likely.
- **F-0019 — timestamp** — `app/services/trade_comparison.py::_parse_ts` accepts 3 naive timestamp formats (`"%Y-%m-%d %H:%M"`, `"%Y-%m-%dT%H:%M:%S"`, `"%Y-%m-%d %H:%M:%S"`) and silently `.replace(tzinfo=UTC)`s them. Reads naive timestamps that prior audit flagged as wrong on the producing side.
- **F-0020 — timestamp — Phase 3 rollup** — Cross-stack grep returned 19 Python + 4 .NET + 45 TS candidate files. Per-file triage deferred to next tick. The list and triage order are pinned in the finding doc so the next tick can resume without re-running the grep.

### P2 (7 new)
- **F-0012** — 4 Backend transport-only services need explicit transport rows: `SanitizationService.cs`, `ResearchService.cs`, `SpecStrategyService.cs`, `PortfolioService.cs`.
- **F-0013** — `PortfolioValidationService.cs` runtime validation suite needs `engine-authority-map.md` § "Validation-only paths" classification.
- **F-0014** — `app/services/{data_quality_service,validation_service}.py` compute QC metrics and have report-tolerance thresholds (`_EXACT=0.001`, `_CLOSE=0.01`, `_OK=0.1`) baked in. Unregistered.
- **F-0015** — `app/research/features/{options_features,ta_features}.py` — feature engineering. Notable: `ta_features.py::compute_rsi_14` is a **third RSI consumer using pandas-ta**, not in the registry's RSI-row duplicates list.
- **F-0016** — `app/engine/strategy/algorithms/spy_strategy_{a,b,c}.py` — three RSI-range variants. Plus `_rsi_range_base.py` base class. None registered.
- **F-0017** — `app/research/divergence/strategies/{s1,s2,s3}_*.py` — vectorized parallels of engine canonicals; per-file docstrings explicitly acknowledge "mirrors the rule-set of...". Need disposition (legacy-ok or divergence-research-only).
- **F-0018** — Migration plan says Phase 2.3 SHIPPED 2026-04-27 (commit `334d419`); `math-sources-of-truth.md` § "Known rule-5 non-compliance" item 3 still describes the partial state. Drift between two governance docs.

## Findings updated (last_seen bumped)
_(F-0001..F-0008 unchanged this tick — Phase 1 backbone of those findings was not re-evaluated, just expanded with new sibling findings.)_

## Findings closed
_(none — read-only audit; no closures possible from the loop.)_

## Skipped / deferred to next tick (Phase 3 per-file triage — top of next-tick agenda)

The F-0020 rollup pins **68 candidate files** for per-file triage. Triage order recommended in F-0020:

1. **Triage the 4 .NET files** (small set — `MarketDataService.cs`, `StudiesApi.cs`, `ResearchService.cs`, `Query.cs`).
2. **Triage the 4 ingestion-path Python files** — `polygon_client.py`, `polygon_ingest.py`, `dataset_service.py`, `data_quality_service.py`.
3. **Triage the 9 wire-crossing TS services** — `replay-strategy.service.ts` (already known per prior audit), `replay-indicator.service.ts`, `replay-engine.service.ts`, `past-chain.service.ts`, `engine-history.component.ts`, `edge-api.service.ts`, `data-lab.component.ts`, `date-validation.ts`, `market-data.factory.ts`.
4. **Mass-classify the remaining ~40 TS files** as display-only / test / candidate via grep + sample inspection.

Also still pending: **Phase 2 (Python math-authority sweep)** — rule-5 violations beyond the two already opened (F-0010, F-0011). The registry's § "Known rule-5 non-compliance" enumerates 5 cases; F-0018 closed item 3 (it's drift, not a violation). Items 1, 2, 4, 5 still need verification:

- Item 1: BacktestService.cs (Phase 3.2 deferred per migration plan) — registry/migration-plan in agreement.
- Item 2: black-scholes.ts legacy-ok — verify the two intentional callers are still the only callers.
- Item 4: rule_based_backtest.py — Phase 4 deferred per migration plan.
- Item 5: hardcoded `r = 0.043` in 4 places — verify.

## Notes for the human

- **You said you're going to bed.** Wake to: 8 + 12 = **20 findings total** (1 awaiting-human, 19 open). Run summaries at `runs/2026-05-05.md` (tick 1) and `runs/2026-05-05-tick2.md` (tick 2). State at `state.json` is `mode: baseline-in-progress`, `phase: 2`.
- **The session-bound cron** (job ID `a85c47fb`, scheduled 23:03 nightly) **will not fire if you closed Claude Code.** If you want overnight ticks while you sleep, you'd need Windows Task Scheduler (offered earlier — say the word and I'll set it up next session).
- **The 20 findings are uncommitted on master.** If you want them durable in git, commit before any branch switch / repo wipe.
- **Top of next-tick agenda:**
  1. Phase 2 — verify the 4 remaining items in registry's "Known rule-5 non-compliance" (items 1, 2, 4, 5 — item 3 already closed via F-0018).
  2. Phase 3 — per-file triage of the 68 ban-list candidates per F-0020's recommended order.
  3. Phase 4 — first pass at provenance blocks on canonical files (the 4-field block per `learn-ai-validation` skill).
- **One pattern-level observation worth your time:** the highest-value Phase 1 findings are **drift findings** (F-0003, F-0006, F-0018) and **subtree-coverage findings** (F-0001, F-0002, F-0007, F-0008). The drift findings are 1-line edits that close gaps immediately; the subtree findings need classification per file, which is real work but mechanically scoped.
- **Pattern-level observation #2:** Several findings rhyme with the prior `computational-fidelity-2026-04-22.md` audit. The math-rigor work has clear precedent — the prior audit found things, they were never closed out, and they're surfacing again. Useful to read F-0009 and F-0020 with the prior audit doc open.
