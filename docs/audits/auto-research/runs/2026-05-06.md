# Auto-research run — 2026-05-06

**Mode:** remediation-in-progress (post-baseline)
**Started:** 2026-05-06T00:00:00-05:00 (resumed from prior session)
**Ended:** 2026-05-06T12:00:00-05:00 (estimated)
**Stop reason:** in-progress (F-0023 awaiting human decision; F-0026/F-0030/F-0032/F-0034 deferred long-term)
**Phases touched:** 3 (timestamp), 4 (provenance)
**Cursor at end:** All autonomous-closeable remediation complete.

## Findings opened
(none this run)

## Findings updated (last_seen bumped)
(all previously opened; statuses updated to fixed-verified)

## Findings closed
- F-0009 — P1 — timestamp — sanitizer.py: int64 ms wire format + fail-fast dedup (coordinated with F-0021)
- F-0019 — P1 — timestamp — trade_comparison.py: fail-fast on naive tz-less timestamp strings
- F-0021 — P0 — timestamp — .NET ingestion: AggregateData.Timestamp string→long; DateTimeOffset.FromUnixTimeMilliseconds; StudiesApi strict ParseExact
- F-0022 — P1 — timestamp — .NET query params: DateOnly.ParseExact for all fromDate/toDate in Query.cs, MarketDataService.cs, ResearchService.cs
- F-0024 — P1 — timestamp — Python additional ban-list: dataset_service, polygon_client, polygon_ingest
- F-0027 — P1 — provenance — 4-field block now present on every canonical math file in the registry (100% coverage across indicators, services, volatility, research/options, research/signal, research/validation, engine/edge — 70 files total across Stages E–I)
- F-0033 — P1 — timestamp — Python non-ingestion ban-list: 13 files across services/research/engine/routers/volatility subtrees

## Skipped
- F-0023 (P0 — forward-fill) — awaiting human decision: choose (A) flip forward_fill default to False (strict, breaking) or (B) add fail_on_gaps opt-in (backward-compatible). No code changes until user decides.
- F-0026 (P1 — fixture coverage) — multi-week effort; not actionable autonomously.
- F-0030 (P2 — reference notes) — deferred; partially addressed via provenance blocks.
- F-0032 (P1 — decimal→double narrowing) — deferred; wire-format DTO change required.
- F-0034 (P1 — frontend TS naive date parse) — deferred; frontend touchpoint changes required.

## Notes for the human

**F-0023 decision still required (P0):** `app/services/dataset_service.py` has `forward_fill: bool = True` as default. Per `.claude/rules/numerical-rigor.md`, forward-filling to align mismatched timestamp series is banned. You must choose:

- **Option A (strict):** Change default to `False`. Breaking change — any caller not explicitly passing `forward_fill=True` will no longer get gap-filling. Charts/indicators will see real data gaps. Correct per the rules.
- **Option B (opt-in strict):** Keep `True` as default, add `fail_on_gaps: bool = False` parameter. Non-breaking. Allows callers that care about correctness to opt in. Requires adding the parameter and wiring it in.

The auto-research loop will not touch this file until you indicate which option to apply.

**Provenance block coverage is now 100%.** The §6 hardening gate requirement for provenance blocks is satisfied. What remains before the gate is fully clear:
1. F-0023 decision (P0)
2. F-0026 golden fixture coverage (P1 — multi-week)
3. F-0030 reference notes (P2 — low urgency)
4. F-0032 decimal→double wire narrowing (P1 — coordinate with wire-format changes)
5. F-0034 frontend TS naive date parse (P1 — frontend changes)
6. F-0011 parity test for SnapshotService stats (awaiting-human — needs human to write the parity test)
