# Auto-research run — 2026-05-05

**Mode:** baseline-in-progress
**Started:** 2026-05-05T23:34:32-05:00
**Ended:** 2026-05-05T23:34:32-05:00 (interactive, single-tick)
**Stop reason:** manual-interrupt (interactive single-tick; cron not yet scheduled)
**Phases touched:** [1]
**Cursor at end:** phase-1-continuation — see `state.json`

## Findings opened

- **F-0001 — P1 — inventory** — `app/engine/edge/` subtree (~25 files) entirely unregistered. Engine-authority-map declares it canonical for "Volatility / edge research"; registry has zero rows.
- **F-0002 — P1 — inventory** — `app/research/signal/` subtree unregistered. Engine-authority-map declares it canonical for "Research signal scoring (IC, walk-forward, diagnostics)".
- **F-0003 — P1 — inventory** — `engine-authority-map.md:27` cites `app/research/options/bs_solver.py` as a canonical IV solver. File does not exist on disk; registry doesn't reference it either. Map ↔ code drift.
- **F-0004 — P1 — inventory** — `app/services/strategy_engine.py::AnalyzeOptionsStrategy` is canonical per map (line 22) for "Options strategy analysis (payoff, POP, Greeks)". No registry row. Outputs render to Strategy Lab UI — Phase 8 wire fidelity will trip.
- **F-0005 — P1 — inventory** — `app/engine/options/pricer.py` is the in-engine options pricing dispatcher (`PricingMode.QUANTLIB_ONLY` / `MARKET_PREFERRED` / `MARKET_REQUIRED`). No registry row. Dispatcher *is* a math-authority surface.
- **F-0006 — P1 — inventory** — Registry rows for Sharpe ratio, max drawdown, and "Bar consolidation, event replay, fill models" point at directory `PythonDataService/app/engine/` instead of specific files. Actual canonicals are `app/engine/results/statistics.py`, `app/engine/engine.py`, `app/engine/consolidators/trade_bar_consolidator.py`, `app/engine/execution/{fill_model,intrabar_resolver,portfolio}.py`.
- **F-0007 — P1 — inventory** — `app/volatility/` has 14 files; only `solver.py` has a registry row (and `fitting.py` is map-cited but registry-absent). Twelve files unregistered, including `vix_replication.py`, `surface.py`, `basis.py`, `iv30_health.py`, `models.py`, `analytics.py`, `price_normalization.py`.
- **F-0008 — P1 — inventory** — `app/research/validation/` (`ic.py`, `quantile.py`, `robustness.py`) absent from both registry and authority map. IC and bootstrap statistics need provenance.

## Findings updated (last_seen bumped)

_(first run — no prior findings)_

## Findings closed

_(none)_

## Skipped / deferred to next tick

- **Backend secondary services inventory** — `PortfolioService.cs`, `PositionEngine.cs`, `PortfolioValidationService.cs`, `SanitizationService.cs`, `SnapshotService.cs`, `ResearchService.cs`, `SpecStrategyService.cs` (most likely transport / persistence; need confirmation).
- **PythonDataService secondary services inventory** — `data_quality_service.py`, `validation_service.py`, `trade_comparison.py`, `sanitizer.py`, `dividend_service.py`, `rate_dividend_service.py`, `iv_recorder.py`, `options_companion_service.py`, `reference_companion_service.py`, `chart_service.py`, `dataset_service.py`, `market_monitor.py`.
- **Parallel strategy implementations** — `app/research/divergence/strategies/{s1_ema_crossover, s2_rsi_mean_reversion, s3_sma_crossover}.py` vs `app/engine/strategy/algorithms/*.py` registered canonicals — duplicates or research-only validators? Need decision documented.
- **Unregistered engine-canonical strategies** — `app/engine/strategy/algorithms/spy_strategy_{a,b,c}.py` not in registry.
- **Feature engineering** — `app/research/features/{options_features,ta_features}.py`.
- **Migration-plan vs registry drift** — `numerical-authority-migration-plan.md` claims phases shipped; verify each shipped row updated registry status accordingly.

## Notes for the human

- This was an **interactive single-tick** run, not an overnight cron run. Resume by re-invoking `/auto-research-tick`; state.json carries the cursor.
- The eight P1 findings cluster around **inventory drift between `docs/architecture/engine-authority-map.md`, `docs/math-sources-of-truth.md`, and code reality**. The map cites a file that doesn't exist (F-0003); the registry uses directory-only paths where pinpoint paths are needed (F-0006); both docs miss entire subtrees of canonical math (F-0001, F-0002, F-0007, F-0008); and one critical user-facing engine has no row (F-0004).
- **Recommended remediation order** (per the dependency-ordering rule): fix F-0003 (1-line drift) and F-0006 (tighten paths) first since they are pure registry edits. Then F-0001/F-0002/F-0007/F-0008 (subtree coverage) which require classification of what each file does. Then F-0004/F-0005 which need the most domain judgment.
- **The volatility subtree (F-0007) is the highest-stakes item** — `vix_replication.py` and `fitting.py` will need explicit external paper citations (Demeterfi-Derman-Kamal-Zou for VIX replication; SVI/SABR/whichever parameterization for surface fitting). These are the kind of math where unregistered provenance is an audit failure waiting to happen.
- Next tick should complete Phase 1 (the deferred items above), then Phase 2 (Python math-authority violations).
