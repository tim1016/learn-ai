# BS-002 — Black-Scholes European Put Price

**Status:** active · v1 (2026-05-08)
**Category:** options-pricing
**Canonical:** `PythonDataService/app/services/bs_greeks.py::bs_european_price`

## What this pins

The closed-form Black-Scholes-Merton put price `P = K·e^(-rT)·N(-d2) - S·e^(-qT)·N(-d1)` as implemented in `bs_european_price`, validated against py_vollib==1.0.1 across a 180-case input grid.

## Oracle

`py_vollib.black_scholes.black_scholes(flag='p', S, K, t, r, sigma)` — GPL-licensed external library, generation-only.

## Agreement

Max observed abs error: < 1e-14.  
Pinned tolerance: `atol=1e-10, rtol=0.0`.

## Input Grid

Same 180-case grid as BS-001 with `is_call=False`.

| Parameter | Values |
|-----------|--------|
| spot | 80, 90, 100, 110, 120 |
| strike | 90, 100, 110 |
| ttm_years | 1/12, 0.25, 0.5, 1.0 |
| rate | 0.05 |
| volatility | 0.15, 0.20, 0.30 |
| dividend | 0.0 |
| is_call | False |

## Additional Checks in Test Suite

- Put lower bound: `P >= max(K·e^(-rT) - S, 0)` for all cases.

## Regeneration

```
python scripts/generate_fixtures.py --id BS-002 --force \
  --justification "<reason>"
```
