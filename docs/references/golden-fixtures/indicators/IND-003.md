# IND-003 — Relative Strength Index (period=3, Wilder's smoothing)

**Status:** active · v1 (2026-05-09)
**Category:** indicators
**Canonical:** `PythonDataService/app/engine/indicators/rsi.py::RelativeStrengthIndex`

## What this pins

RSI values from `RelativeStrengthIndex` (period=3, `MovingAverageType.Wilders`), validated against an independent pure-Python Wilder RSI formula across 3 synthetic 8-bar price series.

## Oracle

hand_computed — pure-Python Wilder RSI formula without calling `RelativeStrengthIndex`. Seeding and smoothing written from first principles.

## Formula

```
gain[i] = max(0, price[i] - price[i-1])
loss[i] = max(0, price[i-1] - price[i])

Seed (after period=3 deltas collected):
  avg_gain = mean(gain[0..period-1])
  avg_loss = mean(loss[0..period-1])

Wilder smoothing (post-seed):
  avg_gain = (avg_gain * (period - 1) + gain) / period
  avg_loss = (avg_loss * (period - 1) + loss) / period

RSI = 100 - 100 / (1 + avg_gain / avg_loss)
Edge: if round(avg_loss, 10) == 0 → RSI = 100
```

## Reference

LEAN `Indicators/RelativeStrengthIndex.cs` with `MovingAverageType.Wilders` (vendored commit `7986ed0aade3ae5de06121682409f05984e32ff7`).

Wilder, J. (1978). *New Concepts in Technical Trading Systems*. Trend Research.

## Critical Seeding Convention (D-009)

The first `avg_gain` / `avg_loss` is the **simple mean** of the first `period` price deltas — not Wilder's smoothing of `period + 1` deltas. This matches LEAN's seeding. Some textbooks differ; this is documented as a known deviation in the canonical module docstring.

## Hand-Verification (Case B, first 5 bars: [50.0, 52.0, 50.0, 54.0, 52.0])

| bar | gain | loss |
|-----|------|------|
| 0→1 | 2.0 | 0.0 |
| 1→2 | 0.0 | 2.0 |
| 2→3 | 4.0 | 0.0 |

Seed: avg_gain = 2.0, avg_loss = 0.6667
RSI at bar 3 ≈ 75.000000

## NaN Convention

- `v0` (first sample — no delta available): `None`
- `v1`, `v2` (accumulating seed deltas): `None`
- `v3` onwards: float RSI value

First non-`None` value appears at bar index 3 (sample 4, is_ready at samples ≥ 4).

## Agreement

Pinned tolerance: `atol=1e-9, rtol=0.0`.

## Additional Checks in Test Suite

- First 3 bars (`v0`, `v1`, `v2`) are `None` for all cases.
- RSI is in [0, 100] for all non-`None` values.
- All-gain case: RSI = 100.

## Regeneration

```
python scripts/generate_fixtures.py --id IND-003 --force \
  --justification "<reason>"
```
