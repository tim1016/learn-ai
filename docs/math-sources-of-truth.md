# Math sources of truth

One row per mathematical concept in learn-ai. Names **the** canonical implementation, calls out every known duplicate, and records what test proves the canonical is right.

Paired with `.claude/skills/learn-ai-validation/SKILL.md` (the Math Provenance Contract) and `.claude/rules/numerical-rigor.md` (the scientific standards). The **skill enforces the contract**; **this file is the lookup** that tells the contract which file to point at.

## How to read this

| Column | Meaning |
|---|---|
| **Concept** | The named math (e.g., "EMA", "Black-Scholes call price"). One row per concept; if the math is parameterized (e.g., EMA period), one row covers all parameterizations. |
| **Canonical** | The **one** file that is source-of-truth. Per root `CLAUDE.md` rule 5, canonical is Python unless explicitly justified otherwise. |
| **Legacy / duplicates** | Every other file in the repo that implements the same math. Each must either (a) call canonical at runtime or (b) carry a parity test naming the canonical file. |
| **Reference** | The external source the canonical was ported from. `references/` path, paper citation, or authoritative URL. |
| **Validated against** | The test that proves canonical ↔ reference equivalence. If no fixture exists yet, write `NONE — pending`. |
| **Status** | `canonical` · `legacy-ok` (duplicate with parity test) · `pending-migration` (rule-5 violation) · `pending-fixture` (canonical but no equivalence proof) · `external-unvalidated` (we trust the vendor) |

## Registry

### Indicators — Python-canonical, ported from LEAN

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| SMA | `PythonDataService/app/engine/indicators/sma.py` | `PythonDataService/app/services/ta_service.py` (pandas-ta passthrough, used by `/api/indicators/calculate`) | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/Indicators/SimpleMovingAverage.cs` | `PythonDataService/tests/test_indicator_parity.py`; `tests/fixtures/test_indicator_fixtures.py::TestIND002SMA` (IND-002, hand_computed oracle, period=3, atol=1e-9) — see [IND-002](docs/references/golden-fixtures/indicators/IND-002.md) | canonical — provenance block pending on sma.py |
| EMA | `PythonDataService/app/engine/indicators/ema.py` | `ta_service.py` (pandas-ta path) | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/Indicators/ExponentialMovingAverage.cs` | `PythonDataService/tests/test_indicator_parity.py`; `tests/fixtures/test_indicator_fixtures.py::TestIND001EMA` (IND-001, hand_computed oracle, period=3, k=0.5 SMA-seeded, atol=1e-9) — see [IND-001](docs/references/golden-fixtures/indicators/IND-001.md) | canonical — existing docstring cites LEAN; needs 4-field conversion on next touch |
| RSI (Wilders) | `PythonDataService/app/engine/indicators/rsi.py` | `ta_service.py` (pandas-ta path); `app/research/features/ta_features.py::compute_rsi_14` (third pandas-ta consumer in research feature pipeline) | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/Indicators/RelativeStrengthIndex.cs` with `MovingAverageType.Wilders` | `PythonDataService/tests/test_indicator_parity.py`; `tests/fixtures/test_indicator_fixtures.py::TestIND003RSI` (IND-003, hand_computed Wilder's-smoothing oracle, period=3, atol=1e-9) — see [IND-003](docs/references/golden-fixtures/indicators/IND-003.md) | canonical — existing docstring is already close to the 4-field format |
| MACD, Bollinger Bands | `PythonDataService/app/services/ta_service.py` (pandas-ta) | — | pandas-ta (external) | `PythonDataService/tests/test_indicators.py` | external-unvalidated — no LEAN parity yet; flag if strategy depends on exact match |

### Options pricing and Greeks

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Black-Scholes price (European call/put) | `PythonDataService/app/services/bs_greeks.py::bs_european_price` (closed-form, canonical) AND `PythonDataService/app/services/quantlib_pricer.py::price_option` (QuantLib analytic_bs engine, second canonical, parity-pinned) | `Frontend/src/app/utils/black-scholes.ts` — **legacy-ok** for two intentional callers (`pricing-lab.component.ts` is a comparison harness; `strategy-builder.component.ts` is interactive UI, decision pending). Header forbids new callers. | Hull, Options Futures and Other Derivatives (10e), §15.8; QuantLib C++ reference | `PythonDataService/tests/services/test_bs_cross_engine_parity.py` — 360-case grid at `atol=1e-10` between `bs_greeks` and `quantlib_pricer.analytic_bs` (**Phase 1.4 shipped 2026-04-26**, precision-leak fix `69d2bfe`) | **canonical** — two-engine parity established |
| Greeks — Delta, Gamma, Theta, Vega, Rho | `PythonDataService/app/services/bs_greeks.py::black_scholes_greeks` (closed-form, canonical) AND `PythonDataService/app/services/quantlib_pricer.py` (QuantLib path) | `Frontend/src/app/utils/black-scholes.ts::bsDelta/bsGamma/bsTheta/bsVega/bsRho` — **legacy-ok** for the two intentional callers above. | Hull §19 (Greek Letters); QuantLib | `Frontend/src/app/utils/black-scholes.spec.ts` (TS self-consistency only); `tests/fixtures/test_options_pricing_fixtures_extended.py` — 180-case py_vollib==1.0.1 oracle: [BS-004](docs/references/golden-fixtures/options-pricing/BS-004.md) (Gamma, atol=1e-10), [BS-005](docs/references/golden-fixtures/options-pricing/BS-005.md) (Theta, atol=1e-10), [BS-006](docs/references/golden-fixtures/options-pricing/BS-006.md) (Vega, atol=1e-10), [BS-007](docs/references/golden-fixtures/options-pricing/BS-007.md) (Rho, atol=1e-10) | **canonical** — all five Greeks pinned against py_vollib==1.0.1 oracle (Phase 1 golden fixtures, 2026-05-08) |
| Normal CDF / PDF | `PythonDataService/app/services/quantlib_pricer.py` (QuantLib internal); `scipy.stats.norm` inside `bs_greeks.py` | `Frontend/src/app/utils/black-scholes.ts::normCdf` (Abramowitz & Stegun 7.1.26, \|error\| < 1.5e-7) — **legacy-ok**, supporting the two intentional black-scholes.ts callers | A&S (1964) 7.1.26 | `NONE — pending` (the TS A&S approximation is exercised transitively by black-scholes.spec.ts) | legacy-ok — TS path remains for the two intentional UI callers; not a math authority |
| Implied volatility (root-finding) | `PythonDataService/app/volatility/solver.py::implied_volatility` (Newton + Brent fallback with Brenner-Subrahmanyam seed) | `app/services/quantlib_pricer.py::implied_volatility` (QuantLib bisection — companion path for callers that want the QuantLib pricer in the loop) | Hull §19.11; Brent's method (scipy.optimize) | `NONE — pending` cross-engine parity between `volatility/solver.py` and the QuantLib companion | pending-fixture — two solvers exist but are not parity-pinned |
| IV term-structure interpolation (30-day constant-maturity) | `PythonDataService/app/research/options/iv_builder.py` | — | `docs/math-rigor.md` Upgrade 1 (variance-time interpolation, industry standard) | `NONE — pending` | pending-fixture — currently uses linear-in-σ (known bias per math-rigor.md); variance interpolation is scheduled |
| Risk-free rate | `PythonDataService/app/services/fred_service.py` (FRED-backed primary, `FALLBACK_RATE = 0.043` if unreachable). Hardcoded constants also live in `app/research/options/iv_builder.py::DEFAULT_RISK_FREE_RATE`, `app/research/options/contract_finder.py::DEFAULT_RFR`, and Pydantic field defaults in `app/models/{strategy,portfolio}.py` | — | `docs/math-rigor.md` Upgrade 4 proposes full FRED interpolation across DTB3/DTB4WK/DTB6/DTB1YR | n/a (constant fallback) | partially-shipped — `fred_service.py` exists; the four hardcoded `0.043` constants in models/research-options should resolve via fred_service rather than literal defaults |
| Options strategy analysis (payoff, POP, current-time PnL curve, Greek curves, per-leg diagnostics) | `PythonDataService/app/services/strategy_engine.py::AnalyzeOptionsStrategy` (+ `app/routers/strategy.py`) | — (Frontend `OptionsStrategyLabComponent` is a passthrough since `451394d`) | Hull §11–12 (payoff diagrams), §19 (Greeks); composes `bs_greeks.py` + `quantlib_pricer.py` + `volatility/solver.py` | `PythonDataService/tests/test_strategy_engine.py` | canonical |
| In-engine option pricing dispatch (QuantLib-only / market-preferred / market-required) | `PythonDataService/app/engine/options/pricer.py` (`PricingMode.QUANTLIB_ONLY` \| `MARKET_PREFERRED` \| `MARKET_REQUIRED`) | — | Composes `bs_greeks.py` + `quantlib_pricer.py`; market path uses observed mid where present | `NONE — pending` (existing engine tests under `app/engine/tests/` exercise it indirectly) | canonical |

### Backtesting engine and statistics

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Bar consolidation, event replay, fill models | `PythonDataService/app/engine/engine.py` (orchestration), `app/engine/consolidators/trade_bar_consolidator.py` (bar consolidation), `app/engine/execution/{fill_model,intrabar_resolver,portfolio,order,execution_config}.py` (execution + accounting) | `Backend/Services/Implementation/BacktestService.cs` — **runs its own `RunSmaCrossover`, `RunRsiMeanReversion`, `RunMomentumRsiStochastic`, `RunRsiReversal` in-process** (scheduled for removal in **Phase 3.2** per migration plan) | LEAN Engine, `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/` (vendored extract); LEAN stale-signal market-fill receipt in `docs/references/fill-model-parity-spike-2026-05-19.md` | `PythonDataService/tests/test_strategy_engine.py`, `tests/test_rule_based_backtest_validation.py`, `PythonDataService/tests/engine/test_fill_model.py`, `PythonDataService/tests/engine/test_engine_fill_modes.py`, `docs/validation/*.pdf` reports | **pending-migration** — rule-5 violation; cutover sequenced in `docs/architecture/numerical-authority-migration-plan.md` Phase 3 |
| Configurable rule-based strategy runner | `PythonDataService/app/engine/strategy/spec/` — **Phase 1 shipped 2026-05-04** (schema + parity-pinned ports of SPY EMA / SMA / RSI). **Phase 2.1 shipped 2026-05-04** (manage layer: ADX/MACD/Supertrend wired, `PnLPercent`/`PnLPoints` primitives, `survival.CLOSE_ALL` action, behavior tests for stop-loss / profit-target / first-match-wins / no-fire-while-flat). Still equity-only and single-symbol. `app/services/rule_based_backtest.py` remains the predecessor (kept until Phase 4 cutover). | `PythonDataService/app/services/rule_based_backtest.py` — same intent, weaker schema, parallel engine. Phase 4 of the numerical-authority migration converts `runRuleBasedBacktest` to a passthrough that translates legacy params to a `StrategySpec`, then deletes `rule_based_backtest.py`. | Internal (no external port reference; behavior validated by parity tests against hand-coded LEAN-pinned twins + behavior tests for new manage primitives) | `app/engine/strategy/spec/tests/test_spec_*_parity.py` (3 strategies) + `test_spec_manage_rules.py` (manage behavior); `tests/test_rule_based_backtest_validation.py` (legacy) | **pending-design** for Phase 4 cutover; spec layer is parity-pinned secondary today |
| Deployment-validation consecutive green minute strategy | `PythonDataService/app/engine/strategy/algorithms/deployment_validation.py` | LEAN sidecar companion template: `PythonDataService/app/lean_sidecar/trusted_samples/deployment_validation.py`; deployment audit copy: `references/qc-shadow/DeploymentValidationAlgorithm.py`; deployment settings fixture: `PythonDataService/app/engine/strategy/spec/fixtures/deployment_validation.spec.json` | Internal session spec, captured in `docs/references/deployment-validation-consecutive-green.md` | `PythonDataService/tests/engine/test_deployment_validation_strategy.py`; `PythonDataService/tests/engine/live/test_deployment_validation_deploy_artifacts.py`; `PythonDataService/tests/lean_sidecar/test_deployment_validation_template.py`; `PythonDataService/tests/services/test_lean_sidecar_template_registry.py` | canonical — internal validation primitive, no alpha/performance claim |
| Max drawdown | `PythonDataService/app/engine/results/statistics.py::_max_drawdown` | `Backend/Services/Implementation/BacktestService.cs::CalculateMaxDrawdown` (scheduled for removal in **Phase 3.2**); `Backend/Services/Implementation/SnapshotService.cs::ComputeDrawdownSeries` (live portfolio path — also pending migration to Python; tracked in finding F-0011) | Bacon, Practical Portfolio Performance Measurement (2e), §8.2 | `Backend.Tests/Unit/Services/BacktestServiceTests.cs` (service-level, no parity fixture); `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG002MaxDrawdown` (ENG-002, numpy `maximum.accumulate` oracle, 3 equity-curve cases + bounds checks, atol=1e-9) — see [ENG-002](docs/references/golden-fixtures/engine-statistics/ENG-002.md) | pending-migration (.NET removal); Python canonical **has golden fixture** (ENG-002, 2026-05-08) |
| Sharpe ratio | `PythonDataService/app/engine/results/statistics.py::_sharpe` (`TRADING_DAYS_PER_YEAR = 252` annualization) | `Backend/Services/Implementation/BacktestService.cs::CalculateSharpeRatio` (scheduled for removal in **Phase 3.2**); `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` (live portfolio Sharpe over snapshot equity curve — also computes Sortino, Calmar, max drawdown locally; **legacy-ok-pending-parity** per finding F-0011, closed 2026-05-06; needs parity test against Python canonical) | Sharpe (1994), *The Sharpe Ratio*, Journal of Portfolio Management | `Backend.Tests/Unit/Services/BacktestServiceTests.cs`; `SnapshotServiceTests.cs`; `tests/fixtures/test_engine_stats_fixtures.py::TestENG001Sharpe` (ENG-001, hand_computed oracle, atol=1e-9) | pending-migration (BacktestService); legacy-ok-pending-parity (SnapshotService); Python canonical **has golden fixture** (ENG-001) |
| Sortino ratio | `PythonDataService/app/engine/results/statistics.py::_sortino` | `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` (**legacy-ok-pending-parity** per F-0011) | Sortino, Miller, Schwartz (1991), *Managing Investment Risk* | `tests/fixtures/test_engine_stats_fixtures.py::TestENG001bSortino` (ENG-001b, degenerate hand_computed oracle, atol=1e-9) | pending-migration (.NET parity); Python canonical **has golden fixture** (ENG-001b) |
| Trade statistics (win rate, profit factor, payoff ratio, expectancy) | `PythonDataService/app/engine/results/statistics.py::compute_trade_statistics` | — | Pardo, *The Evaluation and Optimization of Trading Strategies* (2e), §4 (standard performance ratios) | `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG003TradeStats` (ENG-003, hand_computed oracle — 4-trade cases × 3 rows, atol=1e-9) — see [ENG-003](docs/references/golden-fixtures/engine-statistics/ENG-003.md) | **canonical** — golden fixture pinned (ENG-003, 2026-05-08) |
| CAGR (Compound Annual Growth Rate) | `PythonDataService/app/engine/results/statistics.py::compute_portfolio_statistics` | `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` (**legacy-ok-pending-parity** per F-0011) | Standard CAGR formula: `(final/initial)^(252/trading_days) − 1`; Bacon, Practical Portfolio Performance Measurement (2e), §4.2 | `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG004CAGR` (ENG-004, hand_computed oracle, 3 cases, atol=1e-6) — see [ENG-004](docs/references/golden-fixtures/engine-statistics/ENG-004.md) | **canonical** — golden fixture pinned (ENG-004, 2026-05-08) |
| Calmar ratio | `PythonDataService/app/engine/results/statistics.py::compute_portfolio_statistics` | `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` (**legacy-ok-pending-parity** per F-0011) | Bacon, Practical Portfolio Performance Measurement (2e), §8.3 (CAGR / max_drawdown); composed from ENG-004 + ENG-002 paths | `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG005Calmar` (ENG-005, hand_computed oracle, 3 cases, atol=1e-6; includes hand-algebraic consistency check: Calmar = CAGR / MDD) — see [ENG-005](docs/references/golden-fixtures/engine-statistics/ENG-005.md) | **canonical** — golden fixture pinned (ENG-005, 2026-05-08) |
| Replay determinism (same input → same output) | `PythonDataService/app/engine/` | — | Internal invariant | `Backend.Tests/Unit/Services/ReplayDeterminismTests.cs` | canonical-supporting — this test belongs in Python once engine migration completes |
| Run ledger / reproducibility hashing (canonical-JSON SHA-256 over spec + result; `data_snapshot_id`) | `PythonDataService/app/research/runs/hashing.py` (canonical_json + hash_payload), `app/research/runs/ledger.py` (RunLedger schema + identity columns), `app/research/runs/runner.py` (orchestration + hash assembly), `app/research/runs/storage.py` (atomic file-backed persistence), `app/routers/research_runs.py` (HTTP boundary) — **Phase A shipped 2026-05-06** (Build Alpha-style features 1-8 research pipeline). Equity-only, synchronous, file-backed under `PythonDataService/artifacts/runs/<run_id>/`; the same runner is the substrate for Phase B-E (workbench, walk-forward, Monte Carlo, robustness, baselines, sensitivity). | — | RFC 8785 (JSON Canonicalization Scheme — relaxed contract; see module docstring); SHA-256 (FIPS 180-4); `docs/references/run-ledger.md` for the `data_snapshot_id` rationale and Postgres-index upgrade path | `PythonDataService/tests/research/runs/test_hashing.py` (key-order independence, non-ASCII stability, snapshot-id formatting), `test_runner_inmemory.py` (replay determinism, parameter / window propagation, failure-path hash determinism), `test_storage.py` (round-trip, filter, atomic write), `test_endpoint.py` (HTTP-boundary hash determinism, int64-ms wire format), `test_ema_acceptance.py` (canonical SPY fixture end-to-end gate) | canonical |
| Walk-forward analysis (chronological / rolling / anchored splits over a fixed `StrategySpec`; compounded combined OOS curve; alpha-decay slope) | `PythonDataService/app/research/walk_forward/splits.py` (three split policies + factory), `app/research/walk_forward/runner.py` (fold orchestration + aggregation), `app/research/walk_forward/result.py` (DTOs), `app/research/walk_forward/storage.py` (atomic file-backed persistence), `app/routers/walk_forward.py` (HTTP boundary) — **Phase C shipped 2026-05-06**. Milestone 4A only: fixed spec across folds; train side recorded but not executed. Phase 4B (parameter selection on train, frozen on test) deferred behind Feature 8 (sensitivity sweeps). | — | López de Prado, *Advances in Financial Machine Learning* (2018), §7 — verify on next touch (citation is approximate; the specific split-policy taxonomy chronological/rolling/anchored is repository-internal). See `docs/references/walk-forward.md` for the compounded-vs-rebased combined-curve decision and aggregation formulas. | `PythonDataService/tests/research/walk_forward/test_splits.py` (policy correctness, fold-window invariants, factory dispatch), `test_runner.py` (orchestration, fold persistence, aggregation, failure paths), `test_storage.py` (round-trip, filter, atomic write, path-traversal defense), `test_endpoint.py` (HTTP boundary, request validation, route-clash with `/{run_id}`) | canonical |
| Monte Carlo trade-path simulation (reshuffle / resample over a parent run's trade list, equity bands, drawdown / streak / terminal-PnL quantiles, breach probabilities) | `PythonDataService/app/research/monte_carlo/methods.py` (reshuffle/resample primitives, equity_curve, max_drawdown, max_losing_streak), `app/research/monte_carlo/runner.py` (load parent run + simulate + aggregate), `app/research/monte_carlo/result.py` (DTOs), `app/research/monte_carlo/storage.py` (atomic file-backed persistence), `app/routers/monte_carlo.py` (HTTP boundary) — **Phase D shipped 2026-05-06**. Equity-curve compounding matches `app/engine/results/statistics.py` so simulated curves are directly comparable to the parent run's reported metrics. | — | López de Prado, *Advances in Financial Machine Learning* (2018), §7 (block bootstrap discussion) — and standard non-parametric bootstrap (Efron, 1979). Verify both citations on next touch. The reshuffle / resample / forward-projection split is repository-internal; equity-band quantiles use `numpy.percentile` (linear interpolation, default). See `docs/references/monte-carlo.md` for method semantics, breach-probability formula, and the IID-returns assumption that constrains forward-projection horizons. | `PythonDataService/tests/research/monte_carlo/test_methods.py` (multiset preservation, sample-with-replacement, equity-curve compounding, max-drawdown formula, max-losing-streak), `test_runner.py` (orchestration, quantile invariants P5≤P50≤P95, determinism by seed, reshuffle terminal-equity invariance, failure paths, low-sim warnings), `test_storage.py` (round-trip, filter, atomic write, path-traversal defense), `test_endpoint.py` (HTTP boundary, request validation, route-clash with `/{run_id}`) | canonical |
| Null-baseline empirical percentile + small-sample p-value (parent-run metric ranked against a null distribution from buy-and-hold or random EMA window-pair runs) | `PythonDataService/app/research/baselines/generators.py` (buy_and_hold_spec via BarProperty tautology + never-firing exit; random_ema_window_specs sampling fast/slow from a bounded family), `app/research/baselines/runner.py` (parent → N child runs → null-distribution aggregation), `app/research/baselines/result.py` (DTOs), `app/research/baselines/storage.py` (atomic persistence), `app/routers/baselines.py` (HTTP boundary) — **Phase E1 shipped 2026-05-07**. Each baseline run is a normal Phase A `RunLedger` linked back via `parent_run_id`. | — | Phipson & Smyth (2010), *Permutation P-values should never be zero* — `(1 + count(null >= parent)) / (N + 1)`. The "EMA window family" baseline is repository-internal (no external citation; the architecture spec calls it out as one of the canonical Build Alpha null comparisons). See `docs/references/baselines.md` for method-by-method semantics, the BarProperty-tautology trick for buy-and-hold, and the documented engine-flush limitation that leaves `RunMetrics.total_trades = 0` for B&H (equity-derived metrics are still correct). | `PythonDataService/tests/research/baselines/test_generators.py` (B&H tautology shape, EMA-pair `slow > fast` invariant, parameter-range validation, unsatisfiable-constraint rejection), `test_runner.py` (B&H equity metrics, random-EMA generates N runs, child-run persistence + parent_run_id linkage, null-distribution coverage, percentile + p-value in unit interval, extreme-parent p-value lower bound `1/(N+1)`, failure paths, distinct baseline_ids per run), `test_storage.py` (round-trip, filter, atomic write, path-traversal defense), `test_endpoint.py` (HTTP boundary, request validation, route-clash with `/{run_id}`) | canonical |
| ML prediction-set artifact (precomputed predictions as a pinned data artifact: deterministic manifest hash, leakage invariants, bar-clock coverage with per-ref lookup-mode dispatch) | `PythonDataService/app/research/ml/artifact.py` (manifest schema, `compute_rows_hash`, `compute_prediction_set_hash` with chicken-and-egg field exclusion, parquet I/O), `app/research/ml/loader.py` (`PredictionSet.load` — manifest+rows-hash recompute, leakage check, duplicate-timestamp rejection, O(log n) `next_after` lookup), `app/research/ml/coverage.py` (`assert_bar_clock_coverage` — exact_bar_close + next_after_bar_close modes, mixed-mode specs), `app/research/ml/generators/quantconnect_fixture.py` (QC `qc_export.json` → canonical artifact, deterministic hash), `app/research/runs/ledger.py` (`RunLedger.prediction_set_hash` v1.1 schema field), `app/research/runs/runner.py` (load + assert_pairs_with + coverage at the run-pipeline boundary) — **v0.5 plumbing shipped 2026-04-23 (PR #208); QC tutorial parity Phase 1 shipped 2026-05-09 (PR #211–#215)**. | — | SHA-256 (FIPS 180-4) via `app/research/runs/hashing.py::hash_payload`; QC tutorial source pinned at `https://www.quantconnect.com/docs/v2/writing-algorithms/importing-data/streaming-data/precomputed-ml-predictions`; design at `docs/superpowers/specs/2026-05-09-ml-prediction-as-data-v05-design.md` + `docs/superpowers/specs/2026-05-10-quantconnect-precomputed-predictions-parity.md`; authority at `docs/ml-predictions-authority.md`. | `PythonDataService/tests/research/ml/test_artifact.py` (manifest round-trip, hash determinism, path-safe-id, chunk-rows hash sorting), `test_loader.py` (load + leakage + duplicate-ts + next_after edge cases), `test_coverage.py` (exact_bar_close + next_after_bar_close + mixed-mode + missing-field paths — 11 tests), `test_quantconnect_fixture_determinism.py` (re-import produces identical hash pinned at `b8252cfa9a749f5bf592602f3aebc2b3a4ccc6bb0cd41da48a6db7a581342e0e`), `test_quantconnect_fixture_parity.py` (per-row value parity at `atol=1e-9, rtol=0`), `test_quantconnect_fixture_runtime.py` (RunLedger.prediction_set_hash + result_hash pinned end-to-end), `test_e2e_replay.py` (regenerate produces same hash) | **canonical** — Phase 1 hash pinned, 148/148 ML+parity tests pass (2026-05-12) |
| IBKR equity-tier commission model (per-share with $1 floor and 0.5% trade-value cap) | `PythonDataService/app/research/parity/ibkr_commission.py::IbkrEquityCommissionModel` — single canonical implementation. Re-exported from `PythonDataService/app/engine/execution/commission.py` so `app.engine.*` can consume the model without a backwards dependency on `app.research.*`. Engine consumers (Task 12 / PR #340): `FillModel(fee_model=...)` at `app/engine/execution/fill_model.py`, `LeanSetHoldingsSizing(fee_model=...)` at `app/engine/execution/sizing.py`, wired into the cross-engine matrix runs by `app/lean_sidecar/cross_runner.py`. Reconciler-side use continues at `app/research/parity/qc_reconciler.py`. | — | QuantConnect docs for `BrokerageName.InteractiveBrokersBrokerage` + `AccountType.Margin` equity tier; canonical implementation in this file (in-file provenance block per `learn-ai-validation` skill). Formula: `min(max(abs(qty) * per_share, min_per_order), abs(qty) * fill_price * max_pct_of_value)` quantized to cents. | `PythonDataService/tests/research/parity/test_ibkr_commission.py` (per-share, floor at $1, cap at 0.5%, zero-quantity, zero-price edge cases, plus SPY/AAPL/TSLA representative cases — 11 cases); `PythonDataService/tests/engine/test_commission_reexport.py` (engine re-export is the canonical class, not a wrapper) | **canonical** — Phase 3 shipped 2026-05-11 (PR #218); engine-side wiring shipped 2026-05-22 (PR #340 — Task 12) |
| Fee-aware `SetHoldings` sizing (long-only equity, IBKR brokerage) | `PythonDataService/app/engine/execution/sizing.py::LeanSetHoldingsSizing` with optional `fee_model=IbkrEquityCommissionModel()`. When `fee_model` is set, solves the largest integer `qty` satisfying `qty*price + fee_model.fee(qty, price) <= portfolio_value*(1 - free_portfolio_value_pct)` via monotonic decrement from the naive floor. When `fee_model` is None, falls back to the legacy fixed-`order_fee` path that the 20-entry SPY golden fixture pins. Engine consumers: `app/lean_sidecar/cross_runner.py` (backtest matrix) and **`app/engine/execution/order_sizer.py::OrderSizer` (live path, ADR 0009 PR #486 — the `SetHoldings` percent path delegates here via the policy adapter; `SimpleFloorSizing` was retired from the live path in the same PR and is research/backtest-only)**. | `SimpleFloorSizing` for the `SetHoldings` percent path — retired from live execution 2026-06-13 (ADR 0009 PR #486). Still acceptable for research / quick-iteration backtests where exact LEAN parity is not required. | LEAN `QCAlgorithm.SetHoldings` → `IBuyingPowerModel.GetMaximumOrderQuantityForTargetBuyingPower` under `InteractiveBrokersBrokerage` + `AccountType.Margin`. See `docs/references/lean-set-holdings.md` for the math, the (deliberately narrow) port scope, and the 2026-06-13 live-path cutover note. | `PythonDataService/tests/engine/test_sizing.py` — 20-entry SPY golden fixture (`test_lean_sizing_reproduces_every_golden_entry`, atol=0) pins the legacy path; fee-aware tests pin the SPY parity case, the AAPL per-share-rate case, monotonicity-vs-no-fee, and the zero-budget edge. **Live-path regression**: `PythonDataService/tests/engine/execution/test_order_sizer.py::test_set_holdings_routes_through_lean_buffered_fee_aware_resolver` + `tests/engine/live/test_live_portfolio.py::test_set_holdings_with_set_holdings_policy_routes_through_lean` pin the 199 vs 200 share contrast that proves the live path now matches the canonical LEAN sizing rather than SimpleFloor. | **canonical** — engine-side fee-aware path shipped 2026-05-22 (PR #340 — Task 12); **live path consumes the canonical resolver 2026-06-13 (ADR 0009 PR #486)**. Multi-symbol fixture extraction (QQQ/AAPL/TSLA first-ENTER entries) deferred until those W6mo cells regenerate (blocked on engine cross-session exit-fill-mode gap, tracked separately). |
| QC trade-level parity reconciler (8-category divergence taxonomy, resolution-aware fixture audit, fill alignment with sequence disambiguation, round-trip P&L pairing with algebraically-propagated tolerance) | `PythonDataService/app/research/parity/qc_reconciler.py` — `reconcile_qc_aapl_phase3` public entry; `DivergenceCategory` `StrEnum` (FIXTURE_INSUFFICIENT / DECISION / DIRECTION / QUANTITY / FILL_PRICE / COMMISSION / PNL / ORDER_TYPE); `_parse_qc_orders` (ISO + numeric ms disambiguation, ambiguous-range rejection); `_audit_fixture` (daily=bar-open match, minute=`[low, high]` containment); `_pair_round_trips` (long-only single-position invariant; `realized_pnl = (exit-entry)*shares - entry_fee - exit_fee`); `_classify_pnl_divergences` (propagated atol = `(|entry_qty|+|exit_qty|)*per_share_pnl_atol + 2*commission_atol`). | — | Internal — design at `docs/superpowers/specs/2026-05-11-phase3-pnl-parity-design.md`; reconciliation report at `docs/references/reconciliations/qc-aapl-phase3.md`; taxonomy documented in `.claude/rules/numerical-rigor.md` → "Trade-level reconciliation taxonomy". | `PythonDataService/tests/research/parity/test_qc_reconciler.py` (canonical schema parsing, fixture audit for both resolutions, alignment with seq disambiguation, classification across all 8 divergence categories, round-trip P&L drift detection), `test_qc_aapl_phase3_trade_parity.py` (Phase 3.5 acceptance — 1 pinned aligned fill 2026-02-10 buy at $273.18 vs QC's $273.24, within propagated tolerance), `test_qc_fixture_smoke.py` (Branch-A/B fee-presence gate) | **canonical** — Phase 3.0 + 3.5 Path A shipped 2026-05-11/12 (PR #218–#223) |
| Reconciler-side CSV → TradeBar fixture reader (LEAN bar-semantics: daily anchors to 09:30/16:00 ET; minute literal NY-local; auto-detected from time-of-day component; Decimal prices) | `PythonDataService/app/research/parity/fixture_data_reader.py::FixtureDataReader` — `iter_bars`, `bar_open_by_date` (daily audit), `find_bar_containing(symbol, fill_time_ms)` (minute audit), `is_minute_resolution` (auto-detection); standalone CSV consumer, not a substitute for `LeanMinuteDataReader`. | — | LEAN daily-bar convention (time=09:30 ET, end_time=16:00 ET) and minute-bar convention (time = literal minute start NY-local). Export source is QC Cloud's `qb.history(Resolution.DAILY \| Resolution.MINUTE)`. | `PythonDataService/tests/research/parity/test_fixture_data_reader.py` (daily/minute auto-detection, `find_bar_containing` exact and edge boundaries, `bar_open_by_date` groupby correctness, tz attachment, symbol filter rejection) | **canonical** — Phase 3 shipped 2026-05-11 (PR #218) |
| Defer-only intraday-trigger fill mode (`NEXT_SESSION_OPEN`) + per-ref prediction lookup-mode dispatch (`PredictionRef.lookup`) | `PythonDataService/app/engine/strategy/spec/engine.py::FillMode.NEXT_SESSION_OPEN` (NY-trading-date eligibility; fills at first minute-bar open of the next trading session; `DEFERRED_FILL_MODES` set), `app/engine/strategy/spec/schema.py::PredictionRef.lookup` (`exact_bar_close` \| `next_after_bar_close` discriminator), `app/engine/strategy/spec/primitives.py::PredictionComparison` (per-ref lookup dispatch using `PredictionRef.lookup`), `app/research/ml/loader.py::PredictionSet.next_after` (O(log n) `bisect_right`), `app/research/ml/loader.py::PredictionLookupError` (runtime backstop for coverage-check escape). | — | Internal — design at `docs/superpowers/specs/2026-05-11-phase3-pnl-parity-design.md` (Path A intraday-trigger fill mode subsection) + Phase 3.5 implementation plan; rationale in `docs/ml-predictions-authority.md` § 7 (historical note: Phase 3.0 xfail closed by Phase 3.5 Path A). | `PythonDataService/tests/research/ml/test_loader.py::*next_after*` (smallest strictly-greater key, none-when-no-later, unsorted-input-handling, `PredictionLookupError` subclassing), `test_coverage.py` (5 next_after_bar_close cases including missing rows + missing fields + fired-ts diagnostics), `tests/research/parity/test_qc_aapl_phase3_trade_parity.py` (Phase 3.5 acceptance — closes the Phase 3.0 `xfail(strict=True)` with single-fill pinned at 2026-02-10) | **canonical** — Phase 3.5 Path A shipped 2026-05-12 (PR #223); multi-day round-trip P&L **not pursued** (decision 2026-05-12 — QC free tier's minute-data trailing window, not a backtesting OOS reserve as earlier framing claimed; full rationale in authority doc § 7 and § 10) |
| Live broker-execution verdict authoring (12-value `ReasonCode` taxonomy, four-value `Verdict` enum, versioned operator-facing templates, per-instance lag-driven verdict thresholds) | `PythonDataService/app/services/broker_activity_reconciler.py::author_row_from_event` — pure entry point composing `match_identity` (namespace exact-equality per ADR 0008 §1), `compute_lag_breakdown` (four-phase intent → exec latency), `classify_verdict` (11-rule priority ladder), `select_template`, and `render_template`. Closed taxonomies live on `app/schemas/broker_activity.py::{Verdict, ReasonCode}`; versioned templates on `app/services/broker_activity_templates.py`; per-instance `ReconciliationTimingPolicy` carries `caveat_lag_ms` / `excessive_lag_ms` thresholds. The stateful publisher (`app/services/broker_activity_publisher.py`) is the *only* writer of `broker_activity.jsonl` (ADR 0008 amendment 2026-06-22 — sibling WAL to `intent_events.jsonl`). | `app/research/parity/qc_reconciler.py::DivergenceCategory` is a **sibling, not duplicate** — backtest post-hoc parity vs live per-execution narrative. Consumer concerns differ (acceptance gate vs operator chip + template); the two enums are deliberately separate and must stay separate. See `docs/references/ibkr-reconciliation.md` § "Divergence taxonomy — live vs. backtest". | ADR 0014 (decision); ADR 0008 §1 (`order_ref` ownership) + amendment 2026-06-22 (sibling-WAL contract); `docs/references/ibkr-reconciliation.md` (engineer-facing reference); IBKR TWS API via `ib_async` for the event source (`IB.execDetailsEvent` / `IB.orderStatusEvent`, surfaced by `app/broker/ibkr/orders.py::stream_order_events`). | `PythonDataService/tests/services/test_broker_activity_reconciler.py` (verdict ladder coverage, ReasonCode/template completeness invariant, truthfulness property test — templates exercised transitively via this suite), `test_broker_activity_publisher.py` (subscriber pub-sub, dedupe cache, WAL append ordering), `test_broker_activity_wal.py` (append-fsync contract, trailing-unterminated-line tolerance), `tests/schemas/test_broker_activity.py` (schema validation), `tests/routers/test_broker_activity.py` (SSE stream + REST backfill HTTP contract) | **canonical** — slice 1 (authoring + WAL + SSE/REST surface) shipped on `broker-activity-slice-1-authoring-wal-sse`; verdict + template authoring is the single authority for the cockpit-v2 Activity tab's row stream per ADR 0014 |

### Strategies (LEAN-ported algorithms)

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| SPY EMA Crossover | `PythonDataService/app/engine/strategy/algorithms/spy_ema_crossover.py` (+ options variant `spy_ema_crossover_options.py`). **Parity-pinned secondary**: `PythonDataService/app/engine/strategy/spec/evaluator.py::SpecAlgorithm` driven by `app/engine/strategy/spec/fixtures/spy_ema_crossover.spec.json` reproduces the hand-coded twin trade-by-trade (Phase 1 acceptance gate, 2026-05-04). | `Backend/Services/Implementation/BacktestService.cs::RunSmaCrossover` (similar intent, not identical algo — scheduled for removal in **Phase 3.2**); `app/research/divergence/strategies/s1_ema_crossover.py` — **divergence-research-only** vectorized parallel; module docstring acknowledges "Mirrors the rule-set of `spy_ema_crossover`" but is not the authority and not expected to match exactly | LEAN; TradingView parity: `docs/validation/SPY_EMA_Crossover_RSI.pine`; validation report `docs/validation/SPY_EMA_Crossover_Validation_Report.pdf` | `PythonDataService/tests/test_strategy_engine.py`; TV parity via Pine; spec ↔ hand-coded parity at `app/engine/strategy/spec/tests/test_spec_spy_ema_parity.py` | canonical — strongest external validation in the repo |
| SPY ORB (Opening Range Breakout) | `PythonDataService/app/engine/strategy/algorithms/spy_orb.py` | — | `docs/validation/SPY_ORB_Strategy.pine`, `docs/validation/SPY_ORB_Strategy_Plan.md`, `docs/validation/ORB_Cross_System_Validation_Report.pdf` | TV Pine + PDF report | canonical |
| QQQ ORB | — | — | `docs/validation/QQQ_ORB_Strategy.pine`, `docs/validation/QQQ_ORB_Validation_Report.pdf` | Pine only | external-validated — Python port not yet started |
| RSI Mean Reversion | `PythonDataService/app/engine/strategy/algorithms/rsi_mean_reversion.py`. **Parity-pinned secondary**: `SpecAlgorithm` driven by `spec/fixtures/rsi_mean_reversion.spec.json` reproduces the hand-coded twin trade-by-trade. | `Backend/Services/Implementation/BacktestService.cs::RunRsiMeanReversion` (scheduled for removal in **Phase 3.2**); `app/research/divergence/strategies/s2_rsi_mean_reversion.py` — **divergence-research-only** vectorized parallel | LEAN | `PythonDataService/tests/test_strategy_engine.py`; spec ↔ hand-coded parity at `spec/tests/test_spec_rsi_mean_reversion_parity.py` | pending-migration |
| SMA Crossover | `PythonDataService/app/engine/strategy/algorithms/sma_crossover.py`. **Parity-pinned secondary**: `SpecAlgorithm` driven by `spec/fixtures/sma_crossover.spec.json` reproduces the hand-coded twin trade-by-trade. | `Backend/Services/Implementation/BacktestService.cs::RunSmaCrossover` (scheduled for removal in **Phase 3.2**); `app/research/divergence/strategies/s3_sma_crossover.py` — **divergence-research-only** vectorized parallel (SMA50/SMA200 golden cross) | LEAN | `PythonDataService/tests/test_strategy_engine.py`; spec ↔ hand-coded parity at `spec/tests/test_spec_sma_parity.py` | pending-migration |
| Momentum RSI/Stochastic | (no Python canonical yet — `Backend/Services/Implementation/BacktestService.cs::RunMomentumRsiStochastic` is the only implementation today; an older function-based port exists at `app/services/strategies/momentum_rsi_stochastic.py` but is not exposed via `/api/engine/backtest`) | `Backend/Services/Implementation/BacktestService.cs::RunMomentumRsiStochastic` | Internal — originated in BacktestService.cs; no LEAN origin confirmed (F-0030 research: vendored LEAN extract at `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/` has no matching strategy file) | `Backend.Tests/Unit/Services/BacktestServiceTests.cs` | **pending-migration** — needs a Python port to `app/engine/strategy/algorithms/` *before* Phase 3.2 deletes the .NET implementation, OR the strategy is dropped if not in use |
| RSI Reversal | (no Python canonical yet — `Backend/Services/Implementation/BacktestService.cs::RunRsiReversal` is the only implementation today; an older port exists at `app/services/strategies/rsi_reversal.py` but is not exposed via `/api/engine/backtest`) | `Backend/Services/Implementation/BacktestService.cs::RunRsiReversal` | Internal — originated in BacktestService.cs; no LEAN origin confirmed (same vendored-LEAN search result as Momentum RSI/Stochastic above) | `Backend.Tests/Unit/Services/BacktestServiceTests.cs` | **pending-migration** — same as Momentum RSI/Stochastic above |
| SPY RSI-range A (EMA-gap + MACD entry; ADX<15 exit) | `PythonDataService/app/engine/strategy/algorithms/spy_strategy_a.py` (extends `_rsi_range_base.RsiRangeStrategy`) | — | Internal — no external port reference; long-only 15-min RTH | `app/engine/tests/` (existing tests; no parity fixture identified) | canonical |
| SPY RSI-range B (Supertrend + ADX>20 + MACD entry; ADX<20 exit) | `PythonDataService/app/engine/strategy/algorithms/spy_strategy_b.py` | — | Internal | `app/engine/tests/` | canonical |
| SPY RSI-range C (ADX>20 + ADX-rising entry; ADX<15 exit) | `PythonDataService/app/engine/strategy/algorithms/spy_strategy_c.py` | — | Internal | `app/engine/tests/` | canonical |
| SPY RSI-range base (shared infrastructure for variants A/B/C) | `PythonDataService/app/engine/strategy/algorithms/_rsi_range_base.py` | — | Internal | (exercised transitively via A/B/C tests) | canonical-supporting |

### Portfolio / valuation

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Position mark-to-market valuation (position × live price aggregation) | `Backend/Services/Implementation/PortfolioValuationService.cs::ComputeValuationInternal` (compliant — pure aggregation) | — | Elementary accounting; no external reference | `Backend.Tests/Unit/Services/PortfolioValuationServiceTests.cs` | **pending rule-5 review** — marks-to-market correctly (position × live price). **⚠️ Caution (2026-06-14):** the same service still aggregates stale entry Greeks into `NetDelta/NetGamma/NetTheta/NetVega` (VCR-0005); those fields are misleading and are scheduled for hard-delete in remediation Phase 9. Do NOT bind to GraphQL `netDelta/netGamma/netTheta/netVega` for hedging decisions — use `getPortfolioLiveGreeks` / `getDollarDelta` / `getPortfolioVega` instead. |
| Lot-level FIFO accounting (realized PnL, cost-basis allocation, position rebuild from trade log) | `Backend/Services/Implementation/PositionEngine.cs::{RebuildPositionsAsync,ApplyTradeInternal}` | — | Standard FIFO inventory method (GAAP/IFRS); no external port reference, no paper to cite — well-known accounting arithmetic | `Backend.Tests/Unit/Services/PositionEngineTests.cs` (FIFO determinism, mark-to-market, partial-fill cost basis); `PortfolioValidationService.cs::Test1_FifoAccounting` runtime suite | **canonical-in-dotnet-justified** (per finding F-0010, closed 2026-05-06) — data lives in EF/Postgres; trade-replay-via-Python would round-trip every fill without simplifying anything. Documented exception per the contract's "math may live in any layer that fits the use case" rule. |
| Portfolio reconciliation | `Backend/Services/Implementation/PortfolioReconciliationService.cs` | — | Internal | `Backend.Tests/Unit/Services/PortfolioReconciliationServiceTests.cs` | pending rule-5 review — same reasoning as above |
| Portfolio scenario / what-if (theoretical option value across spot, time, IV grid) | `PythonDataService/app/services/portfolio_scenario.py` + `app/routers/portfolio.py::POST /api/portfolio/scenario` (**Phase 2.1 shipped 2026-04-26**, commit `d9738a5`). `Backend/Services/Implementation/PortfolioRiskService.cs::RunScenarioAsync` is now a typed-HttpClient passthrough that aggregates the Python response. | — (the .NET shock-propagation from stored `EntryDelta`/`EntryVega`/`EntryTheta` was deleted in `d9738a5`) | Hull §19 (Greek Letters), put-call parity bounds; canonical Python options authorities `bs_greeks.py` / `quantlib_pricer.py` / `volatility/solver.py` | `PythonDataService/tests/services/test_portfolio_scenario.py` — 8-case golden fixture for a 3-leg strategy across 5×5 (spot, time) grid at `atol=1e-6 rtol=1e-6` | **canonical** |
| Portfolio live Greeks (current-time delta, gamma, theta, vega per position) | `PythonDataService/app/routers/portfolio.py::POST /api/portfolio/live-greeks` + `IPolygonService.PortfolioLiveGreeksAsync` passthrough (**Phase 2.1 shipped 2026-04-26**, **consumers shipped 2026-04-27**) | — (the .NET shock-propagation from stored `EntryDelta`/`EntryVega` was deleted in commit `334d419`; `ComputeDollarDeltaAsync` and `ComputePortfolioVegaAsync` now project DB positions into `PortfolioScenarioPositionDto`, call `PortfolioLiveGreeksAsync`, and aggregate per-leg recomputed Greeks) | Hull §19; `bs_greeks.py` canonical | `Backend.Tests/Unit/Services/PortfolioRiskServiceTests.cs` 7/7 pass with mocked live-Greeks responses; cross-engine math validation is via `PythonDataService/tests/services/test_portfolio_scenario.py` | **canonical** |
| Portfolio risk (Greek aggregation across positions, post-recompute) | `Backend/Services/Implementation/PortfolioRiskService.cs` (aggregation only — **compliant** once Greeks themselves come from Python passthrough per Phase 2.2) | — | — | `Backend.Tests/Unit/Services/PortfolioRiskServiceTests.cs` | **pending — will be canonical post-Phase 2.2** — aggregating Python-supplied Greeks in .NET is rule-5 compliant; the violation is *computing* the Greeks in .NET |
| Strategy attribution (trade ↔ strategy linking) | `Backend/Services/Implementation/StrategyAttributionService.cs` | — | — | `Backend.Tests/Unit/Services/StrategyAttributionServiceTests.cs` | **not math** — persistence only, compliant |

### Research / divergence pipeline

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Bar divergence (Polygon vs TradingView) | `PythonDataService/app/research/divergence/analysis/bar_divergence.py` | — | `docs/tv-polygon-validation-gotchas.md`; TV Pine files in `docs/validation/` | `PythonDataService/tests/research/divergence/test_bar_divergence.py` | canonical |
| Trade divergence | `PythonDataService/app/research/divergence/analysis/trade_divergence.py` | — | Internal (reconciliation taxonomy from `reconcile-backtest` skill) | `NONE — pending` | pending-fixture |
| Dividend adjustment | `PythonDataService/app/research/divergence/ingest/dividend_adjuster.py` | — | Internal — empirically derived from TradingView/Polygon close-price step changes at dividend ex-dates (see module docstring); hardcoded SPY dividend events, not CRSP (F-0030 research: module docstring confirms "Values were recovered empirically from the TV-vs-Polygon close-price step changes"). No external reference. | `NONE — pending` | pending-fixture — reference is now honest; fixture obligation remains |
| Indicator reliability methodology | `PythonDataService/app/research/indicator_reliability.py` | — | `Frontend/src/assets/docs/indicator-reliability-methodology.md`, `docs/indicator-reliability-methodology.md` | `PythonDataService/tests/research/test_indicator_reliability.py` | canonical |

### Research signal validation primitives

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Information coefficient (IC) | `PythonDataService/app/research/validation/ic.py` | — | Lopez de Prado, *Advances in Financial Machine Learning* (2018), §8 (or whichever method is implemented — verify) | `NONE — pending` | pending-fixture — reference needs verification |
| Quantile-based statistics (binning, monotonicity, returns by quantile) | `PythonDataService/app/research/validation/quantile.py` | — | Standard non-parametric statistics (Conover, *Practical Nonparametric Statistics*) | `NONE — pending` | pending-fixture |
| Robustness statistics (block bootstrap, regime breakdowns) | `PythonDataService/app/research/validation/robustness.py` | — | Politis & Romano (1994) for block bootstrap (verify) | `NONE — pending` | pending-fixture — reference needs verification |

### Volatility surface and analytics

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| IV surface fitting | `PythonDataService/app/volatility/fitting.py` | — | (verify which parameterization is implemented — likely SVI per Gatheral 2014, or SABR per Hagan 2002) | `NONE — pending` | pending-fixture — reference verification owed |
| IV surface representation / model objects | `PythonDataService/app/volatility/{surface.py,models.py}` | — | Internal — paired with `fitting.py` | `NONE — pending` | pending-fixture |
| IV30 construction & health checks | `PythonDataService/app/volatility/iv30_health.py` | — | `docs/math-rigor.md` Upgrade 1 (variance-time interpolation, 30-day constant-maturity); related to `app/research/options/iv_builder.py` row above | `NONE — pending` | pending-fixture |
| IV-RV basis (implied vs realized comparison) | `PythonDataService/app/volatility/basis.py` | — | `docs/references/iv-rv-basis-alignment.md` (existing reference doc) | `NONE — pending` | pending-fixture |
| VIX replication (model-free fair-strike variance from option strip) | `PythonDataService/app/volatility/vix_replication.py` | — | Demeterfi, Derman, Kamal, Zou (1999) "More Than You Ever Wanted to Know About Volatility Swaps" — **verify the file matches the paper** | `NONE — pending` | pending-fixture — high-stakes provenance: VIX-replication math must cite the paper exactly |
| Price normalization (forward / log-moneyness / strike scaling) | `PythonDataService/app/volatility/price_normalization.py` | — | Internal — verify which normalization is in use | `NONE — pending` | pending-fixture |
| Day-count and annualization conventions | `PythonDataService/app/volatility/conventions.py` | — | Standard convention (ACT/365 or ACT/252 — verify) | `NONE — pending` | pending-fixture |
| IV provenance metadata | `PythonDataService/app/volatility/iv_provenance.py` | — | Internal — infrastructure for IV traceability | `NONE — pending` | canonical-supporting |
| IV analytics (aggregate IV statistics) | `PythonDataService/app/volatility/analytics.py` | — | Internal | `NONE — pending` | pending-fixture |

### Edge research / volatility / regime

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Edge score (composite ranking) | `PythonDataService/app/engine/edge/edge_score.py` | — | Internal — composite of underlying signals (VRP, regime, realized vol, etc.) | `NONE — pending` | pending-fixture |
| Variance Risk Premium (VRP) | `PythonDataService/app/engine/edge/vrp.py` | — | Bakshi & Madan (2000), or Carr & Wu (2009) — verify which definition | `NONE — pending` | pending-fixture — reference verification owed |
| Regime clustering | `PythonDataService/app/engine/edge/regime_clustering.py` | — | Verify which method (HMM per Hamilton 1989, k-means, GMM); test in `tests/edge/test_regime_clustering.py` uses `fit_gaussian_hmm` | `tests/edge/test_regime_clustering.py` (partial — only EM convergence + posterior shape) | pending-fixture — reference verification owed |
| Regime drift detection | `PythonDataService/app/engine/edge/regime_drift.py` | — | Internal | `NONE — pending` | pending-fixture |
| Regime-conditioned strategy evaluation | `PythonDataService/app/engine/edge/regime_strategy_eval.py` | — | Internal | `NONE — pending` | pending-fixture |
| Robustness statistics (edge-level) | `PythonDataService/app/engine/edge/robustness_stats.py` | — | (likely overlaps with `app/research/validation/robustness.py` — verify whether one calls the other or duplicates) | `NONE — pending` | pending-fixture |
| Spread model (transaction cost / bid-ask) | `PythonDataService/app/engine/edge/spread_model.py` | — | Internal — verify whether linear, sqrt-impact, or empirical | `NONE — pending` | pending-fixture |
| Trade simulator (edge-context fills) | `PythonDataService/app/engine/edge/trade_simulator.py` | — | Internal — separate from `engine/execution/fill_model.py`? verify | `NONE — pending` | pending-fixture |
| Portfolio aggregator (cross-asset / cross-strategy combination) | `PythonDataService/app/engine/edge/portfolio_aggregator.py` | — | Internal | `NONE — pending` | pending-fixture |
| Cross-asset runner | `PythonDataService/app/engine/edge/cross_asset_runner.py` | — | Internal — orchestration | `NONE — pending` | canonical-supporting |
| Period splitter (train/test or in/out-of-sample slicing) | `PythonDataService/app/engine/edge/period_splitter.py` | — | Internal | `NONE — pending` | pending-fixture |
| Threshold events (signal binarization) | `PythonDataService/app/engine/edge/threshold_events.py` | — | Internal | `NONE — pending` | pending-fixture |
| Realized vol — high-frequency (intraday) | `PythonDataService/app/engine/edge/features_realtime/hf_realized_vol.py` | — | Andersen-Bollerslev-Diebold-Labys (2003) realized variance estimator family — verify | `NONE — pending` | pending-fixture — reference verification owed |
| Realized vol — close-to-close | `PythonDataService/app/engine/edge/features_realtime/realized_vol.py` | — | Standard close-to-close σ estimator — verify | `NONE — pending` | pending-fixture |
| IV30 constructor (realtime) | `PythonDataService/app/engine/edge/features_realtime/iv30_constructor.py` | — | Same family as `app/volatility/iv30_health.py` — possible duplicate; resolution owed | `NONE — pending` | pending-fixture — possible duplicate of `app/volatility/iv30_health.py` |
| Delta inversion feature | `PythonDataService/app/engine/edge/features_realtime/delta_inversion.py` | — | Internal | `NONE — pending` | pending-fixture |
| Regime features (realtime) | `PythonDataService/app/engine/edge/features_realtime/regime_features.py` | — | Internal | `NONE — pending` | pending-fixture |
| Forward realized-vol labels | `PythonDataService/app/engine/edge/labels_oracle/forward_rv.py` (+ `hf_forward_rv.py` HF variant) | — | Internal — supervised-learning label generation | `NONE — pending` | pending-fixture |
| Edge confidence calibration | `PythonDataService/app/engine/edge/{confidence.py,calibration/confidence.py}` | — | Verify which is canonical and which is consumer | `NONE — pending` | pending-fixture — internal duplication owed for resolution |

### Research signal scoring (IC-based pipeline)

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Research signal backtest engine | `PythonDataService/app/research/signal/{backtest.py,engine.py}` | — | Internal — separate-purpose research engine, distinct from `app/engine/` (LEAN-ported event-driven backtester) | `NONE — pending` | pending-fixture |
| Walk-forward analysis | `PythonDataService/app/research/signal/walk_forward.py` | — | Lopez de Prado, Advances in Financial ML (2018), §7 — verify | `NONE — pending` | pending-fixture |
| Signal diagnostics (mean/std/active-pct/autocorrelation) | `PythonDataService/app/research/signal/diagnostics.py` | — | Internal | `NONE — pending` | pending-fixture |
| Signal standardization (z-score / rank) | `PythonDataService/app/research/signal/standardize.py` | — | Standard z-score / rank transformation — verify | `NONE — pending` | pending-fixture |
| Regime-conditioned scoring (research) | `PythonDataService/app/research/signal/regime.py` | — | Internal — overlaps with `app/engine/edge/regime_*` (resolution owed) | `NONE — pending` | pending-fixture |
| Signal graduation criteria | `PythonDataService/app/research/signal/graduation.py` | — | Internal | `NONE — pending` | pending-fixture |

### Research feature engineering

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Research TA features (momentum_5m, rsi_14, realized_vol_30, volume_zscore) | `PythonDataService/app/research/features/ta_features.py` | RSI computation overlaps with `engine/indicators/rsi.py` (Wilders, canonical) and `services/ta_service.py` (pandas-ta) — see RSI row above for the 3-way duplication | pandas-ta (external) | `NONE — pending` | pending-fixture — RSI duplication is the notable concern |
| Research options features (IV30, IV rank) | `PythonDataService/app/research/features/options_features.py` (`compute_iv_30d`, `compute_iv_rank` over rolling 60-day window with `min_periods=30`) | — | Internal — IV rank is standard rolling (max−min)/(max−min) over window | `NONE — pending` | pending-fixture |

### Data / transport (rule-5 compliant by design)

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Technical analysis endpoint fan-out | `Backend/Services/Implementation/TechnicalAnalysisService.cs` | — | n/a (transport) | `Backend.Tests/Unit/Services/TechnicalAnalysisServiceTests.cs` | **not math** — pure HTTP passthrough to Python `/api/indicators/calculate`. Compliant. |
| Market data, bars, aggregates | `Backend/Services/Implementation/MarketDataService.cs` | `PolygonService.cs` (external client) | Polygon.io Aggregates v2 API | `Backend.Tests/Unit/Services/MarketDataServiceTests.cs`, `PolygonServiceTests.cs` | transport — not a math concept |
| Sanitization endpoint fan-out | `Backend/Services/Implementation/SanitizationService.cs` | — | n/a (transport) | (existing tests, if any) | **not math** — HTTP passthrough to Python `/api/sanitize`. Compliant. |
| Research endpoint fan-out | `Backend/Services/Implementation/ResearchService.cs` | — | n/a (transport + EF persistence of research reports) | (existing tests, if any) | **not math** — HTTP passthrough plus persistence. Compliant. |
| Spec strategy backtest passthrough | `Backend/Services/Implementation/SpecStrategyService.cs` | — | n/a (transport) | (existing tests, if any) | **not math** — explicit "thin passthrough" per file header; spec JSON is forwarded without re-validation. Compliant. |
| Portfolio account / order persistence | `Backend/Services/Implementation/PortfolioService.cs` | — | n/a (persistence; FIFO accounting math is in `PositionEngine.cs` per finding F-0010) | (existing tests, if any) | **persistence** — order/account writes; dispatches to `PositionEngine` for math. Compliant once F-0010 classification lands. |
| Data quality metrics (zero-volume rate, flat bars, OHLC violations, fractional-volume bars) | `PythonDataService/app/services/data_quality_service.py` | — | Internal — no external reference; QC metrics are arithmetic counts over bar properties | `NONE — pending` | canonical |
| Pandas-ta vs TradingView validation report | `PythonDataService/app/services/validation_service.py` (thresholds `_EXACT=0.001`, `_CLOSE=0.01`, `_OK=0.1` baked in — verify provenance) | — | Internal — no external reference | `NONE — pending` | validation-only — comparator, not a math producer |

## External data provenance

Per the contract, external data still needs a provenance row once — it's acceptable provenance to say "we trust the vendor, not independently validated."

| Source | Canonical client | Scope | Notes |
|---|---|---|---|
| **Polygon.io REST (Aggregates v2, Options Chain snapshots)** | `PythonDataService/app/services/polygon_client.py` | OHLCV bars, live options snapshots, tickers | Starter plan: 2-year max history, **15-min delayed**, options snapshots only for live contracts. **external: Polygon.io, not independently validated.** Sanitizer at `app/services/sanitizer.py` enforces gap/monotonicity invariants at the ingestion boundary. |
| **TradingView Pine (strategy validation)** | `docs/validation/*.pine` | Parity reference for SPY EMA Crossover, SPY/QQQ ORB | Used for one-time validation runs; not a runtime data source |
| **LEAN Engine (vendored reference)** | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/` (vendored 2026-04-26) | Ground-truth for indicators and engine semantics | Pinned by commit `7986ed0aade3ae5de06121682409f05984e32ff7` (master HEAD as of 2026-04-26). See `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/attribution.md` for vendored-files manifest. Regeneration triggers a fixture rebuild with commit-message justification. |
| **FRED (pending)** | — | Risk-free rate for BS solver | Proposed in `docs/math-rigor.md` Upgrade 4; not yet wired |

## Adding a new entry

1. You're adding a new math function. Before you write it, decide: is this a new concept, or a new parameterization of an existing one?
2. New concept → add a row here in the matching section (or create a new section). Fill all six columns. Commit the row in the same PR as the implementation.
3. New parameterization (e.g., new indicator period, new option expiry) → no new row; the existing row already covers it.
4. The `Validated against` field must either name a real test file OR say `NONE — pending` with a follow-up plan in the PR description. `Validated against: manually checked` is not allowed.

## Legacy-debt burn-down rule (not a backfill mandate)

Existing canonical files that predate this registry (EMA, SMA, RSI) already carry first-class prose docstrings citing LEAN but not in the 4-field format. **Do not open a PR that just converts every existing docstring.** Instead, when you touch one of these files for a real reason, convert its docstring in the same PR. This keeps the registry honest without creating churn.

## Known rule-5 non-compliance (tracked here, not lost)

As of 2026-04-26, these are the known cases where the Python-owns-all-math rule is violated. Each is sequenced in `docs/architecture/numerical-authority-migration-plan.md`.

1. **`Backend/Services/Implementation/BacktestService.cs`** — runs four strategies in-process (`RunSmaCrossover`, `RunRsiMeanReversion`, `RunMomentumRsiStochastic`, `RunRsiReversal`) and computes `CalculateMaxDrawdown` / `CalculateSharpeRatio` locally. **Migration: Phase 3** of the migration plan — **deferred 2026-04-27**, blocked on `lean-engine` Frontend component reaching feature parity with Strategy Lab. The `runBacktest` mutation has two consumers (Strategy Lab + lean-engine), and lean-engine still imports child components from inside `strategy-lab/`. Phase 3.0 (deprecation comment in `BacktestService.cs`) shipped commit `d3c3c18`. Phase 3.1+ deferred until lean-engine supersession completes; the cleanest path at that point is bulk deletion of Strategy Lab UI + the four .NET strategies + the older `app/services/strategies/` strategy files. Note: Python ports of all four strategies do exist in the older `app/services/strategies/` directory (function-based, pandas-ta), but they are not exposed via `/api/engine/backtest`. Newer `app/engine/strategy/algorithms/` ports only `sma_crossover` and `rsi_mean_reversion`.
2. **`Frontend/src/app/utils/black-scholes.ts`** — client-side pricing and Greeks for UI responsiveness. Self-labeled `[LEGACY-OK — RENDER-HELPER ONLY, NO NEW CALLERS]` as of 2026-04-27. **Migration status:** Phase 1.1 (extend `strategy_engine.py::analyze_strategy` payload) — DONE (`451394d`). Phase 1.2 (rewire `OptionsStrategyLabComponent` to consume server fields, drop BS imports) — DONE (`451394d`). Phase 1.3 (freeze module) — **SHIPPED** with two intentional callers documented in the file header: (a) `pricing-lab.component.ts` is a comparison harness whose purpose is to show the TS pricer alongside the server pricer side-by-side; (b) `strategy-builder.component.ts` evaluates BS over a 1200-point price grid on every leg edit and would have UI lag if migrated to server round-trips per change. Both callers produce exploratory feedback, not numbers users compare against another number; the canonical authorities `bs_greeks.py` + `quantlib_pricer.py` remain the only math authorities. Phase 1.4 (cross-engine BS parity fixture) — DONE (`451394d` + `69d2bfe` precision-leak fix).
3. **`Backend/Services/Implementation/PortfolioRiskService.cs` / `PortfolioValuationService.cs`** — shock-propagate from stored `EntryDelta` / `EntryVega` / `EntryTheta` instead of recomputing Greeks at current spot/time/IV. **Migration status (2026-05-06):** ✅ **CLOSED.** Phase 2.1 (Python `/portfolio/scenario` endpoint) — DONE. Phase 2.2 (`RunScenarioAsync` rewired to call Python; old EntryVega/EntryTheta shock-propagation deleted) — DONE. Phase 2.3 (`ComputeDollarDeltaAsync` + `ComputePortfolioVegaAsync` switched from stored entry Greeks to `IPolygonService.PortfolioLiveGreeksAsync`) — DONE in commit `334d419` (2026-04-27). 7/7 tests pass. This rule-5 violation is now fully resolved; entry retained as historical record.
4. **`PythonDataService/app/services/rule_based_backtest.py`** — **not a strategy implementation**, it is a configurable rule engine (composable entry conditions: EMA crossover, RSI band, ADX filter, gap filter; multiple exit modes: fixed-bar, indicator-based; parameterized via JSON). The newer `app/engine/strategy/algorithms/` has only fixed strategies, so the originally proposed "thin adapter" cannot be a literal delegate. **Migration: Phase 4 deferred 2026-04-27**, pending design decision on which of three reformulated paths to take: (a) build a new `RuleBasedAlgorithm(Strategy)` subclass in the newer engine, (b) map specific configurations to specific fixed algorithms (fragile), or (c) accept `rule_based_backtest.py` as a permanent alternate engine and document the divide. See `docs/architecture/numerical-authority-migration-plan.md` § Status as of 2026-04-27.
5. **Hardcoded `r = 0.043`** in **6 production locations**: `app/research/options/iv_builder.py:18 DEFAULT_RISK_FREE_RATE`, `app/research/options/contract_finder.py:26 DEFAULT_RFR`, `app/models/strategy.py:48` (Pydantic Field default), `app/models/portfolio.py:97` and `:184` (two Pydantic Field defaults — same pattern, two model classes). `app/services/fred_service.py:32 FALLBACK_RATE = 0.043` is the canonical fallback path and is correct. **Migration: deferred** — full migration to FRED-interpolated rates tracked in `docs/math-rigor.md` Upgrade 4. Per finding F-0029.
