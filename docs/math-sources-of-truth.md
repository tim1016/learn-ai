# Math sources of truth

One row per mathematical concept in learn-ai. Names **the** canonical implementation, calls out every known duplicate, and records what test proves the canonical is right.

Paired with `.claude/skills/learn-ai-validation/SKILL.md` (the Math Provenance Contract) and `.claude/rules/numerical-rigor.md` (the scientific standards). The **skill enforces the contract**; **this file is the lookup** that tells the contract which file to point at.

## How to read this

| Column | Meaning |
|---|---|
| **Concept** | The named math (e.g., "EMA", "Black-Scholes call price"). One row per concept; if the math is parameterized (e.g., EMA period), one row covers all parameterizations. |
| **Canonical** | The **one** file that is source-of-truth. Per root `CLAUDE.md` rule 5, canonical is Python unless explicitly justified otherwise. |
| **Legacy / duplicates** | Every other file in the repo that implements the same math. Each must either (a) call canonical at runtime or (b) carry a parity test naming the canonical file. |
| **Reference** | The external source the canonical was ported from. `references/` path, paper citation, or authoritative URL. |
| **Validated against** | The test that proves canonical ↔ reference equivalence. If no fixture exists yet, write `NONE — pending`. |
| **Status** | `canonical` · `legacy-ok` (duplicate with parity test) · `pending-migration` (rule-5 violation) · `pending-fixture` (canonical but no equivalence proof) · `external-unvalidated` (we trust the vendor) |

## Registry

> **No-new-concept note — run-scoped replay proof (PRD #1753, Direction 2).** The
> replay-proof engine path added by issues #1758–#1762
> (`PythonDataService/app/services/run_replay_proof.py`) introduces **no new math
> concept** and therefore gains **no registry row**. Its content hashing reuses
> the canonical `trace_root` (`app/engine/strategy/signal_program.py`) and its
> engine-parity leg reuses the already-existing BacktestEngine shadow-trace
> comparator; the AGENTS.md "update both registries" rule is satisfied by the new
> row in `docs/architecture/engine-authority-map.md` plus this explicit note.

### Broker display read models

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Broker orders grouped by symbol (gross requested, filled, and working quantities) | `PythonDataService/app/services/broker_order_groups.py` | — | Product read-model formula over absolute broker-reported quantities; decimal arithmetic from the Python standard library. These activity totals are never net position or directional exposure. | `PythonDataService/tests/services/test_broker_order_groups.py` | canonical |
| Alpaca order submission-to-fill latency (seconds) | `PythonDataService/app/broker/alpaca/adapter.py::fill_latency_seconds` | —; Angular renders the supplied scalar without recomputing it. | Alpaca Trading API order properties (`submitted_at`, `filled_at`): `https://docs.alpaca.markets/us/docs/brokerapi-trading`. Formula: `(filled_at_ms - submitted_at_ms) / 1,000`; unavailable until both broker clocks exist. | `PythonDataService/tests/broker/alpaca/test_adapter_orders.py::test_filled_order_maps_every_field_and_synthesizes_fill_event` and `::test_fill_latency_is_unknown_until_both_broker_clocks_exist` (`atol=1e-12, rtol=0`) | canonical |

### Indicators — Python-canonical, ported from LEAN

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| SMA | `PythonDataService/app/engine/indicators/sma.py` | `PythonDataService/app/services/ta_service.py` (pandas-ta passthrough, used by `/api/indicators/calculate`) | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/Indicators/SimpleMovingAverage.cs` | `PythonDataService/tests/test_indicator_parity.py`; `tests/fixtures/test_indicator_fixtures.py::TestIND002SMA` (IND-002, hand_computed oracle, period=3, atol=1e-9) — see [IND-002](docs/references/golden-fixtures/indicators/IND-002.md) | canonical — provenance block pending on sma.py |
| EMA | `PythonDataService/app/engine/indicators/ema.py` | `ta_service.py` (pandas-ta path) | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/Indicators/ExponentialMovingAverage.cs` | `PythonDataService/tests/test_indicator_parity.py`; `tests/fixtures/test_indicator_fixtures.py::TestIND001EMA` (IND-001, hand_computed oracle, period=3, k=0.5 SMA-seeded, atol=1e-9) — see [IND-001](docs/references/golden-fixtures/indicators/IND-001.md) | canonical — existing docstring cites LEAN; needs 4-field conversion on next touch |
| RSI (Wilders) | `PythonDataService/app/engine/indicators/rsi.py` | `ta_service.py` (pandas-ta path); `app/research/features/ta_features.py::compute_rsi_14` (third pandas-ta consumer in research feature pipeline) | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/Indicators/RelativeStrengthIndex.cs` with `MovingAverageType.Wilders` | `PythonDataService/tests/test_indicator_parity.py`; `tests/fixtures/test_indicator_fixtures.py::TestIND003RSI` (IND-003, hand_computed Wilder's-smoothing oracle, period=3, atol=1e-9) — see [IND-003](docs/references/golden-fixtures/indicators/IND-003.md) | canonical — existing docstring is already close to the 4-field format |
| MACD, Bollinger Bands | `PythonDataService/app/services/ta_service.py` (pandas-ta) | — | pandas-ta (external) | `PythonDataService/tests/test_indicators.py` | external-unvalidated — no LEAN parity yet; flag if strategy depends on exact match |
| Dynamic pandas-ta chart catalog projection (one-shot, non-strategy) | `PythonDataService/app/services/dataset_service.py::calculate_dynamic_indicators` for calculation; `app/services/chart_service.py::compute_indicator_results` for lossless chart-series formatting | `app/services/ta_service.py` remains the six-indicator compatibility surface; `app/services/chart_indicator_service.py` only validates exact caller-owned bars/recipes and delegates | pandas-ta (external); concept-specific LEAN indicator rows above remain authoritative for strategy math | `PythonDataService/tests/services/test_chart_indicator_service.py` (exact bar-close transport, multi-series preservation, explicit `atol=1e-9, rtol=0`); `tests/routers/test_chart_indicators_endpoint.py` | external-unvalidated presentation path — never a strategy-signal authority |
| Catalog-derived chart indicator warmup budget | `PythonDataService/app/services/indicator_warmup_policy.py::configured_indicator_warmup_bars` | `PythonDataService/app/services/broker_v2_panel/chart_projection_service.py` consumes the budget and reports whether available Polygon history satisfied it; Angular sends the returned calculation bars unchanged | Repository-internal conservative policy: five times the largest catalog-valid integer parameter maximum, independent of parameter names; external Polygon history remains capped to the documented entitlement | `PythonDataService/tests/services/test_indicator_warmup_policy.py`; `PythonDataService/tests/broker/v2panel/test_chart_projection.py` (warmup/display separation, sparse-history extension, entitlement exhaustion) | canonical input-budget policy — calculation truth remains with the indicator rows above |

### Options pricing and Greeks

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Black-Scholes price (European call/put) | `PythonDataService/app/services/bs_greeks.py::bs_european_price` (closed-form, canonical) AND `PythonDataService/app/services/quantlib_pricer.py::price_option` (QuantLib analytic_bs engine, second canonical, parity-pinned) | `Frontend/src/app/utils/black-scholes.ts` — **legacy-ok** for two intentional callers (`pricing-lab.component.ts` is a comparison harness; `strategy-builder.component.ts` is interactive UI, decision pending). Header forbids new callers. | Hull, Options Futures and Other Derivatives (10e), §15.8; QuantLib C++ reference | `PythonDataService/tests/services/test_bs_cross_engine_parity.py` — 360-case grid at `atol=1e-10` between `bs_greeks` and `quantlib_pricer.analytic_bs` (**Phase 1.4 shipped 2026-04-26**, precision-leak fix `69d2bfe`) | **canonical** — two-engine parity established |
| Greeks — Delta, Gamma, Theta, Vega, Rho | `PythonDataService/app/services/bs_greeks.py::black_scholes_greeks` (closed-form, canonical) AND `PythonDataService/app/services/quantlib_pricer.py` (QuantLib path) | `Frontend/src/app/utils/black-scholes.ts::bsDelta/bsGamma/bsTheta/bsVega/bsRho` — **legacy-ok** for the two intentional callers above. | Hull §19 (Greek Letters); QuantLib | `Frontend/src/app/utils/black-scholes.spec.ts` (TS self-consistency only); `tests/fixtures/test_options_pricing_fixtures_extended.py` — 180-case py_vollib==1.0.1 oracle: [BS-004](docs/references/golden-fixtures/options-pricing/BS-004.md) (Gamma, atol=1e-10), [BS-005](docs/references/golden-fixtures/options-pricing/BS-005.md) (Theta, atol=1e-10), [BS-006](docs/references/golden-fixtures/options-pricing/BS-006.md) (Vega, atol=1e-10), [BS-007](docs/references/golden-fixtures/options-pricing/BS-007.md) (Rho, atol=1e-10) | **canonical** — all five Greeks pinned against py_vollib==1.0.1 oracle (Phase 1 golden fixtures, 2026-05-08) |
| Normal CDF / PDF | `PythonDataService/app/services/quantlib_pricer.py` (QuantLib internal); `scipy.stats.norm` inside `bs_greeks.py` | `Frontend/src/app/utils/black-scholes.ts::normCdf` (Abramowitz & Stegun 7.1.26, \|error\| < 1.5e-7) — **legacy-ok**, supporting the two intentional black-scholes.ts callers | A&S (1964) 7.1.26 | `NONE — pending` (the TS A&S approximation is exercised transitively by black-scholes.spec.ts) | legacy-ok — TS path remains for the two intentional UI callers; not a math authority |
| Implied volatility (root-finding) | `PythonDataService/app/volatility/solver.py::implied_volatility` (Newton + Brent fallback with Brenner-Subrahmanyam seed) | `app/services/quantlib_pricer.py::implied_volatility` (QuantLib bisection — companion path for callers that want the QuantLib pricer in the loop) | Hull §19.11; Brent's method (scipy.optimize) | `NONE — pending` cross-engine parity between `volatility/solver.py` and the QuantLib companion | pending-fixture — two solvers exist but are not parity-pinned |
| IV term-structure interpolation (30-day constant-maturity) | `PythonDataService/app/research/options/iv_builder.py` | — | `docs/math-rigor.md` Upgrade 1 (variance-time interpolation, industry standard) | `NONE — pending` | pending-fixture — currently uses linear-in-σ (known bias per math-rigor.md); variance interpolation is scheduled |
| Risk-free rate | `PythonDataService/app/services/fred_service.py` (FRED-backed primary, `FALLBACK_RATE = 0.043` if unreachable). Hardcoded constants also live in `app/research/options/iv_builder.py::DEFAULT_RISK_FREE_RATE`, `app/research/options/contract_finder.py::DEFAULT_RFR`, and Pydantic field defaults in `app/models/{strategy,portfolio}.py` | — | `docs/math-rigor.md` Upgrade 4 proposes full FRED interpolation across DTB3/DTB4WK/DTB6/DTB1YR | n/a (constant fallback) | partially-shipped — `fred_service.py` exists; the four hardcoded `0.043` constants in models/research-options should resolve via fred_service rather than literal defaults |
| Options strategy analysis (payoff, POP, current-time PnL curve, Greek curves, per-leg diagnostics) | `PythonDataService/app/services/strategy_engine.py::AnalyzeOptionsStrategy` (+ `app/routers/strategy.py`) | — (Frontend `OptionsStrategyLabComponent` is a passthrough since `451394d`) | Hull §11–12 (payoff diagrams), §19 (Greeks); composes `bs_greeks.py` + `quantlib_pricer.py` + `volatility/solver.py` | `PythonDataService/tests/test_strategy_engine.py` | canonical |
| In-engine option pricing dispatch (QuantLib-only / market-preferred / market-required) | `PythonDataService/app/engine/options/pricer.py` (`PricingMode.QUANTLIB_ONLY` \| `MARKET_PREFERRED` \| `MARKET_REQUIRED`) | — | Composes `bs_greeks.py` + `quantlib_pricer.py`; market path uses observed mid where present | `NONE — pending` (existing engine tests under `app/engine/tests/` exercise it indirectly) | canonical |

### Backtesting engine and statistics

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Engine Lab run verdict v2 (fixed-completeness production-readiness composite, grade, deployment signal) | `PythonDataService/app/services/run_verdict_service.py::compute_run_verdict` (`RUN_VERDICT_VERSION = 2`) | v1 dynamically omitted unavailable sub-scores and renormalized the remaining dimensions; retained only as the historical fixture under `tests/fixtures/golden/run-verdict-v1/` | `docs/references/reconciliations/engine-lab-runs-75-76-statistics-validation-plan.md` § “Replace dynamic readiness reweighting with a fixed completeness contract” — 17 required sub-scores and fixed `25:20:20:20` relative weights; missing required evidence produces no grade | `PythonDataService/tests/services/test_run_verdict_parity.py`; golden contract and attribution under `PythonDataService/tests/fixtures/golden/run-verdict-v2/` | **canonical** — Python authors frozen `RunVerdictJson`; Frontend renders status, coverage, dimensions, and grade verbatim without scoring |
| Persisted Strategy Lab realized-equity staircase | `PythonDataService/app/engine/results/equity_downsample.py::build_realized_equity_envelope`; the engine persistence adapter `app/routers/engine.py::_persisted_trade_net_pnl` supplies net closed-trade P&L and delegates compatibility fees to the canonical `IbkrEquityCommissionModel`. | Engine-native mark-to-market curves remain separately authored through `from_engine_curve` and `from_lean_curve`; .NET only validates/transports the v2 envelope and Angular only renders the received points as a step series. | Internal persisted `BacktestTrade.PnL` accounting contract; [realized-equity-staircase-v1](docs/references/realized-equity-staircase-v1.md); product contract in `docs/prds/strategy-lab-results-experience.md` §§ 9–10. Formula: initial cash plus each persisted net closed-trade P&L at its `exit_ms_utc`, retaining every completed-trade step. | `PythonDataService/tests/engine/results/test_equity_downsample.py::test_realized_equity_matches_golden_fixture` against `PythonDataService/tests/fixtures/golden/engine-results/ENG-006/v1/` (`atol=1e-6, rtol=0`); integration persistence tests in `test_engine_persistence_quantity_pnl.py` (including the compatibility-tier fee regression) and `test_lean_sidecar_persistence.py` | **canonical** — risk statistics continue to use their independently canonical mark-to-market / compatibility-ledger inputs; no UI layer recomputes the staircase. |
| LEAN-native portfolio/trade statistics and formatted dashboard reproduction | `PythonDataService/app/engine/results/lean_statistics.py::reproduce_lean_total_performance` and `PythonDataService/app/engine/results/lean_statistics_format.py::format_lean_statistics_summary` at LEAN source commit `261366a7e26ae942df858ab20df4fef8fa07de67` | `compute_lean_statistics` in `lean_statistics.py` is the historical bar/trade approximation used by ordinary Python-only display paths; it is not allowed to author a native parity receipt | [LEAN native statistics Oracle v1](docs/references/lean-native-statistics-oracle-v1.md); exact pinned `StatisticsBuilder.cs`, `PortfolioStatistics.cs`, `TradeStatistics.cs`, `Statistics.cs`, dated LEAN interest-rate file, DLL/PDB/SourceLink receipts | `PythonDataService/tests/test_lean_statistics.py`; immutable raw Oracle under `PythonDataService/tests/fixtures/golden/lean-statistics-oracle-v1/`; 25 portfolio + 41 trade numerical values at `atol=0.0000500001, rtol=0`, plus 25 exact formatted strings | **canonical for `python.lean-compatible.261366a7...`** — persisted LEAN rows retain `lean.native` separately and carry a fail-closed reproduction receipt |
| Engine Lab raw compatibility-pair closed-trade performance projection | `PythonDataService/app/services/lean_sidecar_persistence.py::_compatibility_ledger_statistics` adapts the LEAN fill ledger into `PythonDataService/app/engine/results/statistics.py::summarize`; the Python anchor calls the same `summarize` path without an engine-native equity curve when `compatibility_profile=us-equity-raw-ibkr-v1`; `PythonDataService/app/services/run_verdict_service.py::build_run_verdict_parity_signature` freezes the required inputs at `1e-12` transport tolerance | Retired behavior left the linked LEAN verdict with an empty platform-statistics object (4/17 readiness inputs) while Python used its dense native curve (12/17) | `docs/references/reconciliations/engine-lab-runs-75-76-statistics-validation-plan.md` §§ 2–4; per-trade return is `(exit-entry)/entry`, and native LEAN chart/statistics remain separate Oracle evidence | `PythonDataService/tests/integration/reconciliation/test_engine_lab_compatibility_golden.py` against immutable runs 95/96; `PythonDataService/tests/services/test_lean_sidecar_persistence.py::test_compatibility_pair_scores_the_same_closed_trade_ledger_contract`; `PythonDataService/tests/services/test_run_verdict_parity.py::test_readiness_parity_signature_ignores_sub_epsilon_float_noise` | **canonical for linked `us-equity-raw-ibkr-v1` readiness inputs** — both peers now author the complete fixed 17/17 platform metric vector; the backend compares the Python-authored frozen readiness signatures before reporting agreement |
| Bar consolidation, event replay, fill models | `PythonDataService/app/engine/engine.py` (orchestration), `app/engine/consolidators/trade_bar_consolidator.py` (bar consolidation), `app/engine/execution/{fill_model,intrabar_resolver,portfolio,order,execution_config}.py` (execution + accounting) | Removed: `Backend/Services/Implementation/BacktestService.cs` and `PythonDataService/app/services/strategies/` | LEAN Engine, `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/` (vendored extract); LEAN stale-signal market-fill receipt in `docs/references/fill-model-parity-spike-2026-05-19.md` | `PythonDataService/tests/test_strategy_engine.py`, `tests/test_rule_based_backtest_validation.py`, `PythonDataService/tests/engine/test_fill_model.py`, `PythonDataService/tests/engine/test_engine_fill_modes.py`, `docs/validation/*.pdf` reports | **canonical** — Phase 3 duplicate-engine retirement complete 2026-08-06 |
| Configurable rule-based strategy runner | `PythonDataService/app/engine/strategy/spec/` — **Phase 1 shipped 2026-05-04** (schema + parity-pinned ports of SPY EMA / SMA / RSI). **Phase 2.1 shipped 2026-05-04** (manage layer: ADX/MACD/Supertrend wired, `PnLPercent`/`PnLPoints` primitives, `survival.CLOSE_ALL` action, behavior tests for stop-loss / profit-target / first-match-wins / no-fire-while-flat). Still equity-only and single-symbol. `app/services/rule_based_backtest.py` remains the predecessor (kept until Phase 4 cutover). | `PythonDataService/app/services/rule_based_backtest.py` — same intent, weaker schema, parallel engine. Phase 4 of the numerical-authority migration converts `runRuleBasedBacktest` to a passthrough that translates legacy params to a `StrategySpec`, then deletes `rule_based_backtest.py`. | Internal (no external port reference; behavior validated by parity tests against hand-coded LEAN-pinned twins + behavior tests for new manage primitives) | `app/engine/strategy/spec/tests/test_spec_*_parity.py` (3 strategies) + `test_spec_manage_rules.py` (manage behavior); `tests/test_rule_based_backtest_validation.py` (legacy) | **pending-design** for Phase 4 cutover; spec layer is parity-pinned secondary today |
| Deployment-validation consecutive green minute strategy | `PythonDataService/app/engine/strategy/algorithms/deployment_validation.py` owns the consecutive-green decision rule and calendar-derived session barrier; `app/engine/strategy/signal_program.py::SignalSession` owns its staged advance/settle lifecycle and canonical `EvaluationTrace` on a raw one-minute decision clock; `strategy/registry.py::_build_deployment_validation_signal_program` is the sole public construction seam (issue #1730). | LEAN sidecar validator template: `PythonDataService/app/lean_sidecar/trusted_samples/deployment_validation.py`; deployment audit copy: `references/qc-shadow/DeploymentValidationAlgorithm.py`. Legacy deploy binding still records `PythonDataService/app/engine/strategy/spec/fixtures/deployment_validation.spec.json` until the live-runner schema stops requiring `strategy_spec_path`; it is not the validation authority. | Internal session spec, captured in `docs/references/deployment-validation-consecutive-green.md` | `PythonDataService/tests/engine/test_deployment_validation_strategy.py`; `PythonDataService/tests/engine/live/test_deployment_validation_deploy_artifacts.py`; `PythonDataService/tests/lean_sidecar/test_deployment_validation_template.py`; `PythonDataService/tests/services/test_lean_sidecar_template_registry.py`; seam behaviour in `PythonDataService/tests/engine/strategy/test_deployment_validation_signal_program.py`; sealed qualification `PythonDataService/tests/engine/strategy/test_signal_program_qualification_matrix.py::test_validated_settings_corpus_has_a_pinned_trace_root` (parametrized across every registered Signal Program, including this one) | canonical — registry-backed staged decision seam added by #1730 (see `docs/references/deployment-validation-signal.md`); Python strategy is source of truth; LEAN template is the validator; StrategySpec is legacy deploy plumbing only |
| Max drawdown | `PythonDataService/app/engine/results/statistics.py::max_drawdown` (`_max_drawdown` is the scalar compatibility wrapper) | Removed: `BacktestService.CalculateMaxDrawdown`; `Backend/Services/Implementation/SnapshotService.cs::ComputeDrawdownSeries` remains the separate live-portfolio path tracked in F-0011 | Bacon, Practical Portfolio Performance Measurement (2e), §8.2 | `tests/test_statistics.py::TestMaxDrawdown`; `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG002MaxDrawdown` (ENG-002, numpy `maximum.accumulate` oracle, atol=1e-9) | **canonical** for backtests; live-portfolio parity remains separately tracked |
| Sharpe ratio | `PythonDataService/app/engine/results/statistics.py::_sharpe` (`TRADING_DAYS_PER_YEAR = 252` annualization) | Removed: `BacktestService.CalculateSharpeRatio`; `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` remains the separate live-portfolio path tracked in F-0011 | Sharpe (1994), *The Sharpe Ratio*, Journal of Portfolio Management | `tests/fixtures/test_engine_stats_fixtures.py::TestENG001Sharpe` (ENG-001, hand-computed oracle, atol=1e-9); `SnapshotServiceTests.cs` for the separate live path | **canonical** for backtests; live-portfolio parity remains separately tracked |
| Sortino ratio | `PythonDataService/app/engine/results/statistics.py::_sortino` | `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` (**legacy-ok-pending-parity** per F-0011) | Sortino, Miller, Schwartz (1991), *Managing Investment Risk* | `tests/fixtures/test_engine_stats_fixtures.py::TestENG001bSortino` (ENG-001b, degenerate hand_computed oracle, atol=1e-9) | pending-migration (.NET parity); Python canonical **has golden fixture** (ENG-001b) |
| Trade statistics (win rate, profit factor, payoff ratio, expectancy) | `PythonDataService/app/engine/results/statistics.py::compute_trade_statistics` | — | Pardo, *The Evaluation and Optimization of Trading Strategies* (2e), §4 (standard performance ratios) | `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG003TradeStats` (ENG-003, hand_computed oracle — 4-trade cases × 3 rows, atol=1e-9) — see [ENG-003](docs/references/golden-fixtures/engine-statistics/ENG-003.md) | **canonical** — golden fixture pinned (ENG-003, 2026-05-08) |
| Engine validation analytics (trailing-window return, weekday/hour expectancy, calendar-month seasonality, rolling trade stability) | `PythonDataService/app/services/engine_validation_analytics.py`; paired compatibility horizons use `build_compatibility_equity_curve` over the common return-normalized closed-trade ledger | Native Python and LEAN display charts remain evidence only and are deliberately not forced into pointwise parity | Pardo, *The Evaluation and Optimization of Trading Strategies* (2e), ch. 4; Bacon, *Practical Portfolio Performance Measurement* (2e), ch. 2; see `docs/references/engine-validation-analytics.md` and [LEAN native statistics Oracle v1](docs/references/lean-native-statistics-oracle-v1.md) | `PythonDataService/tests/services/test_engine_validation_analytics.py`; `test_compatibility_analytics_use_common_trade_curve_not_native_lean_chart` | **canonical** — Python computes; paired runs share one analytics primitive; Angular renders only |
| CAGR (Compound Annual Growth Rate) | `PythonDataService/app/engine/results/statistics.py::compute_portfolio_statistics` | `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` (**legacy-ok-pending-parity** per F-0011) | Standard CAGR formula: `(final/initial)^(252/trading_days) − 1`; Bacon, Practical Portfolio Performance Measurement (2e), §4.2 | `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG004CAGR` (ENG-004, hand_computed oracle, 3 cases, atol=1e-6) — see [ENG-004](docs/references/golden-fixtures/engine-statistics/ENG-004.md) | **canonical** — golden fixture pinned (ENG-004, 2026-05-08) |
| Calmar ratio | `PythonDataService/app/engine/results/statistics.py::compute_portfolio_statistics` | `Backend/Services/Implementation/SnapshotService.cs::ComputeMetrics` (**legacy-ok-pending-parity** per F-0011) | Bacon, Practical Portfolio Performance Measurement (2e), §8.3 (CAGR / max_drawdown); composed from ENG-004 + ENG-002 paths | `tests/fixtures/test_engine_stats_extended_fixtures.py::TestENG005Calmar` (ENG-005, hand_computed oracle, 3 cases, atol=1e-6; includes hand-algebraic consistency check: Calmar = CAGR / MDD) — see [ENG-005](docs/references/golden-fixtures/engine-statistics/ENG-005.md) | **canonical** — golden fixture pinned (ENG-005, 2026-05-08) |
| Replay determinism (same input → same output) | `PythonDataService/app/engine/` | — | Internal invariant | `Backend.Tests/Unit/Services/ReplayDeterminismTests.cs` | canonical-supporting — this test belongs in Python once engine migration completes |
| Run ledger / reproducibility hashing (canonical-JSON SHA-256 over spec + result; `data_snapshot_id`) | `PythonDataService/app/research/runs/hashing.py` (canonical_json + hash_payload), `app/research/runs/ledger.py` (RunLedger schema + identity columns), `app/research/runs/runner.py` (orchestration + hash assembly), `app/research/runs/storage.py` (atomic file-backed persistence), `app/routers/research_runs.py` (HTTP boundary) — **Phase A shipped 2026-05-06** (Build Alpha-style features 1-8 research pipeline). Equity-only, synchronous, file-backed under `PythonDataService/artifacts/runs/<run_id>/`; the same runner is the substrate for Phase B-E (workbench, walk-forward, Monte Carlo, robustness, baselines, sensitivity). | — | RFC 8785 (JSON Canonicalization Scheme — relaxed contract; see module docstring); SHA-256 (FIPS 180-4); `docs/references/run-ledger.md` for the `data_snapshot_id` rationale and Postgres-index upgrade path | `PythonDataService/tests/research/runs/test_hashing.py` (key-order independence, non-ASCII stability, snapshot-id formatting), `test_runner_inmemory.py` (replay determinism, parameter / window propagation, failure-path hash determinism), `test_storage.py` (round-trip, filter, atomic write), `test_endpoint.py` (HTTP-boundary hash determinism, int64-ms wire format), `test_ema_acceptance.py` (canonical SPY fixture end-to-end gate) | canonical |
| Relative operand difference in basis points | `PythonDataService/app/engine/strategy/spec/primitives.py::evaluate_operand` (`DifferenceBps` branch); serialized shape in `app/engine/strategy/spec/schema.py::DifferenceBps` | — | Internal normalized-gap formula `10,000 * (left - right) / right`; protocol and assumptions in `docs/references/spy-ema-normalized-gap-walk-forward.md` | `PythonDataService/tests/engine/strategy/spec/test_difference_bps_operand.py` against `tests/fixtures/golden/spy-ema-difference-bps/`; exact `Decimal` equality (`atol=0`, `rtol=0`) | canonical |
| Recency Chart trade/combo statistics (per-trade dollar PnL net of two flat per-order commissions, calendar-derived holding-session count, windowed total PnL for hero-combo selection, per-combo trade-return Sharpe for bar-opacity encoding) — **not the same concept as the annualized backtest-run Sharpe below**; this is a non-annualized, per-parameter-combo statistic over `pnl_pct` returns, undefined below `min_trades` or at zero variance | `PythonDataService/app/research/recency/stats.py` (`trade_dollar_pnl`, `holding_sessions`, `total_pnl`, `sharpe`) | `app/routers/engine.py::_persisted_trade_net_pnl` (flat-fee branch, `compatibility_profile=None`) — mirrored, not duplicated independently; parity-tested | PRD https://github.com/tim1016/learn-ai/issues/1577; design spec `docs/superpowers/specs/2026-08-16-recency-chart-design.md` §7.1. `holding_sessions` delegates to the canonical calendar (`app/lean_sidecar/trading_calendar.py::trading_session_count`) per `.claude/rules/temporal-rigor.md`. | `PythonDataService/tests/research/recency/test_stats.py` (holding_sessions across a holiday/weekend; total_pnl order-independence; sharpe hand-computed value, ddof=1, min-trades guard; `test_matches_the_canonical_engine_flat_fee_formula` parity test) | canonical |
| Walk-forward analysis (chronological / rolling / anchored splits; optional deterministic train-side candidate selection; TRAIN-to-TEST state pre-roll; frozen/flat TEST spec; fold-local retention; compounded combined OOS curve; alpha-decay slope) | `PythonDataService/app/research/walk_forward/splits.py` (three split policies), `app/research/walk_forward/selection.py` (Sharpe/return/declaration-order selection), `app/research/walk_forward/runner.py` (train/test orchestration + aggregation), `app/research/walk_forward/spy_ema.py` (versioned SPY protocol), `app/research/walk_forward/result.py` (DTOs), `app/research/walk_forward/storage.py` (atomic persistence), `app/routers/walk_forward.py` (generic HTTP boundary), `app/routers/jobs.py` (long protocol worker). Phase 4A shipped 2026-05-06; Phase 4B shipped 2026-08-15. | — | López de Prado, *Advances in Financial Machine Learning* (2018), §7 — verify on next touch (citation is approximate; the split taxonomy, flat-boundary policy, deterministic selection, and retention policy are repository-internal). See `docs/references/walk-forward.md` and `docs/references/spy-ema-normalized-gap-walk-forward.md`. | `PythonDataService/tests/research/walk_forward/test_splits.py`; `test_selection.py`; `test_runner.py` (candidate receipts, frozen hash, pre-roll ledger, fold-local retention, partial/fail-closed paths); `test_spy_ema.py` (18-fold protocol, pinned revision, lineage); `test_storage.py`; `test_endpoint.py`; `tests/research/runs/test_runner_inmemory.py` (FreshCross boundary state) | canonical |
| SPY EMA Exhaustive Run selection and recency (equal-weight fold-local percentile score over TRAIN Sharpe/return; maximum five per fold; spec-hash deduplication; completed-trade recent share and duration-normalized recent/prior rate ratio) | `PythonDataService/app/research/exhaustive_run/selection.py` and `metrics.py`; orchestration and Python-authored aggregates in `runner.py`; shared fold retention in `app/research/walk_forward/metrics.py` | —; Angular only sorts and formats persisted scalars | Repository-internal frozen protocol `spy-ema-exhaustive-run` V1.0; see `docs/references/spy-ema-exhaustive-run.md`. Full two-year results are explicitly selection/look-ahead biased; fixed-gap 18-fold results are OOS. | `PythonDataService/tests/research/exhaustive_run/test_selection.py`; `test_exhaustive_metrics.py`; `test_runner.py`; `PythonDataService/tests/research/walk_forward/test_metrics.py` (`abs=1e-12`, `rel=0` for formula assertions) | canonical |
| Monte Carlo trade-path simulation (reshuffle / resample over a parent run's trade list, equity bands, drawdown / streak / terminal-PnL quantiles, breach probabilities) | `PythonDataService/app/research/monte_carlo/methods.py` (reshuffle/resample primitives, equity_curve, max_drawdown, max_losing_streak), `app/research/monte_carlo/runner.py` (load parent run + simulate + aggregate), `app/research/monte_carlo/result.py` (DTOs), `app/research/monte_carlo/storage.py` (atomic file-backed persistence), `app/routers/monte_carlo.py` (HTTP boundary) — **Phase D shipped 2026-05-06**. Equity-curve compounding matches `app/engine/results/statistics.py` so simulated curves are directly comparable to the parent run's reported metrics. | — | López de Prado, *Advances in Financial Machine Learning* (2018), §7 (block bootstrap discussion) — and standard non-parametric bootstrap (Efron, 1979). Verify both citations on next touch. The reshuffle / resample / forward-projection split is repository-internal; equity-band quantiles use `numpy.percentile` (linear interpolation, default). See `docs/references/monte-carlo.md` for method semantics, breach-probability formula, and the IID-returns assumption that constrains forward-projection horizons. | `PythonDataService/tests/research/monte_carlo/test_methods.py` (multiset preservation, sample-with-replacement, equity-curve compounding, max-drawdown formula, max-losing-streak), `test_runner.py` (orchestration, quantile invariants P5≤P50≤P95, determinism by seed, reshuffle terminal-equity invariance, failure paths, low-sim warnings), `test_storage.py` (round-trip, filter, atomic write, path-traversal defense), `test_endpoint.py` (HTTP boundary, request validation, route-clash with `/{run_id}`) | canonical |
| Null-baseline empirical percentile + small-sample p-value (parent-run metric ranked against a null distribution from buy-and-hold or random EMA window-pair runs) | `PythonDataService/app/research/baselines/generators.py` (buy_and_hold_spec via BarProperty tautology + never-firing exit; random_ema_window_specs sampling fast/slow from a bounded family), `app/research/baselines/runner.py` (parent → N child runs → null-distribution aggregation), `app/research/baselines/result.py` (DTOs), `app/research/baselines/storage.py` (atomic persistence), `app/routers/baselines.py` (HTTP boundary) — **Phase E1 shipped 2026-05-07**. Each baseline run is a normal Phase A `RunLedger` linked back via `parent_run_id`. | — | Phipson & Smyth (2010), *Permutation P-values should never be zero* — `(1 + count(null >= parent)) / (N + 1)`. The "EMA window family" baseline is repository-internal (no external citation; the architecture spec calls it out as one of the canonical Build Alpha null comparisons). See `docs/references/baselines.md` for method-by-method semantics, the BarProperty-tautology trick for buy-and-hold, and the documented engine-flush limitation that leaves `RunMetrics.total_trades = 0` for B&H (equity-derived metrics are still correct). | `PythonDataService/tests/research/baselines/test_generators.py` (B&H tautology shape, EMA-pair `slow > fast` invariant, parameter-range validation, unsatisfiable-constraint rejection), `test_runner.py` (B&H equity metrics, random-EMA generates N runs, child-run persistence + parent_run_id linkage, null-distribution coverage, percentile + p-value in unit interval, extreme-parent p-value lower bound `1/(N+1)`, failure paths, distinct baseline_ids per run), `test_storage.py` (round-trip, filter, atomic write, path-traversal defense), `test_endpoint.py` (HTTP boundary, request validation, route-clash with `/{run_id}`) | canonical |
| ML prediction-set artifact (precomputed predictions as a pinned data artifact: deterministic manifest hash, leakage invariants, bar-clock coverage with per-ref lookup-mode dispatch) | `PythonDataService/app/research/ml/artifact.py` (manifest schema, `compute_rows_hash`, `compute_prediction_set_hash` with chicken-and-egg field exclusion, parquet I/O), `app/research/ml/loader.py` (`PredictionSet.load` — manifest+rows-hash recompute, leakage check, duplicate-timestamp rejection, O(log n) `next_after` lookup), `app/research/ml/coverage.py` (`assert_bar_clock_coverage` — exact_bar_close + next_after_bar_close modes, mixed-mode specs), `app/research/ml/generators/quantconnect_fixture.py` (QC `qc_export.json` → canonical artifact, deterministic hash), `app/research/runs/ledger.py` (`RunLedger.prediction_set_hash` v1.1 schema field), `app/research/runs/runner.py` (load + assert_pairs_with + coverage at the run-pipeline boundary) — **v0.5 plumbing shipped 2026-04-23 (PR #208); QC tutorial parity Phase 1 shipped 2026-05-09 (PR #211–#215)**. | — | SHA-256 (FIPS 180-4) via `app/research/runs/hashing.py::hash_payload`; QC tutorial source pinned at `https://www.quantconnect.com/docs/v2/writing-algorithms/importing-data/streaming-data/precomputed-ml-predictions`; design at `docs/superpowers/specs/2026-05-09-ml-prediction-as-data-v05-design.md` + `docs/superpowers/specs/2026-05-10-quantconnect-precomputed-predictions-parity.md`; authority at `docs/ml-predictions-authority.md`. | `PythonDataService/tests/research/ml/test_artifact.py` (manifest round-trip, hash determinism, path-safe-id, chunk-rows hash sorting), `test_loader.py` (load + leakage + duplicate-ts + next_after edge cases), `test_coverage.py` (exact_bar_close + next_after_bar_close + mixed-mode + missing-field paths — 11 tests), `test_quantconnect_fixture_determinism.py` (re-import produces identical hash pinned at `b8252cfa9a749f5bf592602f3aebc2b3a4ccc6bb0cd41da48a6db7a581342e0e`), `test_quantconnect_fixture_parity.py` (per-row value parity at `atol=1e-9, rtol=0`), `test_quantconnect_fixture_runtime.py` (RunLedger.prediction_set_hash + result_hash pinned end-to-end), `test_e2e_replay.py` (regenerate produces same hash) | **canonical** — Phase 1 hash pinned, 148/148 ML+parity tests pass (2026-05-12) |
| IBKR equity-tier commission model (per-share with $1 floor and 0.5% trade-value cap) | `PythonDataService/app/research/parity/ibkr_commission.py::IbkrEquityCommissionModel` — single canonical implementation. Re-exported from `PythonDataService/app/engine/execution/commission.py` so `app.engine.*` can consume the model without a backwards dependency on `app.research.*`. Engine consumers (Task 12 / PR #340): `FillModel(fee_model=...)` at `app/engine/execution/fill_model.py`, `LeanSetHoldingsSizing(fee_model=...)` at `app/engine/execution/sizing.py`, wired into the cross-engine matrix runs by `app/lean_sidecar/cross_runner.py`. Reconciler-side use continues at `app/research/parity/qc_reconciler.py`. | — | QuantConnect docs for `BrokerageName.InteractiveBrokersBrokerage` + `AccountType.Margin` equity tier; canonical implementation in this file (in-file provenance block per `learn-ai-validation` skill). Formula: `min(max(abs(qty) * per_share, min_per_order), abs(qty) * fill_price * max_pct_of_value)` quantized to cents. | `PythonDataService/tests/research/parity/test_ibkr_commission.py` (per-share, floor at $1, cap at 0.5%, zero-quantity, zero-price edge cases, plus SPY/AAPL/TSLA representative cases — 11 cases); `PythonDataService/tests/engine/test_commission_reexport.py` (engine re-export is the canonical class, not a wrapper) | **canonical** — Phase 3 shipped 2026-05-11 (PR #218); engine-side wiring shipped 2026-05-22 (PR #340 — Task 12) |
| Fee-aware `SetHoldings` sizing (long-only equity, IBKR brokerage model) | `PythonDataService/app/engine/execution/sizing.py::LeanSetHoldingsSizing` with optional `fee_model=IbkrEquityCommissionModel()`. When `fee_model` is set, it solves the largest integer `qty` satisfying `qty*price + fee_model.fee(qty, price) <= portfolio_value*(1 - free_portfolio_value_pct)` via monotonic decrement from the naive floor. When `fee_model` is absent, it retains the legacy fixed-`order_fee` path pinned by the 20-entry SPY fixture. Current consumers are Engine Lab and `app/lean_sidecar/cross_runner.py`; `app/engine/execution/order_sizer.py::OrderSizer` remains a reusable broker-neutral adapter with no IBKR actuation consumer after #1583. | `SimpleFloorSizing` is acceptable only for research / quick-iteration backtests where exact LEAN parity is not required. The former `LivePortfolio` consumer was retired with the entire IBKR order-actuation path on 2026-08-19. | LEAN `QCAlgorithm.SetHoldings` → `IBuyingPowerModel.GetMaximumOrderQuantityForTargetBuyingPower` under `InteractiveBrokersBrokerage` + `AccountType.Margin`. See `docs/references/lean-set-holdings.md` for the math and deliberately narrow port scope. | `PythonDataService/tests/engine/test_sizing.py` — 20-entry SPY golden fixture (`test_lean_sizing_reproduces_every_golden_entry`, `atol=0`) plus fee-aware SPY/AAPL, monotonicity, and zero-budget cases; `PythonDataService/tests/engine/execution/test_order_sizer.py::test_set_holdings_routes_through_lean_buffered_fee_aware_resolver` pins the adapter delegation. | **canonical sizing math** — the real IBKR `LivePortfolio`/adapter boundary and its duplicate integration regression were retired by #1583; no broker-order authority remains. |
| QC trade-level parity reconciler (8-category divergence taxonomy, resolution-aware fixture audit, fill alignment with sequence disambiguation, round-trip P&L pairing with algebraically-propagated tolerance) | `PythonDataService/app/research/parity/qc_reconciler.py` — `reconcile_qc_aapl_phase3` public entry; `DivergenceCategory` `StrEnum` (FIXTURE_INSUFFICIENT / DECISION / DIRECTION / QUANTITY / FILL_PRICE / COMMISSION / PNL / ORDER_TYPE); `_parse_qc_orders` (ISO + numeric ms disambiguation, ambiguous-range rejection); `_audit_fixture` (daily=bar-open match, minute=`[low, high]` containment); `_pair_round_trips` (long-only single-position invariant; `realized_pnl = (exit-entry)*shares - entry_fee - exit_fee`); `_classify_pnl_divergences` (propagated atol = `(|entry_qty|+|exit_qty|)*per_share_pnl_atol + 2*commission_atol`). | — | Internal — design at `docs/superpowers/specs/2026-05-11-phase3-pnl-parity-design.md`; reconciliation report at `docs/references/reconciliations/qc-aapl-phase3.md`; taxonomy documented in `.claude/rules/numerical-rigor.md` → "Trade-level reconciliation taxonomy". | `PythonDataService/tests/research/parity/test_qc_reconciler.py` (canonical schema parsing, fixture audit for both resolutions, alignment with seq disambiguation, classification across all 8 divergence categories, round-trip P&L drift detection), `test_qc_aapl_phase3_trade_parity.py` (Phase 3.5 acceptance — 1 pinned aligned fill 2026-02-10 buy at $273.18 vs QC's $273.24, within propagated tolerance), `test_qc_fixture_smoke.py` (Branch-A/B fee-presence gate) | **canonical** — Phase 3.0 + 3.5 Path A shipped 2026-05-11/12 (PR #218–#223) |
| Reconciler-side CSV → TradeBar fixture reader (LEAN bar-semantics: daily anchors to 09:30/16:00 ET; minute literal NY-local; auto-detected from time-of-day component; Decimal prices) | `PythonDataService/app/research/parity/fixture_data_reader.py::FixtureDataReader` — `iter_bars`, `bar_open_by_date` (daily audit), `find_bar_containing(symbol, fill_time_ms)` (minute audit), `is_minute_resolution` (auto-detection); standalone CSV consumer, not a substitute for `LeanMinuteDataReader`. | — | LEAN daily-bar convention (time=09:30 ET, end_time=16:00 ET) and minute-bar convention (time = literal minute start NY-local). Export source is QC Cloud's `qb.history(Resolution.DAILY \| Resolution.MINUTE)`. | `PythonDataService/tests/research/parity/test_fixture_data_reader.py` (daily/minute auto-detection, `find_bar_containing` exact and edge boundaries, `bar_open_by_date` groupby correctness, tz attachment, symbol filter rejection) | **canonical** — Phase 3 shipped 2026-05-11 (PR #218) |
| Defer-only intraday-trigger fill mode (`NEXT_SESSION_OPEN`) + per-ref prediction lookup-mode dispatch (`PredictionRef.lookup`) | `PythonDataService/app/engine/strategy/spec/engine.py::FillMode.NEXT_SESSION_OPEN` (NY-trading-date eligibility; fills at first minute-bar open of the next trading session; `DEFERRED_FILL_MODES` set), `app/engine/strategy/spec/schema.py::PredictionRef.lookup` (`exact_bar_close` \| `next_after_bar_close` discriminator), `app/engine/strategy/spec/primitives.py::PredictionComparison` (per-ref lookup dispatch using `PredictionRef.lookup`), `app/research/ml/loader.py::PredictionSet.next_after` (O(log n) `bisect_right`), `app/research/ml/loader.py::PredictionLookupError` (runtime backstop for coverage-check escape). | — | Internal — design at `docs/superpowers/specs/2026-05-11-phase3-pnl-parity-design.md` (Path A intraday-trigger fill mode subsection) + Phase 3.5 implementation plan; rationale in `docs/ml-predictions-authority.md` § 7 (historical note: Phase 3.0 xfail closed by Phase 3.5 Path A). | `PythonDataService/tests/research/ml/test_loader.py::*next_after*` (smallest strictly-greater key, none-when-no-later, unsorted-input-handling, `PredictionLookupError` subclassing), `test_coverage.py` (5 next_after_bar_close cases including missing rows + missing fields + fired-ts diagnostics), `tests/research/parity/test_qc_aapl_phase3_trade_parity.py` (Phase 3.5 acceptance — closes the Phase 3.0 `xfail(strict=True)` with single-fill pinned at 2026-02-10) | **canonical** — Phase 3.5 Path A shipped 2026-05-12 (PR #223); multi-day round-trip P&L **not pursued** (decision 2026-05-12 — QC free tier's minute-data trailing window, not a backtesting OOS reserve as earlier framing claimed; full rationale in authority doc § 7 and § 10) |
| Live broker-execution verdict authoring (12-value `ReasonCode` taxonomy, four-value `Verdict` enum, versioned operator-facing templates, per-instance lag-driven verdict thresholds) — **retired 2026-08-27, PR-B of #1813** | No canonical implementation remains. `app/services/broker_activity_reconciler.py` (`author_row_from_event` and its `match_identity` / `compute_lag_breakdown` / `classify_verdict` / `select_template` / `render_template` composition), `broker_activity_templates.py` (the versioned template registry), `app/schemas/broker_activity.py` (the closed `Verdict` and `ReasonCode` taxonomies and `ReconciliationTimingPolicy`), `broker_activity_publisher.py`, `broker_activity_publisher_registry.py`, `broker_activity_reconstruction.py`, `broker_activity_wal.py`, `activity_evidence_matching.py`, `activity_repair_projection.py`, and `app/routers/broker_activity.py` were all deleted by PR-B. The raw first-capture WAL that fed them, `app/engine/live/broker_callbacks.py` (`run_dir/broker_callbacks.jsonl`, ADR 0014 §4), was deleted with them, as was the host runner that owned it. Existing `broker_activity.jsonl` / `broker_callbacks.jsonl` artifacts are **not** deleted; nothing reads them any more. The `LiveStateEnvelope` cursor fields (`last_broker_callbacks_wal_seq`, `last_broker_activity_wal_seq`, `app/engine/live/live_state_sidecar.py`) are deliberately retained: the envelope is `extra="forbid"`, so removing a field would make already-written envelopes unreadable. | `app/research/parity/qc_reconciler.py::DivergenceCategory` was always a **sibling, not a duplicate** — backtest post-hoc parity, not a live per-execution narrative. It is **not** retired, remains canonical for reconciliation, and is referenced by `.claude/rules/numerical-rigor.md`. With this live sibling gone it is simply the only divergence taxonomy left; nothing merged into it and no math moved. | ADR 0014 §4-§5 (host-runner raw callback WAL, projector cursor, backend-authored templates); ADR 0008 §1 (`order_ref` ownership) + amendments 2026-06-22 / 2026-06-25; ADR 0038 (Alpaca is the sole bot-control plane); `docs/references/ibkr-reconciliation.md`; IBKR control-plane decommission issue #1813, PRD #1817. | `tests/services/test_broker_activity_reconciler.py`, `test_broker_activity_templates` coverage inside it, `test_broker_activity_publisher.py`, `test_broker_activity_publisher_sweep.py`, `test_broker_activity_reconstruction.py`, `test_broker_activity_truthfulness_property.py`, `test_broker_activity_wal.py`, `test_broker_activity_wal_migration.py`, `tests/schemas/test_broker_activity.py`, `tests/routers/test_broker_activity.py`, `tests/engine/live/test_broker_callbacks_wal.py`, and `tests/operator/test_broker_activity_health.py` were deleted with the code they proved. | **retired 2026-08-27, PR-B of #1813** — the verdict/template authoring authority is gone, not replaced 1:1; Alpaca Broker V2's SQLite decision receipts are the surviving execution-evidence product and were never authored by this path. |
| Historical IBKR account contamination residual (`broker net − Σ managed namespace fills`) — **retired 2026-08-27, PR-B of #1813** | No canonical implementation composes the residual anymore. `app/engine/live/fleet.py::compute_fleet_contamination` (composed the Σ term with a broker-net fetch into the full residual) and `app/services/fleet_contamination.py::collect_fleet_position_explanations` (supplied the Σ term) were deleted by PR-B — PR-A's fix round 2 had already retired their last production caller (the IBKR broker-net fetcher), leaving both fully dead; PR-B independently re-confirmed zero remaining production callers via a full AST import sweep before deleting. Cascade-orphaned alongside them, each confirmed via the same sweep to have exactly one production caller (the module just deleted, not this PR's stated scope): `app/services/legacy_stale_claim_retirement.py`, `app/services/account_journal_authority.py`, `app/schemas/account_reconciliation.py`. **`app/engine/live/journal_exposure.py` (`fold_execution_exposure`/`project_journal_exposure`/`project_journal_account_exposure`) was retired by PR-C of #1813 on 2026-08-27**, together with `tests/engine/live/test_journal_exposure.py` (9 tests) and the `tests/fixtures/golden/journal-exposure-projection/` golden fixture — the fixture is deleted with the code it proved, never regenerated to make anything pass. PR-B deferred this decision to PR-C rather than take it in a rush; PR-C took it on the following evidence. (1) Zero production callers repo-wide, confirmed at **symbol** level — all eight exported names swept individually, not just the module path. Its sole prior production importer, `fleet_contamination.py`, was deleted by PR-B. (2) The surviving-Alpaca-consumer hypothesis was tested and disproved rather than assumed: the vendor adapter that fed this fold, `app/broker/alpaca/clerk/exposure.py`, and the parity test this module's docstring cited, `tests/broker/alpaca/clerk/test_instance_orders.py::test_alpaca_projection_uses_canonical_execution_fold`, were **both deleted on 2026-08-19 by #1679** ("retire legacy broker control") — over a week before this decommission opened, which is why that citation dangled. Activated SQLite (`app/broker/alpaca/clerk/sqlite/folds.py`) has been Alpaca's position, attribution, and FIFO authority since. (3) The dependency direction settles it: `journal_exposure.py` imported `position_quantity_is_nonzero` **from** the Alpaca SQLite folds, so Alpaca never depended on this module — this module depended on Alpaca's canonical flatness primitive. Cascade-deleted with it: `app/engine/live/account_clerk_journal.py::normalize_broker_event`, a six-line validator whose only external consumer was this fold. PR-B's fix round re-ran the sweep at **symbol** level — the original sweep was module-level and so could not see a symbol orphaned inside a file that still had importers — and deleted four further orphans outright (`app/engine/live/account_identity.py`, the `IBKR_CODE_MEANINGS` cluster in `app/broker/ibkr/event_codes.py`, `IbkrAccountSummary` / `IbkrOrderSpec` in `app/broker/ibkr/models.py`, `confined_wal_path` in `app/services/jsonl_wal.py`). Two further groups were **retained and recorded rather than deleted**, so a later reader can tell "deliberately kept" from "missed". First, **`app/engine/live/live_artifact_io.py` (276 lines) is reduced to `artifact_sha256` in production** — its sole remaining production importer is `app/engine/live/reconcile.py:102`; `artifact_mtime_signature`, `read_parquet_rows`, `read_parquet_tail`, and `list_run_artifacts` became test-only when `routers/live_runs.py`'s three-layer cache was deleted, and `artifact_exists` / `parquet_row_count` / `artifact_size_bytes` / `artifact_mtime_ms` / `LiveArtifactMetadata` / `LiveArtifactReadError` survive only as internal helpers of those four; disposition deferred to PR-C. Second, six files had their **production callers retired by PR-B; their symbols are retained for durable-artifact reads and their tests, disposition deferred to PR-C**: `app/engine/live/account_artifacts.py` (`read_account_freeze`, `write_account_freeze`, `read_legacy_account_events`, `AccountFreezeEvidence`), `app/engine/live/intent_ledger.py` (`LedgerProjection`, `projection_from_envelope`), `app/engine/live/live_state_sidecar.py` (`LiveStateSidecarCorruptError`), `app/engine/live/account_clerk_journal.py` (`read_account_clerk_journal`), `app/schemas/artifact_io.py` (`read_pydantic_artifact`), `app/engine/live/indicator_state.py` (`IndicatorStateRepo`). | The executable IBKR Account Clerk and reconciler are retired. `LiveStateEnvelope.expected_position_by_symbol` survives only as a read comparator. Alpaca no longer adapts JSONL custody into this fold; activated SQLite is its sole position, attribution, and FIFO authority. | ADR 0030; ADR 0037; issues #1024, #1038 decisions 22 and 30, #1039, #1044, #1068, and #1261; IBKR account decommission PRD #1817. | `tests/services/test_fleet_contamination.py` and `tests/services/test_fleet_account_summary.py` were deleted with the code they proved. `PythonDataService/tests/engine/live/test_journal_exposure.py` (9 tests) and the `journal-exposure-projection` golden fixture were deleted by PR-C of #1813 with the module they proved. The one test that exercised the `fleet_contamination.py` composition (`test_contamination_uses_the_canonical_journal_projection`) had already been removed in the PR-B commit that deleted that composition. No surviving test asserts this fold's arithmetic, because no surviving code performs it. | **retired 2026-08-27** — the broker-net residual composition was deleted by PR-B of #1813; `journal_exposure.py` itself, its tests, and its golden fixture were deleted by **PR-C** of #1813 after PR-C verified zero production callers at symbol level and confirmed the Alpaca adapter that once fed it had been gone since #1679 (2026-08-19). Nothing on the live Alpaca path reads this fold; activated SQLite owns that arithmetic. |
| Clerk transaction history projection, IBKR/Postgres lineage — **retired 2026-08-26, PR-A of #1813** | No canonical implementation remains. `clerk_transaction_projection_store.py` (the Postgres store), `clerk_transaction_projection_ibkr.py`, and `clerk_custody_timeline.py` were deleted; `clerk_transaction_projection.py`'s journal-tailing/rebuild/recover, `transaction_history`/`transaction_detail`, cursor encode/decode, and custody-window/lifecycle-fold functions were deleted with them — the file now holds only the shared `ClerkTransactionProjectionUnavailable` exception, still consumed by the active Alpaca/SQLite path. | The active Alpaca/SQLite transaction-history projection (`app/services/sqlite_clerk_transaction_projection.py`, served by `routers/clerk_transactions.py`'s now Alpaca-only `broker: Literal["alpaca"]` endpoints) is unaffected and has its own shared wire schema in `app/schemas/clerk_transaction_projection.py`, but no dedicated row in this registry — a pre-existing gap, not introduced by this retirement. `CANCEL_NAMESPACE` and the legacy `lifecycle_projection_*` retirement (#1224) predate and are unrelated to this row. | ADR 0001 / ADR 0030 amendments; issues #1219–#1224, #1236; IBKR account/control-plane decommission issue #1813, PRD #1817. | `tests/services/test_clerk_transaction_projection.py` and `tests/services/test_clerk_custody_timeline.py` were deleted with the code they proved; `tests/routers/test_clerk_transactions.py` now covers only the Alpaca/SQLite path; `tests/contracts/test_legacy_lifecycle_projection_retirement.py` is unaffected. | **retired 2026-08-26, PR-A of #1813** — the Postgres-backed IBKR projection is gone, not replaced 1:1; no math authority moved or duplicated. |
| Deterministic custody-qualification latency percentiles (p50/p95/p99) | `PythonDataService/app/services/account_custody_qualification.py::nist_r6_percentile` — NIST R6 (`p(n+1)`) interpolation over explicitly supplied nonnegative millisecond samples; the S15 runner reports explicitly named, non-overlapping receipt intervals and does not calculate trading state. | — | NIST/SEMATECH e-Handbook §7.2.6.2, “Percentiles” (R6 interpolation): `https://itl.nist.gov/div898/handbook/prc/section2/prc262.htm` | `PythonDataService/tests/services/test_account_custody_qualification.py::test_nist_r6_percentiles_match_reference_examples` plus invalid-input regressions | **canonical** — deterministic operational evidence only; no broker or UI implementation computes these values. |
| Synthetic Polygon minute → 5-second qualification composition | `PythonDataService/app/broker/alpaca/clerk/sqlite/qualification_polygon_replay.py::synthesize_polygon_5s_bars` (re-exported by `qualification.py`) — creates twelve timestamped contributions, distributes integer volume by quotient/remainder, and places the captured minute high/low/open/close so the production 5-second aggregator exactly reconstructs the source OHLCV. This is test/rehearsal composition only and never authors market or trading state. | — | Issue #1415; `docs/references/alpaca-clerk-synthetic-polygon-composition.md`; pinned Polygon SPY capture and attribution at `PythonDataService/tests/fixtures/polygon_capture/spy_minute_2025-01-13_2025-01-17/attribution.md` (`bars.json` SHA-256 `36d3c9358f4bcbfa89b29ec12493f566fdbd71a61ee2ef9d6c7f3bdd6e90d961`). | `PythonDataService/tests/broker/alpaca/clerk/sqlite/test_qualification.py::test_synthetic_5s_composition_preserves_exact_minute_ohlcv` and `::test_polygon_fixture_replays_through_live_feed_path` (exact decimal OHLC and integer volume, `atol=0`, `rtol=0`). | **canonical for synthetic qualification only** — production aggregation remains `app/broker/ibkr/bars.py::aggregate_realtime_bar`; no runtime feed uses this composer. |
| Dry Run synthetic position projection | `PythonDataService/app/broker/alpaca/clerk/synthetic_broker.py::_project_positions` folds only durable synthetic fills into average-cost `(quantity, signed entry notional)` per symbol. Partial reductions preserve the prior average entry price; side flips open only the residual at the flip fill. Exposure uses the canonical `sqlite/folds.py::position_quantity_is_nonzero` predicate. | No real Alpaca position, mark, P&L, or frontend calculation is a fallback for this synthetic projection. | Internal broker-model rule; `docs/references/synthetic-broker-position-projection.md`. | `PythonDataService/tests/services/test_source_bar_ledger.py::test_synthetic_position_projection_preserves_average_cost_through_reduce_and_flip` (exact values, `atol=0`, `rtol=0`). | **canonical for isolated Dry Run only** — reads the sealed synthetic order ledger and cannot author real-paper custody. |

### Broker-v2 bot panel (S0)

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Bot-gallery day P&L presentation aggregate | `PythonDataService/app/services/broker_v2_panel/gallery_hub.py::_day_pnl` composes the already-canonical attributed `realized_pnl_today` and `open_pnl` components, treating one absent component as zero and returning `None` only when both are absent. | Retired: Angular addition in `bot-tile.component.ts`; the tile now formats `GalleryBotView.day_pnl` verbatim. | `docs/superpowers/specs/2026-08-14-bot-gallery-redesign-design.md` §3.4; component economics and FIFO reference in `docs/references/broker-v2-fifo-pnl.md`. | `PythonDataService/tests/services/test_gallery_hub.py::test_day_pnl_null_safe_projection` (`atol=1e-6, rtol=0`) plus Angular pass-through coverage in `bot-tile.component.spec.ts`. | **canonical presentation aggregate** — does not duplicate the FIFO component calculations. |
| Bot-gallery current-session return | `PythonDataService/app/services/broker_v2_panel/gallery_hub.py::_session_change_pct` computes `(last_session_close - first_session_open) / first_session_open` only from 1-minute bars at or after the canonical session-open boundary supplied by `live_window`. The gallery can render 5-second candles, but never uses that shorter raw buffer to redefine the session baseline. | Retired: Angular derivation from the first/last buffered bars, which could include a prior-session tail. | Internal product formula in `docs/superpowers/specs/2026-08-14-bot-gallery-redesign-design.md` §3.4; no external software port. | `PythonDataService/tests/services/test_gallery_hub.py::test_build_snapshot_session_change_pct_uses_full_session_bars_for_a_five_second_gallery` (`atol=1e-9, rtol=0`). | **canonical presentation aggregate** — Python authors the value; Angular formats it only. |
| Alpaca Clerk attributed position, residual drift, and read-only exact-close plan | For an account with a valid activation fence, `PythonDataService/app/broker/alpaca/clerk/sqlite/folds.py` folds cumulative, identity-bound fill deltas into `positions` and owns `position_quantity_is_nonzero` (`abs(qty) >= 1e-9`) for every exposure/flat verdict; `app/broker/alpaca/clerk/sqlite/reads.py::attributed_positions_by_symbol` aggregates them; `app/broker/alpaca/clerk/sqlite/reconcile.py::plan_account_reconciliation` computes broker-minus-attributed residual only after fresh order evidence is folded; and `app/broker/alpaca/clerk/sqlite/recovery_policy.py::_build_safe_flatten_plan` maps each proven nonzero attribution to the opposite closing side with `abs(attributed_qty)`. `app/broker/alpaca/clerk/active_authority.py` is the sole selector. | There is no JSONL exposure implementation or unactivated fallback. Missing, invalid, conflicting, `OFF_DUTY`, or failed activation leaves the account unavailable. Angular consumes backend-authored flatness verdicts and renders the Python-authored plan; it performs no exposure/flat classification or closing-quantity calculation. | ADR 0035; ADR 0037; `docs/prds/alpaca-account-clerk-sqlite-control-plane.md` R6–R7; pinned contract §3/§6; `docs/references/clerk-position-drift-tolerance.md`; `docs/references/alpaca-sqlite-clerk-invariant-traceability.md`; `docs/references/alpaca-sqlite-clerk-recovery-language.md`. | `tests/broker/alpaca/clerk/sqlite/test_reconcile.py` (fill-before-drift, in-flight suppression, epsilon, foreign identity, and exact positive/negative epsilon exposure boundary); `test_exit.py` (sibling attribution and exact flat verification); `test_recovery_policy.py` (exact opposite side and absolute quantity at `atol=0, rtol=0`, account-wide uncertainty/reconciliation, position-clock, and complete-order safety gates); `test_projections.py` (policy evidence remains complete beyond bounded operation pages and selects the account reconciliation independently); `tests/broker/alpaca/clerk/test_active_authority.py` (selection/failure fence); `tests/broker/alpaca/clerk/sqlite/test_activation_inventory.py` (fresh all-account migration gate); drift tolerance `POSITION_QTY_EPSILON=1e-9`, `rtol=0`. | **sole Alpaca custody math; activation-gated** — `prepare_safe_flatten` is advisory and submits no order; any account without a qualified activation is refused. |
| Effective execution sequencing and FIFO input | `PythonDataService/app/broker/alpaca/clerk/sqlite/economic_projection.py` resolves the current leaves of each correction chain, uses the root execution timestamp plus immutable custody sequence as the deterministic FIFO key, and delegates every FIFO operation to `app/broker/alpaca/clerk/fifo_pnl.py`. `fills.recorded_transition_sequence` is the durable sequence source; unprovable migrated rows fail closed. `execution_coverage.py::prove_execution_coverage_set` is the canonical authored, pure automatic set-equivalence predicate: deterministic aggregate quantity, gross cost, and VWAP evidence over complete cumulative and quarantined-exact sets. `execution_coverage_supersession_fold.py::fold_execution_coverage_superseded` consumes it for direct and accumulated many-to-many coverage, preserving each exact fee and original custody clocks while leaving `positions` unchanged. The S0 `exact_replaces_cumulative` operator proof remains an untouched, temporary one-to-one duplicate. | No legacy JSONL fill projection is selectable or mixed into panel, catalog, chart, or transaction-history responses. Accounts without a valid SQLite activation have no Alpaca execution-economics source. | PRD #1543 stories 18, 19, 28, and 33; ADR 0035; ADR 0037; `docs/architecture/alpaca-clerk-sqlite-pinned-contracts.md` schema-v8 note; `docs/references/clerk-fill-quantity-tolerance.md`. This is authored project logic, not a reused proof. | `tests/broker/alpaca/clerk/sqlite/test_execution_coverage_set_proof.py` (independent equations; strict `QTY_ATOL=1e-9` shares and `PRICE_ATOL=1e-9` currency/share plus the propagated gross-cost envelope, `rtol=0`); `test_folds_execution.py::test_late_exact_execution_after_cumulative_recovery_auto_supersedes_coverage`, `::test_partial_exact_coverage_accumulates_then_auto_supersedes_one_cumulative_fill`, `::test_many_to_many_coverage_supersession_preserves_exact_provenance_and_replay`, and its fail-closed evidence regressions; existing sequencing and projection suites remain listed above. | **sole selected Alpaca execution-economics source** — root-time and ledger-sequence ordering are pinned by tests. |
| FIFO lot-level P&L (realized + open) for Alpaca SQLite fills | `PythonDataService/app/broker/alpaca/clerk/fifo_pnl.py::apply_fill_to_lots` owns FIFO mutation; `compute_open_pnl` owns open-lot valuation and complete-mark semantics; `realized_pnl_for_window` owns closed-lot session aggregation. The SQLite economic and catalog/panel projections compose those primitives rather than repeating the arithmetic. | `Backend/Services/Implementation/PositionEngine.cs` — **parallel, not a duplicate**: operates on EF/Postgres lots for the portfolio engine (different data store, different consumer, different fill scope); see `docs/references/broker-v2-fifo-pnl.md` for the distinction. Per CLAUDE.md rule 5 both instances require a parity test iff they could emit comparable numbers; they cannot (different fill sources), so no cross-stack parity test is required. The deleted JSONL rollup cache is not an authority or consumer. | Standard FIFO inventory method (GAAP / IFRS); Kieso, Weygandt & Warfield, *Intermediate Accounting* (17e), Chapter 8. No external software port. | `PythonDataService/tests/broker/alpaca/clerk/test_fifo_pnl.py`, `tests/broker/alpaca/clerk/sqlite/test_economic_projection.py`, and Broker V2 SQLite catalog/panel projection tests (`atol=1e-9, rtol=0`); fixture and tolerance rationale: `docs/references/broker-v2-fifo-pnl.md` | **canonical** — Python is the sole implementation for the bot-panel P&L path; every live Alpaca consumer reads SQLite effective fills. |
| Account-window FIFO attribution and broker-curve reconciliation (C2/C3) | `PythonDataService/app/broker/alpaca/clerk/sqlite/economic_projection.py::account_pnl_attribution` reads effective account fills at one selected SQLite authority and delegates lot math to the FIFO canonical above. `PythonDataService/app/services/account_pnl_reconciliation.py::reconcile_broker_curve_to_local_pnl` is the sole broker-curve delta comparison: `broker_delta = equity[-1] − equity[0]`, `local_delta = realized_pnl_total + end_open_pnl − start_open_pnl − fee_total`, and `residual = broker_delta − local_delta`. | Angular only renders the single bundled C1/C2/C3 response; it does not calculate a delta, window, residual, tolerance, or divergence. The legacy JSONL/IBKR projections are not fallback data sources for an active SQLite account. | PRD #1460 C2/C3, `docs/prds/2026-08-12-broker-account-desk-lens-redesign.md`; standard FIFO reference above; shared eight-category taxonomy in `app/research/parity/qc_reconciler.py`. | `PythonDataService/tests/broker/alpaca/clerk/sqlite/test_economic_projection.py::test_account_pnl_attribution_includes_both_window_bounds_and_preserves_fifo_rows` and `PythonDataService/tests/services/test_account_pnl_reconciliation.py` (`atol=1e-6, rtol=0`; opening-baseline, fee, coverage, bucket-alignment, matching, and PNL_DRIFT cases). | **canonical; activation-gated** — fail closed as `FIXTURE_INSUFFICIENT` unless execution coverage, fee fidelity, and boundary marks are complete and marks align to the corresponding broker-timeframe bucket; `PNL_DRIFT` names a complete-book numerical mismatch. |
| Bot-panel readiness summary counts | `PythonDataService/app/services/broker_v2_panel/panel_projection_service.py::build_panel_view` counts the backend-authored `ReadinessCheckView.ready` values once and emits `readiness_ready_count` / `readiness_blocked_count`. | Retired: Angular filtering/counting in `operator-readiness.component.ts`; the component now renders the two contract values verbatim. | No external reference; exact finite-cardinality projection defined in `docs/references/broker-v2-readiness-summary.md`. | `PythonDataService/tests/broker/v2panel/test_panel_projection.py::test_panel_composes_cards_rail_and_actions` pins the golden `4 ready / 4 blocked` fixture with exact integer tolerance (`atol=0, rtol=0`); Angular pass-through is covered by `operator-lens.component.spec.ts`. | **canonical** — Python owns the aggregate; transports and UI do not recompute it. |

### Strategies (LEAN-ported algorithms)

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| EMA Crossover Signal | `PythonDataService/app/engine/strategy/algorithms/ema_crossover_signal.py::EmaCrossoverSignalAlgorithm` owns 15-minute EMA/RSI decisions; `app/engine/strategy/signal_program.py::SignalSession` owns their staged advance/settle lifecycle and canonical `EvaluationTrace`; `strategy/registry.py::_build_ema_crossover_signal_program` is the sole public construction seam. The live Action Plan selects the stock; Engine Lab binds execution to the signal stream. **Parity-pinned secondary**: `PythonDataService/app/engine/strategy/spec/evaluator.py::SpecAlgorithm` driven by `app/engine/strategy/spec/fixtures/spy_ema_crossover.spec.json` reproduces the default-SPY decision sequence trade-by-trade. The LEAN twin is `app/lean_sidecar/trusted_samples/ema_crossover_signal.py::EMA_CROSSOVER_SIGNAL_SOURCE`. | `app/engine/strategy/algorithms/spy_ema_crossover.py::SpyEmaCrossoverAlgorithm` — hidden compatibility wrapper for existing ledgers; `app/research/divergence/strategies/s1_ema_crossover.py` — divergence-research-only vectorized parallel, not authority. `SignalSession` does not duplicate EMA/RSI, fills, Action Plan selection, or custody. | LEAN; TradingView parity: `docs/validation/SPY_EMA_Crossover_RSI.pine`; current cross-engine receipt: `docs/references/reconciliations/ema-crossover-signal-lean-2026-07-18.md`; SignalSession trace-root receipt: `docs/references/ema-signal-session.md`. The corpus names all ten validated cross-engine cells and pins its trace root. Indicator parity remains `atol=1e-9, rtol=0`. | `PythonDataService/tests/engine/strategy/algorithms/test_signal_only_ema_crossover.py`; `tests/engine/strategy/test_ema_signal_program.py`; `tests/research/parity/test_cross_engine_study.py`; spec parity at `app/engine/strategy/spec/tests/test_spec_spy_ema_parity.py` | **canonical** — registry-backed staged decision seam added by #1725; execution/custody authority remains outside this row |
| EMA Crossover 2 bps | `PythonDataService/app/engine/strategy/algorithms/ema_crossover_2_bps.py::EmaCrossover2BpsAlgorithm` inherits the full EMA Crossover Signal lifecycle and parameterizes two entry predicates: `strategy/spec/primitives.py::difference_bps(EMA5, EMA10) >= gap_bps` and inclusive `rsi_min <= RSI(14) <= rsi_max`. Defaults are 2/50/70. `registry.py::EmaCrossover2BpsParams` owns finite/bounded validation and strict RSI ordering. The parity-pinned StrategySpec `app/engine/strategy/spec/fixtures/ema_crossover_2_bps.spec.json` receipts the defaults; the LEAN twin reads the same validated runtime parameters from `app/lean_sidecar/trusted_samples/ema_crossover_2_bps.py::EMA_CROSSOVER_2_BPS_SOURCE`. | No independent duplicate lifecycle: both Python and LEAN variants are structurally derived from their absolute-$0.20 counterparts; Angular transports and renders the parameters but computes no signal math. | Internal normalized-gap protocol; formula provenance and exact-Decimal golden rows in `docs/references/spy-ema-normalized-gap-walk-forward.md`; implementation, configuration contract, live paired-run receipts, and pinned LEAN provenance in `docs/references/ema-crossover-2-bps.md`. | `tests/engine/strategy/spec/test_difference_bps_operand.py` (`atol=0, rtol=0` exact `Decimal` fixture); parameterized public signal behavior in `tests/engine/strategy/algorithms/test_signal_only_ema_crossover.py`; default StrategySpec parity in `app/engine/strategy/spec/tests/test_spec_spy_ema_2_bps_parity.py`; independent LEAN fixture `ENG-007` in `tests/integration/reconciliation/test_ema_crossover_2_bps_lean_golden.py` (exact event identity, fill/equity `atol=0.000001`, `rtol=0`); LEAN runtime-parameter and API-boundary tests in `tests/lean_sidecar/test_ema_crossover_template.py` and `test_router_lean_sidecar.py`; Strategy Lab default paired runs 146/147 (38 trades) and custom 4/52/68 paired runs 151/152 (18 trades), both with compatibility `Agree`. | **canonical** — selectable and configurable in Strategy Lab with a trusted LEAN twin and committed trade-level golden proof (2026-08-17) |
| SPY ORB (Opening Range Breakout) | `PythonDataService/app/engine/strategy/algorithms/spy_orb.py` | — | `docs/validation/SPY_ORB_Strategy.pine`, `docs/validation/SPY_ORB_Strategy_Plan.md`, `docs/validation/ORB_Cross_System_Validation_Report.pdf` | TV Pine + PDF report | canonical |
| QQQ ORB | — | — | `docs/validation/QQQ_ORB_Strategy.pine`, `docs/validation/QQQ_ORB_Validation_Report.pdf` | Pine only | external-validated — Python port not yet started |
| RSI Mean Reversion | `PythonDataService/app/engine/strategy/algorithms/rsi_mean_reversion.py` owns the RSI decision math and emits asset-free ENTER/EXIT intents; `app/engine/strategy/signal_program.py::SignalSession` owns its staged advance/settle lifecycle and canonical `EvaluationTrace`; `strategy/registry.py::_build_rsi_mean_reversion_signal_program` is the sole public construction seam (issue #1730). Engine Lab binds them to the backtest signal symbol; the Alpaca adapter may bind them to a paper Action Plan only after accepted evidence or the typed evidence-only human override. **Parity-pinned secondary**: `SpecAlgorithm` driven by `spec/fixtures/rsi_mean_reversion.spec.json` reproduces the hand-coded twin trade-by-trade. | `app/research/divergence/strategies/s2_rsi_mean_reversion.py` — divergence-research-only. Removed: `.NET` and function-based service implementations. | LEAN | `PythonDataService/tests/test_strategy_engine.py`; `app/engine/tests/test_rsi_mean_reversion_parity.py`; spec parity at `spec/tests/test_spec_rsi_mean_reversion_parity.py`; live adapter regression `PythonDataService/tests/services/test_bot_runner.py::test_human_override_strategies_emit_canonical_live_intents`; sealed qualification `PythonDataService/tests/engine/strategy/test_signal_program_qualification_matrix.py::test_validated_settings_corpus_has_a_pinned_trace_root` (parametrized across every registered Signal Program, including this one) | **canonical** — registry-backed staged decision seam added by #1730; Alpaca evidence-only deployment requires immutable human override receipts |
| SMA Crossover | `PythonDataService/app/engine/strategy/algorithms/sma_crossover.py` owns golden/death-cross decisions and emits asset-free ENTER/EXIT intents; `app/engine/strategy/signal_program.py::SignalSession` owns its staged advance/settle lifecycle and canonical `EvaluationTrace`; `strategy/registry.py::_build_sma_crossover_signal_program` is the sole public construction seam (issue #1730). Engine Lab binds them to the backtest signal symbol; the Alpaca adapter may bind them to a paper Action Plan only after accepted evidence or the typed evidence-only human override. **Parity-pinned secondary**: `SpecAlgorithm` driven by `spec/fixtures/sma_crossover.spec.json` reproduces the hand-coded twin trade-by-trade. | `app/research/divergence/strategies/s3_sma_crossover.py` — divergence-research-only. Removed: `.NET` and function-based service implementations. | LEAN | `PythonDataService/tests/test_strategy_engine.py`; `app/engine/tests/test_sma_crossover_parity.py`; spec parity at `spec/tests/test_spec_sma_parity.py`; live adapter regression `PythonDataService/tests/services/test_bot_runner.py::test_human_override_strategies_emit_canonical_live_intents`; sealed qualification `PythonDataService/tests/engine/strategy/test_signal_program_qualification_matrix.py::test_validated_settings_corpus_has_a_pinned_trace_root` (parametrized across every registered Signal Program, including this one) | **canonical** — registry-backed staged decision seam added by #1730; Alpaca evidence-only deployment requires immutable human override receipts |
| SPY RSI-range A (EMA-gap + MACD entry; ADX<15 exit) | `PythonDataService/app/engine/strategy/algorithms/spy_strategy_a.py` (extends `_rsi_range_base.RsiRangeStrategy`) emits asset-free ENTER/EXIT intents (#1700); `app/engine/strategy/signal_program.py::SignalSession` owns its staged advance/settle lifecycle and canonical `EvaluationTrace`; `strategy/registry.py::_build_spy_strategy_a_signal_program` is the sole public construction seam (issue #1730). Engine Lab binds them to the backtest signal symbol; the Alpaca adapter may bind them to a paper Action Plan only after accepted evidence or the typed evidence-only human override. | — | Internal — no external port reference; long-only 15-min RTH | `app/engine/tests/test_strategies_abc.py` (gate-wiring + rollback-blocked-entry unit tests); golden fixture `ENG-008` (`PythonDataService/tests/fixtures/test_strategy_parity_fixtures.py`) pins the pre-port trade_log as the #1700 intent-port's refactor-neutrality receipt; live adapter regression `PythonDataService/tests/services/test_bot_runner.py::test_human_override_strategies_emit_canonical_live_intents`; sealed qualification `PythonDataService/tests/engine/strategy/test_signal_program_qualification_matrix.py::test_validated_settings_corpus_has_a_pinned_trace_root` (parametrized across every registered Signal Program, including this one) | **canonical** — registry-backed staged decision seam added by #1730; Alpaca evidence-only deployment requires immutable human override receipts |
| SPY RSI-range B (Supertrend + ADX>20 + MACD entry; ADX<20 exit) | `PythonDataService/app/engine/strategy/algorithms/spy_strategy_b.py` emits asset-free ENTER/EXIT intents (#1700), same binding rules as Strategy A; `app/engine/strategy/signal_program.py::SignalSession` owns its staged advance/settle lifecycle and canonical `EvaluationTrace`; `strategy/registry.py::_build_spy_strategy_b_signal_program` is the sole public construction seam (issue #1730). | — | Internal | `app/engine/tests/test_strategies_abc.py`; golden fixture `ENG-008`; live adapter regression `PythonDataService/tests/services/test_bot_runner.py::test_human_override_strategies_emit_canonical_live_intents`; sealed qualification `PythonDataService/tests/engine/strategy/test_signal_program_qualification_matrix.py::test_validated_settings_corpus_has_a_pinned_trace_root` (parametrized across every registered Signal Program, including this one) | **canonical** — registry-backed staged decision seam added by #1730; Alpaca evidence-only deployment requires immutable human override receipts |
| SPY RSI-range C (ADX>20 + ADX-rising entry; ADX<15 exit) | `PythonDataService/app/engine/strategy/algorithms/spy_strategy_c.py` emits asset-free ENTER/EXIT intents (#1700), same binding rules as Strategy A; `app/engine/strategy/signal_program.py::SignalSession` owns its staged advance/settle lifecycle and canonical `EvaluationTrace`; `strategy/registry.py::_build_spy_strategy_c_signal_program` is the sole public construction seam (issue #1730). | — | Internal | `app/engine/tests/test_strategies_abc.py`; golden fixture `ENG-008`; live adapter regression `PythonDataService/tests/services/test_bot_runner.py::test_human_override_strategies_emit_canonical_live_intents`; sealed qualification `PythonDataService/tests/engine/strategy/test_signal_program_qualification_matrix.py::test_validated_settings_corpus_has_a_pinned_trace_root` (parametrized across every registered Signal Program, including this one) | **canonical** — registry-backed staged decision seam added by #1730; Alpaca evidence-only deployment requires immutable human override receipts |
| SPY RSI-range base (shared infrastructure for variants A/B/C) | `PythonDataService/app/engine/strategy/algorithms/_rsi_range_base.py` owns the shared RSI-range entry filter, ADX exit, and (since #1700) the ENTER/EXIT `SignalIntent` emission + `rollback_blocked_entry` lifecycle all three variants share. | — | Internal | (exercised transitively via A/B/C tests); golden fixture `ENG-008` | canonical-supporting |
| Offline-hours IBKR bot-trading replay (SPY + TSLA) — **retired composition; registered no math** | No canonical implementation remains. The former `LiveEngine`/`LivePortfolio` replay composition, replay broker, virtual clock, coordinator, API, fixtures, and UI were removed by #1583. Strategy and fill authorities used elsewhere are unaffected. | — | Current engine ownership: `docs/architecture/engine-authority-map.md`; historical design detail: Git history | Structural retirement contract: `PythonDataService/tests/contracts/test_ibkr_order_actuation_retirement.py` | **retired 2026-08-19** — no math authority moved or duplicated. |

### Portfolio / valuation

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Position mark-to-market valuation (position × live price aggregation) | `Backend/Services/Implementation/PortfolioValuationService.cs::ComputeValuationInternal` (compliant — pure aggregation) | — | Elementary accounting; no external reference | `Backend.Tests/Unit/Services/PortfolioValuationServiceTests.cs` | **pending rule-5 review** — marks-to-market correctly (position × live price). **⚠️ Caution (2026-06-14):** the same service still aggregates stale entry Greeks into `NetDelta/NetGamma/NetTheta/NetVega` (VCR-0005); those fields are misleading and are scheduled for hard-delete in remediation Phase 9. Do NOT bind to GraphQL `netDelta/netGamma/netTheta/netVega` for hedging decisions — use `getPortfolioLiveGreeks` / `getDollarDelta` / `getPortfolioVega` instead. |
| Lot-level FIFO accounting (realized PnL, cost-basis allocation, position rebuild from trade log) | `Backend/Services/Implementation/PositionEngine.cs::{RebuildPositionsAsync,ApplyTradeInternal}` | — | Standard FIFO inventory method (GAAP/IFRS); no external port reference, no paper to cite — well-known accounting arithmetic | `Backend.Tests/Unit/Services/PositionEngineTests.cs` (FIFO determinism, mark-to-market, partial-fill cost basis); `PortfolioValidationService.cs::Test1_FifoAccounting` runtime suite | **canonical-in-dotnet-justified** (per finding F-0010, closed 2026-05-06) — data lives in EF/Postgres; trade-replay-via-Python would round-trip every fill without simplifying anything. Documented exception per the contract's "math may live in any layer that fits the use case" rule. |
| Portfolio reconciliation | `Backend/Services/Implementation/PortfolioReconciliationService.cs` | — | Internal | `Backend.Tests/Unit/Services/PortfolioReconciliationServiceTests.cs` | pending rule-5 review — same reasoning as above |
| Portfolio scenario / what-if (theoretical option value across spot, time, IV grid) | `PythonDataService/app/services/portfolio_scenario.py` + `app/routers/portfolio.py::POST /api/portfolio/scenario` (**Phase 2.1 shipped 2026-04-26**, commit `d9738a5`). `Backend/Services/Implementation/PortfolioRiskService.cs::RunScenarioAsync` is now a typed-HttpClient passthrough that aggregates the Python response. | — (the .NET shock-propagation from stored `EntryDelta`/`EntryVega`/`EntryTheta` was deleted in `d9738a5`) | Hull §19 (Greek Letters), put-call parity bounds; canonical Python options authorities `bs_greeks.py` / `quantlib_pricer.py` / `volatility/solver.py` | `PythonDataService/tests/services/test_portfolio_scenario.py` — 8-case golden fixture for a 3-leg strategy across 5×5 (spot, time) grid at `atol=1e-6 rtol=1e-6` | **canonical** |
| Portfolio live Greeks (current-time delta, gamma, theta, vega per position) | `PythonDataService/app/routers/portfolio.py::POST /api/portfolio/live-greeks` + `IPolygonService.PortfolioLiveGreeksAsync` passthrough (**Phase 2.1 shipped 2026-04-26**, **consumers shipped 2026-04-27**) | — (the .NET shock-propagation from stored `EntryDelta`/`EntryVega` was deleted in commit `334d419`; `ComputeDollarDeltaAsync` and `ComputePortfolioVegaAsync` now project DB positions into `PortfolioScenarioPositionDto`, call `PortfolioLiveGreeksAsync`, and aggregate per-leg recomputed Greeks) | Hull §19; `bs_greeks.py` canonical | `Backend.Tests/Unit/Services/PortfolioRiskServiceTests.cs` 7/7 pass with mocked live-Greeks responses; cross-engine math validation is via `PythonDataService/tests/services/test_portfolio_scenario.py` | **canonical** |
| Portfolio risk (Greek aggregation across positions, post-recompute) | `Backend/Services/Implementation/PortfolioRiskService.cs` (aggregation only — **compliant** once Greeks themselves come from Python passthrough per Phase 2.2) | — | — | `Backend.Tests/Unit/Services/PortfolioRiskServiceTests.cs` | **pending — will be canonical post-Phase 2.2** — aggregating Python-supplied Greeks in .NET is rule-5 compliant; the violation is *computing* the Greeks in .NET |
| Strategy attribution (trade ↔ strategy linking) | `Backend/Services/Implementation/StrategyAttributionService.cs` | — | — | `Backend.Tests/Unit/Services/StrategyAttributionServiceTests.cs` | **not math** — persistence only, compliant |

### Research / divergence pipeline

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Bar divergence (Polygon vs TradingView) | `PythonDataService/app/research/divergence/analysis/bar_divergence.py` | — | `docs/tv-polygon-validation-gotchas.md`; TV Pine files in `docs/validation/` | `PythonDataService/tests/research/divergence/test_bar_divergence.py` | canonical |
| Trade divergence | `PythonDataService/app/research/divergence/analysis/trade_divergence.py` | — | Internal (reconciliation taxonomy from `reconcile-backtest` skill) | `NONE — pending` | pending-fixture |
| Dividend adjustment | `PythonDataService/app/research/divergence/ingest/dividend_adjuster.py` | — | Internal — empirically derived from TradingView/Polygon close-price step changes at dividend ex-dates (see module docstring); hardcoded SPY dividend events, not CRSP (F-0030 research: module docstring confirms "Values were recovered empirically from the TV-vs-Polygon close-price step changes"). No external reference. | `NONE — pending` | pending-fixture — reference is now honest; fixture obligation remains |
| Indicator reliability methodology | `PythonDataService/app/research/indicator_reliability.py` | — | `Frontend/src/assets/docs/indicator-reliability-methodology.md`, `docs/indicator-reliability-methodology.md` | `PythonDataService/tests/research/test_indicator_reliability.py` | canonical |

### Research signal validation primitives

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Information coefficient (IC) | `PythonDataService/app/research/validation/ic.py` | — | Lopez de Prado, *Advances in Financial Machine Learning* (2018), §8 (or whichever method is implemented — verify) | `NONE — pending` | pending-fixture — reference needs verification |
| Quantile-based statistics (binning, monotonicity, returns by quantile) | `PythonDataService/app/research/validation/quantile.py` | — | Standard non-parametric statistics (Conover, *Practical Nonparametric Statistics*) | `NONE — pending` | pending-fixture |
| Robustness statistics (block bootstrap, regime breakdowns) | `PythonDataService/app/research/validation/robustness.py` | — | Politis & Romano (1994) for block bootstrap (verify) | `NONE — pending` | pending-fixture — reference needs verification |

### Volatility surface and analytics

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| IV surface fitting | `PythonDataService/app/volatility/fitting.py` | — | (verify which parameterization is implemented — likely SVI per Gatheral 2014, or SABR per Hagan 2002) | `NONE — pending` | pending-fixture — reference verification owed |
| IV surface representation / model objects | `PythonDataService/app/volatility/{surface.py,models.py}` | — | Internal — paired with `fitting.py` | `NONE — pending` | pending-fixture |
| IV30 construction & health checks | `PythonDataService/app/volatility/iv30_health.py` | — | `docs/math-rigor.md` Upgrade 1 (variance-time interpolation, 30-day constant-maturity); related to `app/research/options/iv_builder.py` row above | `NONE — pending` | pending-fixture |
| IV-RV basis (implied vs realized comparison) | `PythonDataService/app/volatility/basis.py` | — | `docs/references/iv-rv-basis-alignment.md` (existing reference doc) | `NONE — pending` | pending-fixture |
| VIX replication (model-free fair-strike variance from option strip) | `PythonDataService/app/volatility/vix_replication.py` | — | Demeterfi, Derman, Kamal, Zou (1999) "More Than You Ever Wanted to Know About Volatility Swaps" — **verify the file matches the paper** | `NONE — pending` | pending-fixture — high-stakes provenance: VIX-replication math must cite the paper exactly |
| Price normalization (forward / log-moneyness / strike scaling) | `PythonDataService/app/volatility/price_normalization.py` | — | Internal — verify which normalization is in use | `NONE — pending` | pending-fixture |
| Day-count and annualization conventions | `PythonDataService/app/volatility/conventions.py` | — | Standard convention (ACT/365 or ACT/252 — verify) | `NONE — pending` | pending-fixture |
| IV provenance metadata | `PythonDataService/app/volatility/iv_provenance.py` | — | Internal — infrastructure for IV traceability | `NONE — pending` | canonical-supporting |
| IV analytics (aggregate IV statistics) | `PythonDataService/app/volatility/analytics.py` | — | Internal | `NONE — pending` | pending-fixture |

### Edge research / volatility / regime

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Edge score (composite ranking) | `PythonDataService/app/engine/edge/edge_score.py` | — | Internal — composite of underlying signals (VRP, regime, realized vol, etc.) | `NONE — pending` | pending-fixture |
| Variance Risk Premium (VRP) | `PythonDataService/app/engine/edge/vrp.py` | — | Bakshi & Madan (2000), or Carr & Wu (2009) — verify which definition | `NONE — pending` | pending-fixture — reference verification owed |
| Regime clustering | `PythonDataService/app/engine/edge/regime_clustering.py` | — | Verify which method (HMM per Hamilton 1989, k-means, GMM); test in `tests/edge/test_regime_clustering.py` uses `fit_gaussian_hmm` | `tests/edge/test_regime_clustering.py` (partial — only EM convergence + posterior shape) | pending-fixture — reference verification owed |
| Regime drift detection | `PythonDataService/app/engine/edge/regime_drift.py` | — | Internal | `NONE — pending` | pending-fixture |
| Regime-conditioned strategy evaluation | `PythonDataService/app/engine/edge/regime_strategy_eval.py` | — | Internal | `NONE — pending` | pending-fixture |
| Robustness statistics (edge-level) | `PythonDataService/app/engine/edge/robustness_stats.py` | — | (likely overlaps with `app/research/validation/robustness.py` — verify whether one calls the other or duplicates) | `NONE — pending` | pending-fixture |
| Spread model (transaction cost / bid-ask) | `PythonDataService/app/engine/edge/spread_model.py` | — | Internal — verify whether linear, sqrt-impact, or empirical | `NONE — pending` | pending-fixture |
| Trade simulator (edge-context fills) | `PythonDataService/app/engine/edge/trade_simulator.py` | — | Internal — separate from `engine/execution/fill_model.py`? verify | `NONE — pending` | pending-fixture |
| Portfolio aggregator (cross-asset / cross-strategy combination) | `PythonDataService/app/engine/edge/portfolio_aggregator.py` | — | Internal | `NONE — pending` | pending-fixture |
| Cross-asset runner | `PythonDataService/app/engine/edge/cross_asset_runner.py` | — | Internal — orchestration | `NONE — pending` | canonical-supporting |
| Period splitter (train/test or in/out-of-sample slicing) | `PythonDataService/app/engine/edge/period_splitter.py` | — | Internal | `NONE — pending` | pending-fixture |
| Threshold events (signal binarization) | `PythonDataService/app/engine/edge/threshold_events.py` | — | Internal | `NONE — pending` | pending-fixture |
| Realized vol — high-frequency (intraday) | `PythonDataService/app/engine/edge/features_realtime/hf_realized_vol.py` | — | Andersen-Bollerslev-Diebold-Labys (2003) realized variance estimator family — verify | `NONE — pending` | pending-fixture — reference verification owed |
| Realized vol — close-to-close | `PythonDataService/app/engine/edge/features_realtime/realized_vol.py` | — | Standard close-to-close σ estimator — verify | `NONE — pending` | pending-fixture |
| IV30 constructor (realtime) | `PythonDataService/app/engine/edge/features_realtime/iv30_constructor.py` | — | Same family as `app/volatility/iv30_health.py` — possible duplicate; resolution owed | `NONE — pending` | pending-fixture — possible duplicate of `app/volatility/iv30_health.py` |
| Delta inversion feature | `PythonDataService/app/engine/edge/features_realtime/delta_inversion.py` | — | Internal | `NONE — pending` | pending-fixture |
| Regime features (realtime) | `PythonDataService/app/engine/edge/features_realtime/regime_features.py` | — | Internal | `NONE — pending` | pending-fixture |
| Forward realized-vol labels | `PythonDataService/app/engine/edge/labels_oracle/forward_rv.py` (+ `hf_forward_rv.py` HF variant) | — | Internal — supervised-learning label generation | `NONE — pending` | pending-fixture |
| Edge confidence calibration | `PythonDataService/app/engine/edge/{confidence.py,calibration/confidence.py}` | — | Verify which is canonical and which is consumer | `NONE — pending` | pending-fixture — internal duplication owed for resolution |

### Research signal scoring (IC-based pipeline)

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Research signal backtest engine | `PythonDataService/app/research/signal/{backtest.py,engine.py}` | — | Internal — separate-purpose research engine, distinct from `app/engine/` (LEAN-ported event-driven backtester) | `NONE — pending` | pending-fixture |
| Walk-forward analysis | `PythonDataService/app/research/signal/walk_forward.py` | — | Lopez de Prado, Advances in Financial ML (2018), §7 — verify | `NONE — pending` | pending-fixture |
| Signal diagnostics (mean/std/active-pct/autocorrelation) | `PythonDataService/app/research/signal/diagnostics.py` | — | Internal | `NONE — pending` | pending-fixture |
| Signal standardization (z-score / rank) | `PythonDataService/app/research/signal/standardize.py` | — | Standard z-score / rank transformation — verify | `NONE — pending` | pending-fixture |
| Regime-conditioned scoring (research) | `PythonDataService/app/research/signal/regime.py` | — | Internal — overlaps with `app/engine/edge/regime_*` (resolution owed) | `NONE — pending` | pending-fixture |
| Signal graduation criteria | `PythonDataService/app/research/signal/graduation.py` | — | Internal | `NONE — pending` | pending-fixture |

### Research feature engineering

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Research TA features (momentum_5m, rsi_14, realized_vol_30, volume_zscore) | `PythonDataService/app/research/features/ta_features.py` | RSI computation overlaps with `engine/indicators/rsi.py` (Wilders, canonical) and `services/ta_service.py` (pandas-ta) — see RSI row above for the 3-way duplication | pandas-ta (external) | `NONE — pending` | pending-fixture — RSI duplication is the notable concern |
| Research options features (IV30, IV rank) | `PythonDataService/app/research/features/options_features.py` (`compute_iv_30d`, `compute_iv_rank` over rolling 60-day window with `min_periods=30`) | — | Internal — IV rank is standard rolling (max−min)/(max−min) over window | `NONE — pending` | pending-fixture |

### Data / transport (rule-5 compliant by design)

| Concept | Canonical | Legacy / duplicates | Reference | Validated against | Status |
|---|---|---|---|---|---|
| Technical analysis endpoint fan-out | `Backend/Services/Implementation/TechnicalAnalysisService.cs` | — | n/a (transport) | `Backend.Tests/Unit/Services/TechnicalAnalysisServiceTests.cs` | **not math** — pure HTTP passthrough to Python `/api/indicators/calculate`. Compliant. |
| Market data, bars, aggregates | `Backend/Services/Implementation/MarketDataService.cs` | `PolygonService.cs` (external client) | Polygon.io Aggregates v2 API | `Backend.Tests/Unit/Services/MarketDataServiceTests.cs`, `PolygonServiceTests.cs` | transport — not a math concept |
| Sanitization endpoint fan-out | `Backend/Services/Implementation/SanitizationService.cs` | — | n/a (transport) | (existing tests, if any) | **not math** — HTTP passthrough to Python `/api/sanitize`. Compliant. |
| Research endpoint fan-out | `Backend/Services/Implementation/ResearchService.cs` | — | n/a (transport + EF persistence of research reports) | (existing tests, if any) | **not math** — HTTP passthrough plus persistence. Compliant. |
| Spec strategy backtest passthrough | `Backend/Services/Implementation/SpecStrategyService.cs` | — | n/a (transport) | (existing tests, if any) | **not math** — explicit "thin passthrough" per file header; spec JSON is forwarded without re-validation. Compliant. |
| Portfolio account / order persistence | `Backend/Services/Implementation/PortfolioService.cs` | — | n/a (persistence; FIFO accounting math is in `PositionEngine.cs` per finding F-0010) | (existing tests, if any) | **persistence** — order/account writes; dispatches to `PositionEngine` for math. Compliant once F-0010 classification lands. |
| Data quality metrics (zero-volume rate, flat bars, OHLC violations, fractional-volume bars) | `PythonDataService/app/services/data_quality_service.py` | — | Internal — no external reference; QC metrics are arithmetic counts over bar properties | `NONE — pending` | canonical |
| Pandas-ta vs TradingView validation report | `PythonDataService/app/services/validation_service.py` (thresholds `_EXACT=0.001`, `_CLOSE=0.01`, `_OK=0.1` baked in — verify provenance) | — | Internal — no external reference | `NONE — pending` | validation-only — comparator, not a math producer |

## External data provenance

Per the contract, external data still needs a provenance row once — it's acceptable provenance to say "we trust the vendor, not independently validated."

| Source | Canonical client | Scope | Notes |
|---|---|---|---|
| **Polygon.io REST (Aggregates v2, Options Chain snapshots)** | `PythonDataService/app/services/polygon_client.py` | OHLCV bars, live options snapshots, tickers | Starter plan: 2-year max history, **15-min delayed**, options snapshots only for live contracts. **external: Polygon.io, not independently validated.** Sanitizer at `app/services/sanitizer.py` enforces gap/monotonicity invariants at the ingestion boundary. |
| **TradingView Pine (strategy validation)** | `docs/validation/*.pine` | Parity reference for SPY EMA Crossover, SPY/QQQ ORB | Used for one-time validation runs; not a runtime data source |
| **LEAN Engine (vendored reference)** | `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/` (vendored 2026-04-26) | Ground-truth for indicators and engine semantics | Pinned by commit `7986ed0aade3ae5de06121682409f05984e32ff7` (master HEAD as of 2026-04-26). See `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/attribution.md` for vendored-files manifest. Regeneration triggers a fixture rebuild with commit-message justification. |
| **FRED (pending)** | — | Risk-free rate for BS solver | Proposed in `docs/math-rigor.md` Upgrade 4; not yet wired |

## Adding a new entry

1. You're adding a new math function. Before you write it, decide: is this a new concept, or a new parameterization of an existing one?
2. New concept → add a row here in the matching section (or create a new section). Fill all six columns. Commit the row in the same PR as the implementation.
3. New parameterization (e.g., new indicator period, new option expiry) → no new row; the existing row already covers it.
4. The `Validated against` field must either name a real test file OR say `NONE — pending` with a follow-up plan in the PR description. `Validated against: manually checked` is not allowed.

## Legacy-debt burn-down rule (not a backfill mandate)

Existing canonical files that predate this registry (EMA, SMA, RSI) already carry first-class prose docstrings citing LEAN but not in the 4-field format. **Do not open a PR that just converts every existing docstring.** Instead, when you touch one of these files for a real reason, convert its docstring in the same PR. This keeps the registry honest without creating churn.

## Known rule-5 non-compliance (tracked here, not lost)

These are the remaining known cases where the Python-owns-all-math rule is violated or carries a documented exception. Phase 3's `.NET` backtest violation was closed on 2026-08-06 by deleting the duplicate service, UI, and Python strategy package.
2. **`Frontend/src/app/utils/black-scholes.ts`** — client-side pricing and Greeks for UI responsiveness. Self-labeled `[LEGACY-OK — RENDER-HELPER ONLY, NO NEW CALLERS]` as of 2026-04-27. **Migration status:** Phase 1.1 (extend `strategy_engine.py::analyze_strategy` payload) — DONE (`451394d`). Phase 1.2 (rewire `OptionsStrategyLabComponent` to consume server fields, drop BS imports) — DONE (`451394d`). Phase 1.3 (freeze module) — **SHIPPED** with two intentional callers documented in the file header: (a) `pricing-lab.component.ts` is a comparison harness whose purpose is to show the TS pricer alongside the server pricer side-by-side; (b) `strategy-builder.component.ts` evaluates BS over a 1200-point price grid on every leg edit and would have UI lag if migrated to server round-trips per change. Both callers produce exploratory feedback, not numbers users compare against another number; the canonical authorities `bs_greeks.py` + `quantlib_pricer.py` remain the only math authorities. Phase 1.4 (cross-engine BS parity fixture) — DONE (`451394d` + `69d2bfe` precision-leak fix).
3. **`Backend/Services/Implementation/PortfolioRiskService.cs` / `PortfolioValuationService.cs`** — shock-propagate from stored `EntryDelta` / `EntryVega` / `EntryTheta` instead of recomputing Greeks at current spot/time/IV. **Migration status (2026-05-06):** ✅ **CLOSED.** Phase 2.1 (Python `/portfolio/scenario` endpoint) — DONE. Phase 2.2 (`RunScenarioAsync` rewired to call Python; old EntryVega/EntryTheta shock-propagation deleted) — DONE. Phase 2.3 (`ComputeDollarDeltaAsync` + `ComputePortfolioVegaAsync` switched from stored entry Greeks to `IPolygonService.PortfolioLiveGreeksAsync`) — DONE in commit `334d419` (2026-04-27). 7/7 tests pass. This rule-5 violation is now fully resolved; entry retained as historical record.
4. **`PythonDataService/app/services/rule_based_backtest.py`** — **not a strategy implementation**, it is a configurable rule engine (composable entry conditions: EMA crossover, RSI band, ADX filter, gap filter; multiple exit modes: fixed-bar, indicator-based; parameterized via JSON). The newer `app/engine/strategy/algorithms/` has only fixed strategies, so the originally proposed "thin adapter" cannot be a literal delegate. **Migration: Phase 4 deferred 2026-04-27**, pending design decision on which of three reformulated paths to take: (a) build a new `RuleBasedAlgorithm(Strategy)` subclass in the newer engine, (b) map specific configurations to specific fixed algorithms (fragile), or (c) accept `rule_based_backtest.py` as a permanent alternate engine and document the divide. See `docs/architecture/numerical-authority-migration-plan.md` § Status as of 2026-04-27.
5. **Hardcoded `r = 0.043`** in **6 production locations**: `app/research/options/iv_builder.py:18 DEFAULT_RISK_FREE_RATE`, `app/research/options/contract_finder.py:26 DEFAULT_RFR`, `app/models/strategy.py:48` (Pydantic Field default), `app/models/portfolio.py:97` and `:184` (two Pydantic Field defaults — same pattern, two model classes). `app/services/fred_service.py:32 FALLBACK_RATE = 0.043` is the canonical fallback path and is correct. **Migration: deferred** — full migration to FRED-interpolated rates tracked in `docs/math-rigor.md` Upgrade 4. Per finding F-0029.
