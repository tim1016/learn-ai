# Engine authority map

**Status:** Active
**Last reviewed:** 2026-08-22 (sealed signal-program and synthetic-custody admission boundaries, #1722; Signal Program build-proof admission and legacy seal migration, #1728, ADR 0043; promotion of the six remaining validated programs behind the Signal Program seam, #1730)
**Pairs with:** `docs/math-sources-of-truth.md` (concept-level registry), `docs/architecture/numerical-authority-migration-plan.md` (migration sequencing)

This document answers exactly one question: **which engine owns which job?**

`math-sources-of-truth.md` is the *concept-level* registry — one row per math concept, with the canonical file. This document is the *engine-level* map — one row per execution path, with its declared role. The two are paired: every engine that produces user-comparable numbers must also have its outputs accounted for in the concept-level registry.

If two docs disagree, `math-sources-of-truth.md` wins for math and this doc wins for engine ownership; both should be updated in the same PR.

## The map

| Job | Owning engine (canonical) | Path / entry point | Role | Status |
|---|---|---|---|---|
| Alpaca Broker V2 bot-gallery projection | **Python GalleryHub over the activation-selected catalog/FIFO and live-bar authorities** | `PythonDataService/app/services/broker_v2_panel/gallery_hub.py` via `GET /api/brokers/{broker}/accounts/{account_id}/gallery/{snapshot,stream}` | Renders 5-second candles from the raw live-bar buffer, while retaining the full-session 1-minute buffer as the sole current-session-return baseline. It also composes the Python-authored null-safe day-P&L total, preserves fill event identity, and projects stopped-bot Resume enablement from the full panel action policy. It does not recompute FIFO lots or admission policy. Angular formats and renders the received values only. | **canonical presentation projection** — FIFO components remain canonical in `fifo_pnl.py` / the selected economic projection; action enablement remains canonical in `action_policy.py`. |
| Interactive backtest (stocks, indicator strategies) | **Engine Lab** | `PythonDataService/app/engine/` via router `app/routers/engine.py` | Canonical event-driven engine; LEAN-ported semantics. Produces the Python-authored validation analytics consumed by the unified Engine Lab workbench (`engine_validation_analytics.py`: trailing windows, weekday/hour expectancy, seasonality, rolling stability). New persisted Results evidence uses `equity_downsample.py::build_run_equity_envelope` schema v2: Python authors both the independent mark-to-market audit curve and the closed-trade realized-equity staircase; .NET validates/transports the envelope and Angular renders it. Also hosts internal deployment-validation strategies such as `deployment_validation`, whose purpose is lifecycle/timing validation rather than alpha research. The selectable `ema_crossover_2_bps` entry inherits the canonical EMA signal lifecycle, delegates normalized-gap math to `strategy/spec/primitives.py::difference_bps`, and exposes validated `gap_bps`, `rsi_min`, and `rsi_max` configuration. Its trusted LEAN twin consumes and receipts the same values; .NET transports/persists them and Angular renders the controls without recomputing signals. Default runs 146/147 and custom 4/52/68 runs 151/152 are recorded in `docs/references/ema-crossover-2-bps.md`. | **canonical** — vendored LEAN reference at `references/lean/7986ed0aade3ae5de06121682409f05984e32ff7/` |
| Alpaca paper strategy execution | **Canonical strategy algorithms + Alpaca Clerk adapter** | `PythonDataService/app/services/bot_trade_strategy.py::run_trade_bot` feeds broker-neutral minute bars into the admitted EMA Crossover Signal, SMA Crossover, RSI Mean Reversion, or SPY RSI-range A/B/C (#1700) algorithm under `app/engine/strategy/algorithms/` and routes its asset-free intents through `AlpacaClerk.execute_for_instance`. | The adapter owns runtime dispatch and transport only; each algorithm remains the decision authority named in `docs/math-sources-of-truth.md`, the Action Plan selects the stock, and the Clerk owns order effects and custody. `paper_deploy_service.py` admits current accepted evidence normally and exposes the explicitly dangerous, typed human override only for a human-validated `evidence_only` strategy with a registered runtime. The override is immutable receipt evidence and cannot bypass any runtime, account, custody, liveness, or Start-admission gate. | **canonical production adapter** — validated by `PythonDataService/tests/services/test_bot_runner.py::test_every_admitted_alpaca_paper_strategy_has_a_runtime`, `::test_ema_trade_bot_matches_first_lean_round_trip`, and `::test_human_override_strategies_emit_canonical_live_intents` |
| Alpaca Dry Run strategy execution | **Binding-selected synthetic custody and retained-source evidence** | `PythonDataService/app/services/bot_binding_authority.py::SyntheticBindingAuthority` composes the deterministic `sim:<strategy_instance_id>` Clerk, `source_bar_ledger.py`, and `synthetic_broker.py`; the shared runner consumes the same typed Signal Program and Action Plan as paper execution. | Dry Run seals one synthetic account per strategy instance, ingests the unfiltered source stream into its SQLite ledger before session filtering, and routes only durable simulated fills to its own account-scoped Clerk. It cannot fall back to real-paper custody, real Alpaca ports, or the real account panel. The shared Start/Resume admission re-hashes the selected validation proof and requires the sealed account to equal the selected Clerk snapshot before registration. | **canonical isolated simulation path** — the synthetic position fold is registered in `docs/math-sources-of-truth.md`; validated by source-ledger, account-authority, runner, and panel projection tests. |
| Alpaca account control plane | **Activated SQLite Account Clerk** | `PythonDataService/app/broker/alpaca/clerk/active_authority.py` resolves the broker account before constructing a writer. Only a valid account/generation/database-bound activation fence can select `sqlite/runtime.py` over the event authority in `sqlite/repository.py`; a missing, `OFF_DUTY`, invalid, conflicting, or failed activation selects no custody authority. SQLite exposure/flat decisions share `sqlite/folds.py::position_quantity_is_nonzero`; automatic execution-coverage equivalence is canonically authored by `sqlite/execution_coverage.py::prove_execution_coverage_set`; offline ceremonies live in `scripts/manage_alpaca_sqlite_clerk.py`. ADR 0037's landing gate is `scripts/qualify_alpaca_activation_inventory.py`, which accepts a fresh, explicit, nonempty in-use account inventory and emits a receipt only when every entry verifies against its SQLite database, activation fence, and cutover artifacts. | Exactly one account writer can be installed, and it is SQLite. For cutover account `PA3KWXU1C4C3`, the accepted active generation 2 supplies account-scoped command/effect custody, capture-before-contact, exact broker identity recovery, exclusive broker-contact claims, cancel-first sibling-aware EXIT, monotonic evidence, typed admission/reduction policy, read-only versioned exact-close plans, online backup, mirror rebuild, restore, reset, and human-confirmed cutover. Schema v8 supplies execution slices/effective fills with immutable transition sequencing, attributed positions and FIFO P&L, bot attribution, account history, external-order observation, immutable bot config, decision receipts, and the panel/catalog/chart/consolidated-desk projections. Schema v9 makes the economic actor explicit: immutable `BOT` and `MANUAL_OPERATOR` custody subjects own commands, effects, positions, holds, uncertainties, and manual-ticket folds without pseudo-bots or pseudo-runs. Account FIFO/history reads resolve each effective fill through its immutable effect subject and expose manual origin, so manual fills participate in the one canonical account book without contaminating bot projections. Its `GET`/`POST`/`PUT` ticket endpoints under `/api/brokers/alpaca/accounts/{account_id}/manual-orders` are the only manual-entry path: the browser supplies stable ticket/leg UUIDs and the server supplies operator custody, preview freshness, durable acceptance, asset admission, and broker identity. The former generic `/orders`, cancel, direct hold-clear, and custody-resolve mutations are absent. Limit/GTC and ordered multi-leg tickets remain independently idempotent, require an explicit refreshed continuation after any pause, and cancel only verified manual working orders. Account transaction history projects the immutable custody subject and classifies manual effects from their durable `MANUAL_OPERATOR` subject, while bot-only catalog/panel paths remain strategy-scoped. A data-bearing v8 authority is upgraded only by the stopped, verified `upgrade-v9` ceremony: verified backup, finalized-mirror stage replay, journal/projection parity and integrity proof, durable prepared receipt, atomic swap, and receipt-bound recovery/rollback. Normal startup never rewrites v8. See [ADR 0035](adrs/0035-alpaca-clerk-sqlite-event-sourced-authority.md#decision), [ADR 0037](adrs/0037-sqlite-sole-alpaca-custody-authority.md), and the [pinned v9 contract](alpaca-clerk-sqlite-pinned-contracts.md). #1521 quarantines every ambiguous exact/cumulative execution; #1552 supplies the pure, typed many-to-many automatic set proof; #1554 adds direct exact-to-one-cumulative replacement; and #1557 atomically replaces a proved accumulated exact/cumulative set under the repository lock and commit-time revalidation. The replacement preserves all original exact custody clocks/FIFO sequences, never changes position quantity, and leaves the S0 operator flow untouched. Product reads are SQLite-only; JSONL, PostgreSQL, the process registry, and broker-direct reads are not custody fallbacks. Safe-flatten preparation derives its legs in Python and submits no order; Angular only renders them. | **Sole selectable Alpaca custody authority; activation-gated** — cutover account `PA3KWXU1C4C3`, generation 2 remains the committed qualified example. Every account absent from a fresh operational inventory, or present without a valid activation, is refused; the repository does not claim a universal external account inventory. |
| Alpaca Trader-lens historical proof (C1/C2/C3) | **Broker C1 curve + activation-selected SQLite FIFO read model** | C1: `AlpacaBroker.get_portfolio_history`. C2: `SqliteEconomicProjectionReader.account_pnl_attribution` over effective fills through the requested end boundary. C3: `account_pnl_reconciliation.reconcile_broker_curve_to_local_pnl`; the single-snapshot transport is `GET /api/brokers/{broker}/portfolio-history-proof`. | Alpaca remains the custodian-of-record equity curve. Selected SQLite effective fills are the independent local book, with FIFO owned by Python. C3 compares the broker delta to `realized + end_open − start_open − fees`, returning explicit `atol=1e-6`/`rtol=0` and shared-taxonomy divergences. A green proof requires complete account execution coverage, reported fees, complete boundary marks, and mark observations in the corresponding C1 timeframe buckets. Angular renders the bundled snapshot and backend-authored unavailable reason only. | **canonical read/proof path; activation-gated** — no JSONL, Postgres, duplicate broker-history fetch, or browser calculation fallback. |
| Alpaca Clerk synthetic qualification rehearsal | **Test-only composition over canonical feed, strategy, fault, and Clerk paths** | `PythonDataService/app/broker/alpaca/clerk/sqlite/qualification.py` façade delegates Polygon composition/replay, performance evidence, and the protected synthetic rehearsal to `qualification_polygon_replay.py`, `qualification_performance.py`, and `qualification_synthetic_rehearsal.py`; `scripts/run_alpaca_sqlite_qualification.py` is the one-shot entry point. | Replays pinned Polygon structure through the production 5-second aggregation and decision consumer, drives existing paper-only Alpaca fault hooks through public Clerk paths, and records #1409 worksheets/recovery evidence on an isolated XFS qualification authority. It authors no production signal, fill, custody, or execution math; the concept-level synthetic composer is registered in `docs/math-sources-of-truth.md`. | **canonical qualification harness only** — issue #1415; supplied pre-cutover evidence and now remains the broker-free regression harness for active Alpaca-paper generation 2 on schema v8. |
| Offline-hours IBKR bot trading replay (SPY + TSLA) | **Retired; no owning engine** | The former `offline_replay_*` service/router/schema family, `ReplaySimBroker`, `LiveEngine`, and `LivePortfolio` were removed by #1583. The structural retirement contract is `PythonDataService/tests/contracts/test_ibkr_order_actuation_retirement.py`; historical design detail is in Git history. | No registered route, service, virtual clock, replay broker, or Angular client remains. Engine Lab and its research runners remain the supported replay/backtest authorities. | **retired 2026-08-19** — the deleted composition registered no independent math. |
| Interactive backtest (options strategies) | **Engine Lab options layer** | `PythonDataService/app/engine/options/` + `app/engine/strategy/algorithms/spy_ema_crossover_options.py` | Same engine as stocks; options pricing routes through `bs_greeks.py` / `quantlib_pricer.py` | canonical |
| External LEAN compatibility / reference runs (user-authored `QCAlgorithm`) | **LEAN sidecar** (non-canonical external runner) | Strategy Lab's LEAN-only `lean-source-editor` loads the registered source through `GET /api/engine/strategies/{name}/lean-source`, then explicit custom runs travel through the `lean_engine_run` job to `PythonDataService/app/routers/lean_sidecar.py`, the launcher service with Podman API access, and the pinned `quantconnect/lean` container. Design authority: `docs/architecture/lean-sidecar-lab.md`. | Runs ephemeral user-supplied Python `QCAlgorithm` code in a disposable, network-isolated LEAN container against pre-staged local data. Source viewing/editing does not probe the launcher; launcher readiness gates execution only. Produces raw LEAN artifacts and normalized DTOs for compatibility and audit evidence. The custom source is not registered or persisted as canonical math and never replaces the registered twin in the automatic Python-vs-LEAN parity flow. | **browser custom-source path restored 2026-08-26** — registered source + SHA provenance, undetected-runtime viewing/editing, and explicit LEAN-only execution over the existing sandboxed `algorithm_source` contract. |
| Configurable strategy spec (declarative entry/exit/manage rules over indicators) | **Strategy Spec layer** | `PythonDataService/app/engine/strategy/spec/` (Pydantic schema, `SpecAlgorithm` evaluator, primitives, indicator registry); canonical fixtures at `app/engine/strategy/spec/fixtures/*.spec.json`; HTTP entry `app/routers/spec_strategy.py` (`POST /api/spec-strategy/backtest`, `GET /api/spec-strategy/schema`, `GET /api/spec-strategy/fixtures`); GraphQL passthrough `Backend/GraphQL/SpecStrategyMutation.cs::runSpecStrategyBacktest` via `Backend/Services/Implementation/SpecStrategyService.cs`. | Phase 1 (shipped 2026-05-04): equity-only, single-symbol, indicator-driven entry/exit. The SPY EMA spec is a parity-pinned secondary for `algorithms/ema_crossover_signal.py::EmaCrossoverSignalAlgorithm`: it reproduces the default-SPY signal sequence, while Engine Lab binds its instrument-free intents to the signal stream for backtests and the live Action Plan chooses the traded stock. The hidden `spy_ema_crossover` wrapper is legacy compatibility only, not a second authority. SMA and RSI mean-reversion specs likewise reproduce their hand-coded twins trade-by-trade. **Phase 2.1:** ADX / MACD / Supertrend indicators wired; `PnLPercent`/`PnLPoints` primitives; `survival.CLOSE_ALL` action. **Phase 2.2:** `DrawdownFromPeak`/`BarProperty` primitives. **Phase 2.3 (2026-08-15):** explicit `DifferenceBps` operand for normalized threshold research, exact-Decimal golden-pinned. **Phase 3a:** FastAPI router. **Phase 3b:** .NET passthrough. | parity-pinned secondary — hand-coded `algorithms/*.py` remain math-authority per `docs/math-sources-of-truth.md`; spec layer drift = test failure. Replaces `app/services/rule_based_backtest.py` per migration **Phase 4** |
| Research signal scoring (IC, walk-forward, diagnostics) | **Research signal backtester** | `PythonDataService/app/research/signal/backtest.py` (+ `engine.py`, `walk_forward.py`, `diagnostics.py`) | Research-metric infrastructure; produces IC, hit rates, regime splits, etc. | canonical — separate-purpose engine, not a duplicate of Engine Lab |
| Volatility / edge research | **Edge research** | `PythonDataService/app/engine/edge/` (`edge_score.py`, `regime_clustering.py`, `vrp.py`, `cross_asset_runner.py`) | Edge / regime / VRP analysis; consumes Engine Lab outputs and Polygon snapshots | canonical |
| Options strategy analysis (payoff, POP, Greeks for a hypothetical strategy) | **Options strategy analysis service** | `PythonDataService/app/services/strategy_engine.py::AnalyzeOptionsStrategy` (+ `app/routers/strategy.py`) | Server-side options-strategy analytics consumed through transport-only GraphQL. | canonical — complete server payload; Angular renders it |
| Portfolio scenario / what-if (theoretical option value across spot/time/IV grid) | **Python portfolio scenario service** | `PythonDataService/app/services/portfolio_scenario.py` + `app/routers/portfolio.py` | Recomputes values against current spot/time/IV using the canonical Python options authorities. | **canonical** — Phase 2 shipped 2026-04-26 |
| Portfolio live Greeks (current-time delta/gamma/theta/vega per position) | **Python portfolio live-Greeks endpoint** | `PythonDataService/app/routers/portfolio.py::POST /api/portfolio/live-greeks` | Supplies recomputed Greeks to transport/aggregation consumers. | **canonical** — Phase 2 shipped 2026-04-27 |
| Indicator computation (one-shot, for charts and `/api/indicators/calculate`) | **Pandas-ta one-shot adapters** | The six-indicator compatibility endpoint remains in `PythonDataService/app/services/ta_service.py`. Catalog-backed charts delegate to `dataset_service.calculate_dynamic_indicators`; `chart_service.compute_indicator_results` supplies presentation metadata without fetching or rebucketing, and `chart_indicator_service.py` validates caller-owned bars/recipes for `POST /api/chart/indicators`. | Vectorized indicator computation for non-engine callers (charts, divergence pipeline, indicator reliability). The bot chart endpoint preserves its selected live/Polygon bar sequence and adds no second calculation implementation. | canonical one-shot path — not parity-tested against Engine Lab streaming indicators because the call shape differs; divergences would mean a real bug |
| Alpaca Broker V2 chart-history input window | **Python calendar/warmup projection over Polygon aggregates** | `PythonDataService/app/services/broker_v2_panel/chart_projection_service.py` uses the NYSE calendar in `app/lean_sidecar/trading_calendar.py` and the catalog policy in `app/services/indicator_warmup_policy.py`; transport is `GET /api/brokers/{broker}/accounts/{account_id}/bots/{strategy_instance_id}/chart/history`. | Python derives distinct display and calculation budgets from completed sessions, extends sparse aggregate reads backward, and caps all reads at the documented Polygon two-year entitlement. The response exposes the requested calculation budget and whether available bars satisfied it. Angular draws only `bars`, passes `indicator_bars` unchanged to the canonical one-shot calculator, and renders an explicit limitation when the budget is not met. | **canonical presentation-input projection** — it does not compute indicators, trading signals, or fill economics. |
| Indicator computation (streaming, inside an event-driven backtest) | **Engine Lab indicator framework** | `PythonDataService/app/engine/indicators/` (`sma.py`, `ema.py`, `rsi.py`, `macd.py`, `adx.py`, `supertrend.py`) | LEAN-ported streaming indicators; consumed by Engine Lab strategies | canonical — bit-exact parity with vendored LEAN reference (`references/lean/<sha>/Indicators/`) per `tests/test_indicator_parity.py` |
| Broker-neutral signal decision lifecycle (EMA Crossover, SMA Crossover, RSI Mean Reversion, SPY Strategies A/B/C, Deployment Validation) | **Registry-backed Signal Program family** | `PythonDataService/app/engine/strategy/registry.py`'s seven per-program `_build_*_signal_program` factories (`_build_ema_crossover_signal_program`, `_build_sma_crossover_signal_program`, `_build_rsi_mean_reversion_signal_program`, `_build_spy_strategy_a_signal_program`, `_build_spy_strategy_b_signal_program`, `_build_spy_strategy_c_signal_program`, `_build_deployment_validation_signal_program`) each construct `app/engine/strategy/signal_program.py::SignalProgram`; its `SignalSession` owns an accepted decision clock resolved per instance. That clock comes from one of three places, none interchangeable: a fixed 15-minute consolidated cadence for `ema_crossover_signal` (no `resolution_minutes` parameter); a deploy-configurable consolidated `resolution_minutes` (default 15) for `sma_crossover`, `rsi_mean_reversion`, and `spy_strategy_a`/`b`/`c`; and the raw one-minute bar itself for `deployment_validation` (no `resolution_minutes` parameter and no consolidated bucket — its factory fixes `timeframe_ms=60_000` and the strategy decides inside `on_minute_bar`). The session also owns explicit `advance`/`settle`, staged candidates, and `EvaluationTrace`. `BacktestEngine` commits the stage at its historical order-drain seam. | This is a pure-engine boundary: the session never chooses an Action Plan leg, account, Clerk, broker, or submitted order. `COMMIT` delegates the pre-bound executor; `DISCARD` restores each program's own retryable entry/exit state. An unsettled stage quarantines the next advance as `UNSETTLED_STAGE`. | **canonical decision-cycle authority** for the seven promoted signal programs — numeric indicators remain in the indicator framework and trade/fill economics remain in Backtest execution. Validated by `tests/engine/strategy/test_ema_signal_program.py`, `tests/engine/strategy/test_spy_strategy_c_signal_program.py`, `tests/engine/strategy/test_deployment_validation_signal_program.py`, the registry-parameterized cross-program matrices in `tests/engine/strategy/` (`test_signal_program_qualification_matrix.py`, `test_signal_program_discard_safety.py`, `test_signal_program_mode_parity.py`, `test_signal_program_session_boundaries.py`, `test_signal_program_exit_and_fill_semantics.py`) and `tests/services/test_signal_program_crash_replay.py`, the trace corpora, and the existing LEAN parity suite. |
| Signal Program seal identity and running-build proof (Start/Resume admission) | **`signal_program_admission.py` seal-and-proof boundary** | `PythonDataService/app/schemas/signal_program_seal.py` (two-level `ConfiguredSignalProgramSeal` / `SealedBotProgram`, both self-hashing); `app/services/signal_program_admission.py` (`build_start_program_seal`, `reconstruct_legacy_program_seal`, `prove_running_program_build`, `running_artifact_digest`); registry admission metadata is `app/engine/strategy/registry.py::SignalProgramContract` (`golden_trace_root`, `parameter_units`, `validated_settings`, `artifact_paths`); receipts are minted only by `scripts/run_signal_program_build_qualification.py` after a fresh golden-trace pass, never hand-authored, into `app/data/signal_program_build_receipts.json`. | Seals a deployed program's exact resolved semantics once at deploy time (ADR 0043), then re-hashes the closed signal-decision import closure named by `artifact_paths` — not the whole module graph — at every Start/Resume and requires a matching qualification receipt for the sealed `(program_version, golden_trace_root)`; absent or mismatched evidence returns `PROGRAM_BUILD_UNPROVEN` before any run or effect (`run_admission.py`). `ema_crossover_2_bps` and `spy_ema_crossover` are deliberately `signal_program_contract=None` — they must never inherit the canonical program's identity via `dataclasses.replace()` (issue #1728 defect; guarded by `tests/engine/strategy/test_registry_signal_program_identity.py`) — so they run `NOT_APPLICABLE`, ungated, on their existing non-sealed path. Issue #1730 promoted six more programs (`sma_crossover`, `rsi_mean_reversion`, `spy_strategy_a`, `spy_strategy_b`, `spy_strategy_c`, `deployment_validation`) to their own sealed qualification alongside `ema_crossover_signal`; these two keys remain deliberately unpromoted. It never selects an account, admits a mode, or claims custody. | **canonical admission boundary, layered above the decision-cycle row directly above** — seven programs (`ema_crossover_signal`, `sma_crossover`, `rsi_mean_reversion`, `spy_strategy_a`, `spy_strategy_b`, `spy_strategy_c`, `deployment_validation`) are currently sealed and qualified; validated by `tests/engine/strategy/test_signal_program_qualification_matrix.py` (cross-program corpus/trace-root and closure parity for every sealed program), `tests/engine/strategy/test_signal_decision_digest_closure.py` (EMA-specific AST-recomputed closure parity), `tests/engine/strategy/test_registry_signal_program_identity.py` (no cross-registration identity sharing), and `tests/services/test_signal_program_admission.py`. |
| BS / Greeks / IV math | **Python options authorities** | `app/services/quantlib_pricer.py` (price + Greeks), `app/services/bs_greeks.py` (closed-form), `app/volatility/solver.py` (IV solver), `app/volatility/fitting.py` (surface fits) | Per `docs/architecture/options-math-authorities.md`; the dispatch rules between QuantLib and closed-form live there | canonical |
| Strategy research run ledger (reproducible, hash-addressable run records over `StrategySpec` execution) | **Run ledger** | `PythonDataService/app/research/runs/` (`hashing.py`, `ledger.py`, `result.py`, `runner.py`, `storage.py`) + `app/routers/research_runs.py` (`POST /api/research/strategy-runs`, `GET /{run_id}`, `GET /` listing). Wraps `BacktestEngine.run` over a validated `StrategySpec` and persists a `RunLedger` + `BacktestRunResult` to `PythonDataService/artifacts/runs/<run_id>/`. | Phase A of the Build Alpha-style research pipeline (shipped 2026-05-06). Does not reimplement engine math — it composes the canonical engine + statistics modules and adds canonical-JSON SHA-256 over inputs/outputs to make replays addressable. The substrate for Phase B (workbench), Phase C (walk-forward folds), Phase D (Monte Carlo + robustness), Phase E (baselines + sensitivity) — each consumes this run as its `parent_run_id`. No GraphQL passthrough yet; deferred to Phase B. | canonical — see `docs/references/run-ledger.md` for the hashing scheme + `data_snapshot_id` design |
| Walk-forward analysis (fixed-spec or train-selected out-of-sample evaluation) | **Walk-forward** | `PythonDataService/app/research/walk_forward/` (`splits.py`, `selection.py`, `result.py`, `runner.py`, `spy_ema.py`, `storage.py`) + generic CRUD in `app/routers/walk_forward.py`. The frozen SPY V1 protocol enters through `Backend/Jobs/JobsApi.cs` job type `spy_ema_walk_forward` and Python internal worker `/api/jobs-internal/spy-ema-walk-forward`. Search mode persists every candidate train run, selects by train Sharpe/return/declaration order, pre-rolls state, freezes the exact winner for TEST, and fails closed without auditable evidence. Every child is a normal `RunLedger`; the aggregate is persisted under `artifacts/walk-forward/<wf_id>/`. | Phase 4A shipped 2026-05-06; Phase 4B and SPY protocol V1 shipped 2026-08-15. OOS equity compounds independently-flat successful TEST folds. Angular's `/research-lab/backtests/spy-ema-walk-forward` route starts/cancels the job and only formats/charts persisted Python output. No GraphQL passthrough. | canonical — see `docs/references/walk-forward.md` and `docs/references/spy-ema-normalized-gap-walk-forward.md` |
| SPY EMA selected-candidate exhaustive characterization (fold-local TRAIN ranking, full-data fit, fixed-gap forward stability, and trade recency) | **Exhaustive Run** | `PythonDataService/app/research/exhaustive_run/` + `app/routers/exhaustive_runs.py`; background job `spy_ema_exhaustive` enters through `Backend/Jobs/JobsApi.cs` and `/api/jobs-internal/spy-ema-exhaustive`. It consumes a canonical SPY EMA V1 walk-forward receipt, keeps at most five training candidates per fold, deduplicates exact specs, and persists both a full two-year run and an 18-fold fixed-spec walk-forward for each unique candidate. | The full-period track is explicitly selection/look-ahead biased and is used only for descriptive return/risk/trades/recency. Forward stability uses TEST-only fold metrics. Angular's walk-forward detail page sorts and formats the persisted Python scalars and provides all-fold drill-down. | canonical — see `docs/references/spy-ema-exhaustive-run.md` |
| Recency Chart (accumulating multi-strategy x multi-symbol trade sweep against a rolling display window) | **Recency Chart runner** | `PythonDataService/app/research/recency/` (`grid.py` lazy cartesian expansion + analytic sizing, `runner.py` bounded-concurrency dispatch over `execute_engine_backtest`, `stats.py` per-trade/per-combo statistics, `fingerprint.py` canonical evidence identity, `validation.py` dispatch-time request validation, `persist_client.py` transport); background job enters through `Backend/Jobs/JobsApi.cs` and `POST /api/jobs-internal/recency-chart`. Each run's snapshot persists via `POST /api/recency/snapshots` to `Backend/Services/Implementation/RecencyPersistenceService.cs`, the Recency Chart's sole authority for `RecencyRun`/`RecencyTrade`/`RecencyTradeMembership`; reads are transport-only through `Backend/GraphQL/RecencyQuery.cs`. | Python computes every trade/run statistic (dollar PnL net of commission, holding-session count, windowed total PnL, per-combo Sharpe — registered in `docs/math-sources-of-truth.md`) and the evidence fingerprint (symbol/strategy/params/resolved data policy/fill mode/commission/entry-exit window), so a re-run of the same combo over an overlapping window dedups onto one trade row instead of duplicating evidence. .NET only persists/transports the Python-authored snapshot and joins/filters for the GraphQL read; it derives no PnL, Sharpe, or fingerprint. Angular renders the persisted swimlane. | canonical — PRD https://github.com/tim1016/learn-ai/issues/1577; design spec `docs/superpowers/specs/2026-08-16-recency-chart-design.md` |
| Monte Carlo risk analysis over a parent run's trade list (reshuffle / resample, equity bands, drawdown distributions, breach probabilities) | **Monte Carlo** | `PythonDataService/app/research/monte_carlo/` (`methods.py`, `result.py`, `runner.py`, `storage.py`) + `app/routers/monte_carlo.py` (`POST /api/research/strategy-runs/monte-carlo`, `GET /{mc_id}`, `GET /` listing). Loads a parent `RunLedger`, extracts the per-trade `pnl_pct` array, simulates N paths via `numpy.random.default_rng(seed)`, and aggregates into 5/50/95 percentile equity bands plus drawdown / streak / terminal-PnL quantile dicts plus client-supplied breach probabilities. Persisted under `artifacts/monte-carlo/<mc_id>/`. | Phase D of the Build Alpha-style research pipeline (shipped 2026-05-06). Operates on the parent run's trade list only — does **not** re-run the engine. Reshuffle preserves the trade multiset (tests path dependence); resample draws with replacement (tests sample sensitivity, supports forward projection past historical trade count). Same `random_seed` produces identical simulations across machines. See `docs/references/monte-carlo.md` for method semantics, quantile aggregation, and the 10k server-side simulation cap. Feature 6 (OHLC noise / shifted-bar / synthetic-data tests) deferred to a future `app/research/robustness/` module. No GraphQL passthrough yet. | canonical — see `docs/references/monte-carlo.md` |
| Null-baseline analysis (buy-and-hold or random EMA window pairs vs a parent run; per-metric null distributions + empirical percentile + small-sample p-value) | **Null baselines** | `PythonDataService/app/research/baselines/` (`generators.py`, `result.py`, `runner.py`, `storage.py`) + `app/routers/baselines.py` (`POST /api/research/strategy-runs/baselines`, `GET /{baseline_id}`, `GET /` listing). Loads a parent `RunLedger`, generates N alternative `StrategySpec`s via the requested method, runs each through the canonical engine on the parent's symbol/window/cost model as a child `RunLedger` with `parent_run_id = baseline_id`, and aggregates target metrics into per-metric null distributions with `(empirical_percentile, empirical_p_value)`. Persisted under `artifacts/baselines/<baseline_id>/`. | Phase E1 of the Build Alpha-style research pipeline (shipped 2026-05-07). Two methods in v1: `buy_and_hold` (BarProperty tautology + never-firing exit, no spec-layer change required) and `random_ema_windows` (samples `(fast, slow)` pairs from a bounded family). The other architecture-spec baselines (`random_entries`, `random_signal_timestamps`, `cross_symbol`) need a new spec primitive (`BarIndex` / fixed-time-list firing) or multi-symbol data wiring — deferred. p-value uses the Phipson-Smyth small-sample form `(1 + count(null >= parent)) / (N + 1)`. Server-side cap of 200 samples. No GraphQL passthrough yet. | canonical — see `docs/references/baselines.md` |
| IBKR paper `set_holdings` quantity resolution | **Retired; no broker-execution authority** | `LivePortfolio`, its pending-order queue, and the real `IbkrBrokerAdapter` were removed by #1583. `PythonDataService/app/engine/execution/order_sizer.py::OrderSizer` remains a broker-neutral math primitive but has no IBKR execution consumer. | No strategy, host, account service, or route can turn this sizing result into an IBKR order. The canonical `LeanSetHoldingsSizing` remains available to Engine Lab and LEAN reconciliation independently of the retired broker path. | **IBKR actuation retired 2026-08-19**; sizing math remains canonical under `docs/math-sources-of-truth.md`. |
| Historical IBKR broker-activity evidence — **retired 2026-08-27, PR-B of #1813** | **No owning engine remains.** | `app/services/broker_activity_publisher.py`, `broker_activity_publisher_registry.py`, `broker_activity_reconciler.py`, `broker_activity_reconstruction.py`, `broker_activity_templates.py`, `broker_activity_wal.py`, `app/services/activity_evidence_matching.py`, `app/services/activity_repair_projection.py`, `app/schemas/broker_activity.py`, `app/routers/broker_activity.py`, and the `app/operator/notices/broker_activity_health.py` notice were deleted by PR-B. The `GET /api/live-instances/{strategy_instance_id}/broker-activity` REST and SSE routes are unregistered and gone from the committed OpenAPI contract; the reconnect-recovery sweep that rode the `AutoReconnectMonitor` chain in `app/main.py` went with them. The raw first-capture WAL writer (`app/engine/live/broker_callbacks.py`) was deleted in the same PR — nothing writes `run_dir/broker_callbacks.jsonl` any more. Existing artifact files are **not** deleted; they are simply no longer read. | Alpaca Broker V2 is the sole bot-control and execution-evidence product (ADR 0038); its SQLite decision-receipt and fill folds are unaffected by this retirement and were never fed by this projector. | **retired 2026-08-27, PR-B of #1813** — was read-only compatibility evidence with no control authority (evaluator/host-runner order ownership had already been retired by #1636); see PRD #1817. |
| Legacy IBKR account-instance binding evidence | **Read-only binding ledger and registry** | `app/engine/live/account_binding_ledger.py` and `account_registry.py` retain durable compatibility schemas for historical custody evidence. The Clerk RPC parser and executable writer path were removed by #1583. | No registered bot-control route or host process can create an IBKR run, binding, or lifecycle transition. The remaining ledger/registry values are read evidence only. | **parked read compatibility** — executable IBKR custody retired by #1583. |
| IBKR account admission/session evidence | **Retired; no owning engine** | `app/services/account_gate_promotion.py`, `app/engine/live/account_observation_lease.py`, `app/engine/live/account_session_policy.py`, and the account reconciliation reads (`app/services/account_reconciliation.py`, `app/routers/account_reconciliation.py`) were removed by PR-A of #1813 (2026-08-26). | No result can authorize IBKR order actuation — the read-only classify/display surface these modules provided is gone along with the account-safety/reconciliation composition it fed. ADR 0037's removal of every Alpaca JSONL fallback is unaffected. | **retired 2026-08-26, PR-A of #1813** — IBKR account authority decommissioned; see PRD #1817. |
| Suspended-account IBKR broker effects | **Retired; no broker-effect authority** | The former `account_effect.py`, executable `account_clerk.py`, final-write fence, recovery actions, cancel helpers, and flatten operations were removed by #1583. Historical Clerk journal models remain readable for audit projections only. | No suspended-account recovery, cancel, flatten, or normal-entry request can reach IBKR. Durable historical rows retain their original schema; they do not confer executable authority. | **retired 2026-08-19** — deletion closes the former custody S7 write boundary. |
| Account safety composition read | **Retired; no owning engine** | `app/services/account_safety_snapshot.py`, `app/schemas/account_safety_snapshot.py`, `app/services/account_safety_access.py`, and `GET /api/accounts/{account_id}/safety-snapshot` were removed by PR-A of #1813 (2026-08-26). | The composition this endpoint performed (Account Truth cache/freshness, reconciliation receipt, durable safety verdict, Clerk epoch, observation lease, and Clerk service projection) is gone along with every source it read. | **retired 2026-08-26, PR-A of #1813** — was canonical composition contract for custody S8, issue #1250; superseded by nothing — Account Truth is decommissioned, see PRD #1817. |
| Alpaca FIFO primitives | **Shared pure-Python math over SQLite-owned fills** | `PythonDataService/app/broker/alpaca/clerk/fifo_pnl.py::{apply_fill_to_lots,compute_open_pnl,realized_pnl_for_window}` is consumed by `sqlite/economic_projection.py`; it owns no persistence or active-authority selection. `panel_projection_service.py::build_panel_view` remains a presentation aggregate over its SQLite data source. | `sqlite/decision_receipts.py`, SQLite execution/fill folds, and `sqlite/economic_projection.py` exclusively supply product evidence, attribution, positions/lots, and P&L. The legacy decision journal, namespace fill adapter, and rollup cache are deleted. Reusing `fifo_pnl.py` is math delegation from SQLite-owned rows, not a second execution or persistence authority. | **canonical shared FIFO math primitive; SQLite is the sole data authority** — issue #1296 records the generation-1 origin; ADR 0037 records its retirement. |
| Alpaca recent-order transaction-history read model | **Broker-v2 order read-model projector** | `PythonDataService/app/broker/alpaca/adapter.py::fill_latency_seconds`; `PythonDataService/app/services/broker_order_groups.py`; `PythonDataService/app/routers/brokers.py` (`GET /api/brokers/{broker}/orders` and `/order-groups`). | The Alpaca adapter authors submission-to-fill latency from the broker's canonical millisecond clocks. The grouping view remains available for consumers that need newest-first symbol groups and gross requested, filled, and still-working quantities. Angular's transaction table sorts, filters, and renders Python-authored order values without recomputing latency or trading quantities. | **canonical display read model**; does not alter broker execution or order authority. Validated by `tests/broker/alpaca/test_adapter_orders.py`, `tests/services/test_broker_order_groups.py`, and `tests/broker/test_brokers_router.py`. |
| Account cockpit posture | **Retired; no owning engine** | `app/services/account_cockpit.py`, `app/schemas/account_cockpit.py`, and `app/routers/account_reconciliation.py` (the `/{account_id}/cockpit` route) were removed by PR-A of #1813 (2026-08-26). | The degraded-mode/blockers composition this endpoint authored over durable Clerk/account artifacts and host health is gone; there is no successor. | **retired 2026-08-26, PR-A of #1813** — was canonical read projection for legacy account evidence, no IBKR broker-write authority; see PRD #1817. |
| Historical IBKR account contamination verdict — **retired 2026-08-27, PR-B of #1813** | **No owning engine remains for the residual verdict.** | `app/engine/live/fleet.py::compute_fleet_contamination` and `app/services/fleet_contamination.py` (whole file, not just its IBKR broker-net fetcher) were deleted by PR-B — PR-A's fix round 2 had already retired their last production caller, leaving both fully dead; PR-B re-confirmed via a full AST import sweep before deleting. Cascade-orphaned alongside them, each confirmed via the same sweep to have exactly one production caller (the module just deleted): `app/services/legacy_stale_claim_retirement.py`, `app/services/account_journal_authority.py`, `app/schemas/account_reconciliation.py`. `app/engine/live/journal_exposure.py` was **retired by PR-C of #1813 on 2026-08-27**, with its tests and golden fixture. PR-B left the decision to PR-C rather than take it in a rush; PR-C verified zero production callers at symbol level (all eight exported names, not just the module path — its sole prior production importer was `fleet_contamination.py`) and disproved the surviving-Alpaca-consumer hypothesis against real history: the adapter that fed the fold, `app/broker/alpaca/clerk/exposure.py`, and the parity test its docstring cited were both deleted by #1679 on 2026-08-19, and the dependency ran the other way (this module imported `position_quantity_is_nonzero` **from** the Alpaca SQLite folds). Cascade-deleted with it: `app/engine/live/account_clerk_journal.py::normalize_broker_event`. PR-B's fix round re-ran that sweep at **symbol** level (the original was module-level and could not see a symbol orphaned inside a file that still had importers), deleted four further orphans outright — `app/engine/live/account_identity.py`, the `IBKR_CODE_MEANINGS` cluster in `app/broker/ibkr/event_codes.py`, `IbkrAccountSummary` / `IbkrOrderSpec` in `app/broker/ibkr/models.py`, and `confined_wal_path` in `app/services/jsonl_wal.py` — and **recorded two groups it deliberately kept**, so "retained" stays distinguishable from "missed". `app/engine/live/live_artifact_io.py` (276 lines) is now reduced to `artifact_sha256` in production, its sole production importer being `app/engine/live/reconcile.py:102`; `artifact_mtime_signature`, `read_parquet_rows`, `read_parquet_tail`, and `list_run_artifacts` became test-only when `routers/live_runs.py`'s three-layer cache was deleted, with the remaining helpers reachable only through those four. **PR-C decided: retained** — see "#1813 close-out residue" below for the reasoning. Six further files had their production callers retired by PR-B and keep their symbols for durable-artifact reads and their tests. **PR-C decided: retained** (see "#1813 close-out residue" below): `app/engine/live/account_artifacts.py` (`read_account_freeze`, `write_account_freeze`, `read_legacy_account_events`, `AccountFreezeEvidence`), `app/engine/live/intent_ledger.py` (`LedgerProjection`, `projection_from_envelope`), `app/engine/live/live_state_sidecar.py` (`LiveStateSidecarCorruptError`), `app/engine/live/account_clerk_journal.py` (`read_account_clerk_journal`), `app/schemas/artifact_io.py` (`read_pydantic_artifact`), and `app/engine/live/indicator_state.py` (`IndicatorStateRepo`). | The executable IBKR Account Clerk and reconciler were already gone (PR-A). Alpaca never adapted JSONL rows into this fold: activated SQLite owns Alpaca positions, attribution, and FIFO evidence, unaffected by this retirement. This projection used to report historical contamination but could never cancel, flatten, or submit. | **retired 2026-08-27, PR-B of #1813** — the broker-net residual verdict computation is gone; `journal_exposure.py` itself was then deleted by **PR-C** of #1813 (2026-08-27) once verified dead at symbol level; it was never an Alpaca fallback. Both its proofs are gone with the code they proved: `test_fleet_contamination.py` with PR-B's composition deletion, and `test_journal_exposure.py` plus the `journal-exposure-projection` golden fixture with PR-C's module deletion. |
| Operator transaction history and broker lifecycle, IBKR/Postgres lineage | **Retired; no owning engine** | `clerk_transaction_projection_store.py` (the Postgres read model), `clerk_transaction_projection_ibkr.py`, and `clerk_custody_timeline.py` were deleted; `clerk_transaction_projection.py`'s journal-tail/replay/rebuild orchestration and history/detail readers were deleted with them — the file now holds only the shared `ClerkTransactionProjectionUnavailable` exception. `app/routers/clerk_transactions.py`'s `broker` param is narrowed to `Literal["alpaca"]`; physical Postgres storage `Backend/Migrations/20260725010000_AddClerkTransactionProjection.cs` through `20260725070000_AddClerkTransactionOrigin.cs` is historical schema, not deleted. | The active Alpaca/SQLite transaction-history projection (`app/services/sqlite_clerk_transaction_projection.py`) is unaffected and continues to serve `clerk_transactions.py`'s Alpaca-only endpoints; it has no dedicated row in this map, a pre-existing gap not introduced by this retirement. | **retired 2026-08-26, PR-A of #1813** — see PRD #1817; the Postgres-backed IBKR projection is gone, not replaced 1:1. |
| Run-scoped replay proof (per-run parity receipt) | **Python replay-proof service over the existing BacktestEngine shadow-trace comparator and the shared `strategy_evaluations` runner seam** | `PythonDataService/app/services/run_replay_proof.py` (pure compute + `RunReplayProofService` orchestration); `app/schemas/run_replay.py` (`RunReplayReceipt`); reuses `app/broker/alpaca/clerk/sqlite/qualification_shadow_trace.py::run_shadow_trace_evaluation` (BacktestEngine vs runner-seam trace parity) as its **first production caller**. | Replays a completed Paper/Dry Run's own run-bounded retained bars through two legs — an engine-parity leg (all-COMMIT shadow trace) and a disposition-faithful fidelity leg (production `strategy_evaluations`, settled with the live-recorded dispositions), aligned by deterministic `evaluation_id` and compared **content-by-content** against each receipt's live-captured `trace_root` digest. It classifies every divergence (expected-live-effect vs drift) and persists a durable `RunReplayReceipt`. It introduces **no new math concept**: the hashing reuses the canonical `trace_root` (`app/engine/strategy/signal_program.py`) and the engine leg reuses the already-registered shadow-trace comparator, so `docs/math-sources-of-truth.md` gains no row (only a no-new-concept note). It never gates admission — the permanent evidence-only Paper override is untouched. | **canonical evidence-generation path** — reads + recompute only; PRD #1753 (Direction 2), issues #1758–#1762. |

> **Shared artifact seam under the four research-run rows above.** The storage / id-validation / atomic-write / list-by-parent code under `runs/`, `walk_forward/`, `monte_carlo/`, and `baselines/` is being consolidated into one `app/research/artifact/` module (descriptor-backed `ArtifactStore`). Design ratified 2026-05-26; strangler PR plan starts with `monte_carlo` and finishes with `runs/`. See `docs/architecture/research-artifact-seam.md` for the seven decisions, on-disk-compatibility contract, and per-PR acceptance bar.

### Retired account-recovery daemon-command boundary

The Account Clerk restore command and its daemon-command idempotency repository
were removed by #1583. The host exposes no account-scoped command, child-process
registry, broker writer, or lifecycle actuator. It retains authenticated health,
Gateway-socket evidence, and capability-lease renewal only.

### Read-only Clerk S4 account-binding compatibility

The direct CLI and lifecycle-evaluator producers of `ACTIVE run.start` and
`RETIRED lifecycle.retire` records are gone. The binding ledger and registry
retain durable historical schemas, but the Clerk RPC parser and executable
writers were removed by #1583. No registered route or host process can create a
new binding or lifecycle transition.

### Retired Clerk S5 account-gate and session-policy evidence

`account_gate_promotion.py` (evidence-gated promotion from the Account Truth
gate to Clerk-keyed proof), `app/engine/live/account_observation_lease.py`,
and `app/engine/live/account_session_policy.py` were removed by PR-A of #1813
(2026-08-26), along with their tests (`test_account_gate_promotion.py`,
`test_account_session_policy.py`, `test_account_observation_lease.py`). The
promotion mechanism existed solely to migrate off the (now-deleted) Account
Truth gate; once Account Truth is gone there is nothing left to promote from.
No successor reads or writes this evidence. See PRD #1817.

### Retired Clerk S7 evaluator boundary

The IBKR lifecycle evaluator, its disposition protocol, host bot-process
actuation, and executable Account Clerk retired under ADR 0038 / #1636 and
#1583. Alpaca duty facts are owned by the activation-selected SQLite authority
and its in-process runner. The remaining host process is an authenticated,
evidence-only capability bridge.

### Retired Clerk S8 account cockpit read projection

`account_cockpit.py`, `app/schemas/account_cockpit.py`, and the
`/api/accounts/{account_id}/cockpit` route were removed by PR-A of #1813
(2026-08-26). The degraded-mode composition this projection authored over
immutable historical Clerk status and daemon health has no successor. See
PRD #1817.

### Per-run build evidence is file-backed on purpose

FR-031 requires routine *immutable configuration* reads to come from SQLite, and
the Broker V2 roster does read `mode`, `quantity`, and `carryover_policy` from
`bot_config.config_json`. `ProgramBuildRunEvidence` is deliberately not part of
that migration and stays file-backed under
`live_state/<instance>/run_build_evidence/<run_id>.json`.

The reason is a classification in the sealed-program PRD, not an unfinished
migration: §11.3 lists "running build digest and compatibility-receipt identity"
as **dynamic run evidence** — recorded per run, explicitly *not* semantic bot
identity — alongside admission verdicts, feed health, and runtime watermarks.
FR-031's subject is the immutable configuration projection of §11.1/§11.2, which
this is not. It also has no SQLite representation at all: no table carries
`running_artifact_digest` or `qualification_receipt_hash`, and `config_json` is
built with `sealed_program` and `program_build` excluded precisely so build
content cannot perturb `configuration_hash`.

The read is honest about itself rather than pretending to an authority it does
not have: `ProgramBuildAdmissionFact.state` is always one of `PROVEN` /
`UNPROVEN` / `NOT_APPLICABLE` and is never fabricated, and its `verification`
field discloses whether the value is a fresh live re-proof or a replay of the
durable per-run record. Moving it to SQLite would be a new table plus a
Start/Resume write path — a deliberate change, not a cleanup.

## Retirement register and remaining deprecated paths

| Path | Role today | Replacement | Migration plan reference |
|---|---|---|---|
| `Backend/Services/Implementation/BacktestService.cs` | **Removed** — formerly in-process .NET strategy execution and local statistics | `app/engine/` via `/api/engine/backtest` | **Phase 3 complete** |
| `Frontend/src/app/components/strategy-lab/` | **Active** interactive backtest and analytical-results UI | Engine Lab / `lean-lab` compatibility routes redirect to `strategy-lab` | Current product surface; do not treat the historical Phase 3 removal plan as implementation state |
| `PythonDataService/app/services/strategies/` | **Removed** — formerly a second function-based strategy registry | `app/engine/strategy/algorithms/` | **Phase 3 complete** |
| `Frontend/src/app/components/broker/{bots,bot-control}/` and `/api/live-instances/catalog*` | **Removed** — deprecated IBKR bot list/control projections | Alpaca Broker V2 under `/brokers/alpaca/...` and `/api/brokers/alpaca/...` | **retired 2026-08-06** |
| `Frontend/src/app/utils/black-scholes.ts` | Retained render/comparison helper for Pricing Lab and Strategy Builder; not an authority | Python options services | **legacy-ok render helper** |
| `PythonDataService/app/services/rule_based_backtest.py` (as a separate engine) | Standalone rule-based backtest path | `app/engine/strategy/spec/` (`StrategySpec` schema + `SpecAlgorithm` evaluator). Phase 1 of the spec layer shipped 2026-05-04 with parity-pinned ports of three reference strategies; Phase 4 will translate legacy `rule_based_backtest` params to a `StrategySpec` and delete this file. | **Phase 4** |
| `Backend/Services/Implementation/PortfolioRiskService.cs` (former Greek computation) | **Removed** — the service now transports/aggregates Python results | Python `/portfolio/scenario` and `/portfolio/live-greeks` endpoints | **Phase 2 complete** |
| `Backend/Services/Implementation/PortfolioValuationService.cs` (former theoretical option-value path) | **Removed** — stale entry-Greek theoretical fields no longer define current value | Same as above | **Phase 2 complete** |

### #1813 close-out residue — what PR-C decided to keep, and why

PR-A and PR-B recorded several groups as "disposition deferred to PR-C". PR-C
has shipped, so each is settled below. Two were retired outright and are
recorded in their own rows above: `app/engine/live/journal_exposure.py` and
`app/broker/runtime_snapshot.py`. PR-C also deleted
`app/services/activity_projection_contract.py` (orphaned before PR-B, correctly
disclosed rather than swept there) with the two `live_runs.py` DTOs it
anchored, and `account_clerk_journal.py::normalize_broker_event` as its own
cascade.

`app/schemas/live_runs.py` was **narrowed from 55 top-level symbols to 11**: the
44 that nothing imports were deleted, having gone dead when PR-A/PR-B retired
the `/api/live-runs` surface and the host-runner control plane. The surviving
boundary is exact rather than estimated — six names (`BotDutyOutcomeView`,
`GateResult`, `ReconciliationReceipt`, `MutationRungReceipt`, `ExitReason`,
`RunStatusSidecar`) are imported by five production modules
(`app/schemas/broker_bots.py`, `app/engine/live/account_artifacts.py`,
`app/engine/live/account_registry.py`,
`app/engine/live/reconciliation_receipt.py`,
`app/services/mutation_rung_receipts.py`), closing to 11 with their intra-file
dependencies (`GateResultStatus`, `ReceiptStatus`, `ReceiptOutcome`,
`MutationRungReceiptCode`, `MutationBlockageStageId`). The file has no
`__all__`, no `from app.schemas import live_runs` attribute access, and no
router reference, so no deleted name was reachable dynamically. The deletion is
contract-neutral, re-proved rather than inherited: `export_openapi_contract.py
--check` leaves the contract byte-identical (its `OpenRunbookAction` is
`app/schemas/operator_blocker.py`'s identically-named model, never this file's).
Two consequences worth recording — the deleted set included `FailureRecord` and
`IncidentRecord`, whose docstrings an earlier PR-C commit had rewritten to
describe their retained-residue status, so the deletion supersedes that wording
fix; and `app/services/sqlite_clerk_compat.py::active_sqlite_facade` cited
`HostRunnerHealth.clerks` in prose, so its docstring now records that inventory
as retired while keeping the rule it motivated.

Everything below is **deliberately retained by PR-C**, not missed. Each entry
names the follow-up that would clear it, so "kept" stays distinguishable from
"overlooked" — and so a later reader does not have to re-derive the analysis.

| Retained | Measured state at PR-C | Why PR-C kept it |
|---|---|---|
| `app/engine/live/live_artifact_io.py` (276 lines) | Production surface is `artifact_sha256` alone (`app/engine/live/reconcile.py:102`). `artifact_mtime_signature`, `read_parquet_rows`, `read_parquet_tail`, `list_run_artifacts` are test-only; `artifact_exists`, `parquet_row_count`, `artifact_size_bytes`, `artifact_mtime_ms`, `LiveArtifactMetadata`, `LiveArtifactReadError` are reachable only through those four. | These are read paths onto durable run artifacts that still exist on disk. Deleting a *reader* of data the repo still stores is a different and higher-stakes call than deleting a control-plane surface, and it is not what a feed-consolidation PR is for. |
| Six files whose production callers PR-B retired: `account_artifacts.py` (`read_account_freeze`, `write_account_freeze`, `read_legacy_account_events`, `AccountFreezeEvidence`), `intent_ledger.py` (`LedgerProjection`, `projection_from_envelope`), `live_state_sidecar.py` (`LiveStateSidecarCorruptError`), `account_clerk_journal.py` (`read_account_clerk_journal`), `schemas/artifact_io.py` (`read_pydantic_artifact`), `indicator_state.py` (`IndicatorStateRepo`) | Retained symbols with tests but no production caller. | Same reason as the row above: each is the read side of a durable artifact the repo still holds. |
| `account_clerk_journal.py::fold_account_clerk_custody_statuses` | **Newly identified by PR-C**, and it is this decommission's cascade rather than pre-existing debt: at master `03ce52b6` its sole production consumer was `app/services/account_safety_snapshot.py`, which PR-A/PR-B deleted. Now referenced only by its own `__all__` entry. Deleting it cascades three levels — it takes the 134-line private helper `_custody_status_for_entries` with it, which in turn is the only constructor of `AccountClerkCustodyStatus`, leaving that type a re-export. | The cascade reaches into a shared model type inside a file PR-B deliberately retained, and arrived late in a twelve-item task with no room to verify a three-level deletion properly. Recorded with the chain fully mapped so the follow-up is mechanical rather than exploratory. |
| `app/config.py` / `app/broker/ibkr/config.py` — `live_runner_fleet_poll_interval_seconds`, `live_runner_daemon_breaker_*`, `account_gate_authority` | Defined but read by nothing in production. | Genuinely pre-existing debt: PR-B verified them caller-less at master `03ce52b6`, so no part of #1813 orphaned them. Settings also carry env-var blast radius that a code-only sweep cannot see. |


## Validation-only paths (not deprecated; have a clean separate purpose)

| Path | Why it stays | What it must NOT do |
|---|---|---|
| `Backend/Services/Implementation/PortfolioValuationService.cs::ComputeValuationInternal` (the aggregation arithmetic, not the option-theoretical path) | Pure aggregation over persistence data; rule-5 compliant | Compute FX, theoretical option value with cost-basis lots, or any non-trivial math. If it ever does, the canonical moves to Python and this path becomes a passthrough. |
| `Backend/Services/Implementation/PortfolioReconciliationService.cs` | Persistence-layer reconciliation only | Same as above |
| `Backend/Services/Implementation/StrategyAttributionService.cs` | Persistence-layer trade ↔ strategy linking | Same as above |
| `Backend/Services/Implementation/TechnicalAnalysisService.cs` | HTTP passthrough to `/api/indicators/calculate` | Compute indicators locally |
| `Backend/Services/Implementation/MarketDataService.cs` | Polygon API client for bars / aggregates | Compute indicators or strategy results |
| `docs/validation/*.pine` | TradingView Pine reference for strategy validation | Run in production; one-time validation only |
| `Backend/Services/Implementation/PortfolioValidationService.cs` | Runtime validation suite asserting FIFO accounting correctness, position rebuild determinism, cash accounting integrity, unrealized PnL valuation | Be the canonical implementation of any of those quantities — it's a checker, not a producer. The math under test belongs to `PositionEngine.cs` (F-0010) and `PortfolioValuationService.cs::ComputeValuationInternal`. |

## Reading guide for future contributors

A new feature lands in this repo in roughly this order:

1. **Check `math-sources-of-truth.md`** for the concept being touched. If it has a canonical row, use it.
2. **Check this map** for which engine owns the job. If the job is owned by a specific engine, extend or call that engine — do not introduce a parallel path.
3. **If there is no concept row and no engine row,** decide *first* which engine owns the new work, then add a row to both docs in the same PR as the implementation.
4. **If you find yourself wanting to compute math in `.NET` or Angular,** stop and re-read `AGENTS.md` § Python owns all math. The exception is rare and must be documented in `math-sources-of-truth.md` with `legacy-ok` status and a parity test.

## Why this doc exists, and why it's short

`math-sources-of-truth.md` answers "where is the canonical implementation of *X*?" One row per math concept, granular, dozens of rows.

This doc answers "which engine should I extend / which engine owns this job?" One row per engine path, coarse, ~20 rows.

The two documents are deliberately at different levels of granularity. A coarse engine map plus a granular concept registry is enough metadata for one developer to make consistent decisions over time. Anything more would be ceremony.
