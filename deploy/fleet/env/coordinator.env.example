# Copy to deploy/fleet/env/coordinator.env (chmod 600) for a fleet deployment.
# The coordinator owns only this control-plane volume. Do not put Alpaca,
# IBKR, Polygon, or other broker credentials in this file.
FLEET_COORDINATOR_SERVICE_TOKENS_JSON={}
FLEET_POSTGRES_PASSWORD=
FLEET_AGENT_SERVICE_TOKENS_JSON={}
