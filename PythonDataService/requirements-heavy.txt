# Large / slow-to-install packages — cached in a separate Docker layer.
# Only changes to THIS file trigger a rebuild of the heavy layer.
scipy==1.17.1
numpy==2.2.6
pandas==3.0.1
statsmodels==0.14.6
# pyarrow: parquet I/O for vol-surface cache (test_cache.py) and golden
# fixtures (tests/fixtures/golden/iv30/). Heavy binary, hence this layer.
pyarrow>=18.0,<20
