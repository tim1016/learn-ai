"""Support library for the golden fixture system.

Modules:
    manifest   — Pydantic schema for manifest.json
    conventions — Pinned numerical units and constants
    hashing    — content_sha256 and file_sha256
    io         — normalize_timestamp and Arrow IPC read/write
    compare    — Explicit-tolerance comparator
    registry   — Fixture lookup by ID
"""
