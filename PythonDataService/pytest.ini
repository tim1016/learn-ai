[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    slow: marks tests as slow (training/validation, deselect with '-k "not slow"')
    cross_engine_smoke: marks the four W6mo cross-engine parity cells that run on every PR
