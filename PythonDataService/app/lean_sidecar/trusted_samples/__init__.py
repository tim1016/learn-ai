"""Trusted sample algorithms.

Phase 1 of the LEAN Sidecar Lab runs only hard-coded trusted samples;
no user input crosses the launcher boundary until Phase 3 gates clear.
These samples double as the round-trip fidelity fixture for the
LEAN data-folder contract and the determinism gate.
"""
