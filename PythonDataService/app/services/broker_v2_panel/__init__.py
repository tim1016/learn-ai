"""Broker-v2 bot control panel — projection + action services (S1).

Read/action projections the panel router calls (transport-only). Each service
owns one concern: capability profile, catalog rollup projection, panel
projection, presented-action execution, and chart live/history projection.
"""
